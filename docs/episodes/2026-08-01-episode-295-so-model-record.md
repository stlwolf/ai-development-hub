---
id: "01KYWVH8WHQTQK09CAVCRCW3SF"
title: "Issue #295 so-compare の解決後モデル記録 — 取れる経路と、あえて依存しない経路"
date: 2026-08-01
type: episode
status: stable
related:
  - type: parent_issue
    ref: "https://github.com/stlwolf/ai-development-hub/issues/295"
    reason: "本作業の対象 Issue（cursor / claude の解決後モデルが meta に残らない）"
  - type: design_context
    ref: ".oe/plan-295-so-model-record.md"
    reason: "前の単位（M 系）で作成した調査結果と設計。本単位はこの plan の一部を owner 判断で削って実装する"
---

# Episode: #295 so-compare の解決後モデル記録

本文層は作業中の追記で伸ばす。closure は末尾の「振り返り」節に置き、本文の再掲ではなく本文を指す形で書く。

## 前提（この単位に入る前に決まっていたこと）

**なぜこの作業が始まったか**: `so-compare` の SO 判定は plan / episode / discussion から証跡リンクで引かれ、committed 層に残る。ところが cursor と claude のレーンは「その判定を出したのがどのモデルか」を meta に残しておらず、後から言えなかった。cursor の既定は `auto` で実行時に選ばれ、claude はエイリアス解決と CLI 既定が入るため、記録されている `model_requested` からは確定できない。#290 では返却された判定の約半分がこれに当たった。

前の単位（M1〜M3）で調査と plan を作り、M3 で終端した。その結果 owner が gate 3 で範囲を決めた。

**owner の判断**: claude レーンだけ自動記録する。cursor は `unavailable` を種別つきで記録し、`store.db` 経路は手動調査の手順として文書化する。

**理由**: 非公開の内部 SQLite 形式への自動依存を作らないこと。M1 で見つけた `~/.cursor/chats/**/store.db` の `modelName` は Cursor の更新で無言に取得不能へ落ちうる。**発見は文書として残し、コードは依存しない。**

この判断により、plan の実装ステップ8件のうち cursor の自動取得に当たる部分は実装しない。

## 作業中の記録（随時追記・closure から指せる形で残す）

### N1: worktree と episode 枠

- ブランチは `fix/#295_so_model_record`。prefix は `fix` を選んだ。証跡が記録されない欠陥の修正であり、コミット型が `fix` になるため（`branch-naming` の対応表）。
- worktree は子が自分で作成した（統括は hands-off）。
- episode の置き場は root の `docs/episodes/`。判断理由は、対象が root の `scripts/so-compare.sh` と `canonical/skills/so-compare/SKILL.md` であり、特定 project 配下の資産ではないため。既存の root episode（#113 / #149）も canonical skill の作業を置いており、前例と整合する。

### N2: claude レーンの実装

`--output-format json` に切り替え、`.modelUsage` のキーから解決後モデルを取り出すようにした。

判断したことが4つある。

1. **jq が無い環境では json 化しない。** `CLAUDE_JSON_MODE` を起動時に決め、jq が無ければ従来の text 形式で実行してモデル記録だけを諦める。モデル記録は付随情報であり、そのために回答本文の取得を落とすのは本末転倒だから。
2. **主モデルの選び方はトークン投入量の最大値。** `.modelUsage` は補助モデル（haiku）を必ず含むので、1つ選ぶ規則が要る。実測では主モデルが補助を2桁引き離しており（45034 対 521、24156 対 521）この規則で足りる。ただし経験則なので `models_all` に全キーを併記し、後から検証できる形を残した。
3. **本文は `.result` から取り出して `claude-stdout.txt` へ書く。** ここは `so-verdict.sh` が VERDICT を抽出する入力なので形式を変えられない。取り出しに失敗したら生出力を複写する退避を入れた。これが無いと、タイムアウトで json が切れたときに本文が消えて `timeout_partial` が `timeout_empty` に化け、余計なリトライを誘発する。

    > **この判断は N5 で撤回した（実装SO の CRITICAL 1）。** 現在の実装は生出力を複写せず `claude-stdout.txt` を空のままにし、生出力は `claude-raw.json` に残す。理由は「N5: 実装SO で自分の設計の穴が2つ出た」節を見よ。**以下この節に書かれた退避の記述は、当時の判断の記録であって現在の挙動ではない。**
4. **環境エラーとデータ不在を canary で切り分けた。** 入力が JSON として読めなかったとき、それが「出力が壊れている」のか「jq 自体が動かない」のかは、その場では区別できない。`printf '{}' | jq` を1回走らせて jq の健全性を確かめてから種別を決めている。推測で種別を割り当てない。

**撤回した案**: 当初 `model_resolved_source` を無条件で `cli-json` と書いていたが、jq が無くて記録を諦めた場合にも `cli-json` と出てしまい、出所として嘘になる。条件分岐して `none` を書くよう直した。

### N3: cursor レーンの実装（自動取得はしない）

`model_resolved=unavailable:cli-not-exposed` を固定で書く。`store.db` を読むコードは書いていない。

種別名を `cli-not-exposed` にしたのは、これが**こちらの失敗ではなく CLI 側が出していない**という理由を表すためである。`query-failed`（環境エラー）や `no-modelusage`（データ不在）と混ざらない。

なお owner 判断で自動取得を落とした結果、plan の7分類のうち `store.db` 経路に属する4種別（`no-session-id` / `store-not-found` / `db-not-found` / `no-modelname`）はコード上到達しなくなった。**受け入れ基準は「`query-failed` と `no-modelname` が別種別」と書いているが、`no-modelname` は落とした経路の種別である。** 基準が求めている実体（環境エラーとデータ不在を混ぜない）は `query-failed` と `no-modelusage` の対で満たしている。この読み替えは N5 の PR 本文にも書く。

### N2/N3 の検証（実コードの関数を取り出して実行）

コピーではなく `scripts/so-compare.sh` から関数本体を `awk` で抜き出して実行した。

| 入力 | `model_resolved` | 本文 |
|---|---|---|
| 実際の json（既定） | `claude-fable-5`（haiku より正しく主を選ぶ） | 取り出し成功 |
| 実際の json（`--model sonnet`） | `claude-sonnet-5`（エイリアス解決が残る） | 取り出し成功 |
| `modelUsage` が空 | `unavailable:no-modelusage` | 取り出し成功 |
| `modelUsage` が無い | `unavailable:no-modelusage` | 取り出し成功 |
| 途中で切れた json | `unavailable:parse-failed` | 生出力へ退避（**この挙動は N5 で撤回済み。現在は空のまま**） |
| 空ファイル | `unavailable:parse-failed` | 空 |
| jq が壊れている（同じ入力） | `unavailable:query-failed` | — |
| jq が無い（同じ入力） | `unavailable:query-failed` | — |

最後の2行が肝心である。**`modelUsage` を持つ同じ入力が、環境が壊れているときは `query-failed`、データが無いときは `no-modelusage` になる。** 種別が実際に分かれていることを、同一入力の対比で確認した。jq の stderr も捨てずに `claude-modelmeta-stderr.txt` へ残している。

### N4: SKILL.md の更新

追加キーの意味、`unavailable` の種別表、`store.db` の手動手順、`auto` が実行ごとに変わる実測を書いた。

**判断を求められていた点（codex の `model_resolved` も観測値でないことを書くか）は、書くことにした。** 理由は、これが利用者の行動を変える情報だからである。SO の判定を証跡として引くとき、claude の `model_resolved` は実際に使われたモデルの観測値だが、codex のそれは設定ファイルを読んだだけの値で、設定と実際が食い違えば嘘になる。同じキー名で並んでいるのに強さが違うことを知らなければ、利用者は codex の値を観測値として引用してしまう。**キー名からは差が見えないので、書かなければ気づけない類の情報である。** レーンごとの確からしさを表にし、「codex の値を実行されたモデルの記録として引用しないこと」と明示した。

あわせて `model_resolved_source` というキー自体を新設し、確からしさの差を機械可読にした。文章の注意書きだけだと読み飛ばされるため。

手動手順を書くときは `head` を使わず `awk 'NR==1'` にした（採用 NK `01KYA7C9M4EN…` の SIGPIPE 回避を、利用者が真似する手順にも効かせるため）。

### N5: 実装SO で自分の設計の穴が2つ出た

`so-compare --with codex,cursor` で実装SO を回した。**codex が CRITICAL 2件・WARNING 2件を返し、そのうち3件を実際に取り込んだ。** 自分では気づいていなかった穴なので、SO が効いた事例として残す。

**CRITICAL 1: 生 json を `stdout.txt` へ退避する設計が間違っていた。**

私は「取り出しに失敗したら生出力を複写して部分出力を守る」と設計し、plan にもそう書いていた。codex の指摘はこうである。text 形式のころの部分出力は**そのまま読める回答**だったが、途中で切れた json は**回答として使えない**。使えないものを非空として置くと `classify_result` が `timeout_partial` と判定し、**`timeout_empty` 限定のリトライが起きなくなる**。つまり再取得の機会まで失う。

これは私の設計判断が逆だったということで、指摘のとおり直した。生出力は `claude-raw.json` に残るので情報は失われない。**plan の §5.4 手順3は誤りだったことになる。** plan は前の単位の成果物なので書き換えず、ここに訂正を残す。

**CRITICAL 2: 環境エラーとデータ不在の分離が、途中までしかできていなかった。**

最初の JSON 妥当性チェックだけ canary で切り分けていたが、その後の `jq` 呼び出しは失敗を全部 `no-modelusage` に落としていた。**本変更の主目的そのものが未完成だった。**

実際に再現も確認した。`{"modelUsage":{"main":{"inputTokens":"x"}}}` を渡すと、`.modelUsage` は存在するのに `max_by` が型エラー（終了コード 5）で落ち、実装は `no-modelusage`（データ不在）と記録する。**データはあるのに「無い」と記録する**ので、SKILL.md の定義とも食い違っていた。

直し方は終了コードの意味を使い分けることにした。`jq -e` は判定が false なら 1、実行に失敗すれば 2 以上を返す。この差がそのままデータ不在と環境エラーの差なので、まとめて非ゼロ扱いにするのをやめた。あわせて「`.modelUsage` はあるが形が想定と違う」を `schema-unexpected` として新設した。

**WARNING: `model_resolved` を観測値と説明するのは強すぎた。**

どのモデルが動いたか（`models_all`）は観測値だが、**そのうちどれが回答を書いた主モデルかは推定**である。トークン投入量が最大、という私の規則は CLI が保証したものではない。補助モデルが何度も呼ばれた場合や同点の場合に外れる。SKILL.md の表現を直し、「厳密に引くときは `models_all` を併記し、`model_resolved` だけで断定しない」と書いた。

**WARNING: モデル ID を検証せずに meta へ書いていた。** meta は `cut -d= -f2` で読まれるので、キーに `=` や改行が入ると行が壊れる。Anthropic の既知の ID では起きないが、外部から来る文字列をそのまま書いている以上コード上の保証が無い。書く前に形を確かめ、外れたら `schema-unexpected` にするようにした。

**取り込まなかった指摘は無い。** 4件すべて対応した。

このとき自分でも1つ見つけた。検証を `printf '%s' "$v" | grep -qE ...` で書いたが、これは**採用 NK `01KYA7C9M4EN…` がまさに警告している早期終了 consumer** である。入力が短いので実害は出ないが、パターンとして残すべきでないので bash の正規表現に置き換えた。NK を守るつもりで書いたコードの中で NK を踏みかけた。

**cursor レーンは 334 秒かけて返った。** 最初の試行は 300 秒でタイムアウトし、タイムアウト値を 600 秒へ上げて再実行したら返った。最小プロンプトなら 38 秒で返るので、**差分レビューという重い課題ではレーンごとに必要な時間がまるで違う**ということになる。既定の 240 秒は設計レビューには足りない場面がある。

cursor は codex とは独立に **CRITICAL 2件の同じ結論に到達した**。別レーンが同じ穴を指摘したことで、これが偶然の指摘でないことがはっきりした。

さらに cursor だけが見つけた欠陥が2件あり、どちらも実機で再現を確認して取り込んだ。

- **`.result` が文字列でない場合。** `jq -r` に object を渡すと JSON を整形して出力するので、`body_source=json-result` と書いてあるのに `stdout.txt` が JSON になる。`.result` が文字列のときだけ受け取るよう型を確かめるようにした。
- **リトライ時のバックアップに `claude-raw.json` が入っていない。** タイムアウト再実行時に1回目の生出力が上書きされて消える。**証跡を残すための変更で証跡を失っていた**ので、退避対象に加えた。

cursor はこちらの修正のトレードオフも指摘した。生 json を捨てる方針にすると、完全な JSON の中に部分的な `.result` が既にある稀な場合に本文を捨てることになる。これは受け入れる。生出力は `claude-raw.json` に残るので復元でき、リトライで取り直せる利益のほうが大きい。

**SO で見つかった欠陥は合計6件、すべて取り込んだ。** 内訳は codex と cursor が共通で指摘した2件、codex 単独の2件、cursor 単独の2件である。**単独指摘が両側にあったことは、レーンを複数にした意味がそのまま出た形になっている。**

### N5 後半: Copilot レビュー

Copilot が2件指摘し、どちらも妥当だったので取り込んだ。

- **episode の N2 節が現在の挙動と食い違っている。** 生出力を退避する記述が残っていて、読者が現行挙動と誤解する。**ただし episode は追記ログなので、当時の判断を消して書き換えるのは違う。** N5 で撤回した旨の前方参照を当該箇所に付け、当時の記録としては残した。
- **SKILL.md の出力ディレクトリ一覧に `model_resolved_source` が抜けている。** その節だけ見る利用者がキーを見落とすので追記した。

規律どおり1ラウンドで完結させ、再レビュー依頼はしていない。

### N5c: scope 追加 — 「claude レーンは返らない」は通説であって事実ではなかった

closure 後に owner 判断で scope が1件足された。#293 の子が claude レーンのタイムアウトを実測で崩したためである。**同じ `scripts/so-compare.sh` を触るのでファイル衝突を避ける目的と、SO 証跡の質という theme の一致から #295 に足された。**

これは終端（N6）を動かさず、ゲートも外さず、義務を足す変更なので、子側ガードの (a)(b)(c) のいずれにも当たらない。指摘せず受けた。

**まず自分で裏取りした**（採用 NK `01KYMRE1NC…`）。`tmp/so-293-gate2/` の meta を直接読んだ。

| 試行 | timeout | 結果 | 所要 | 出力 |
|---|---|---|---|---|
| 初回（`.attempt1`） | 480秒 | `timeout_empty` | 480秒 | 0 bytes |
| リトライ | 720秒 | `success` | **652秒** | 10659 bytes / 76行 |

**claude は返っていた。** 480秒では足りず、720秒なら返る。環境が非対応なのではなく、単にタイムアウトが短かっただけである。

さらに同じディレクトリの他レーンを見たところ、**原因の構造がはっきりした**。同一プロンプト（7.4KB）での所要は次のとおりである。

| レーン | 所要 | 既定240秒で足りるか | リトライ360秒で足りるか |
|---|---|---|---|
| cursor | 254秒 | 足りない | **足りる** |
| codex | 296秒 | 足りない | **足りる** |
| claude | **652秒** | 足りない | **足りない** |

**3レーンとも既定の240秒では返らない。** ところがリトライ（×1.5 = 360秒）が codex と cursor だけを救い、claude だけが構造的に取り残される。だから「claude だけ返らない」という観測になり、環境の非対応だと解釈されていた。**通説の原因は環境ではなく、リトライ幅が2レーンにだけ届いていたことである。**

この誤解の代償も実際に出ている。統括は全 brief に「claude は返らない実績がある」と書いてきたため、#284 / #289 / #290 は事実上2レーンで反証していた。**多様性の主張が1レーン分弱っていたことになる。** なお本単位の実装SO も、brief が「claude は必須に数えない」としていたため codex + cursor で回している。

**手当ての選び方**: owner は「既定値の引き上げ / claude だけ長くする / リトライを長くする」のどれでもよいとし、実測で選べとした。選んだのは **claude だけ長くする**である。

- 既定値の一律引き上げは、codex と cursor の失敗検出まで遅くする。両者は360秒で足りているので、遅くする理由が無い。
- リトライを長くする案は、初回の240秒を丸ごと捨ててから長い2回目に入るので、成功までの総時間が最も長くなる。claude は初回から長くしたほうが速い。
- 実測が示しているのは「claude だけが systematically 2倍以上遅い」という**レーン固有の差**なので、レーン固有の既定で受けるのが素直である。

`SO_CLAUDE_TIMEOUT` を新設し、`SO_TIMEOUT`（codex / cursor 用・240秒のまま）と分けた。リトライ時間もレーンごとの基準から算出する。

**既定値は自分の実測で決め直した。** 最初は #293 の652秒だけを見て900秒にしたが、同じ重いプロンプト（実装SO で codex 241秒 / cursor 334秒だったもの）に claude を投げたら **777秒** かかった。900秒では余裕が16%しかなく足りないので **1200秒** へ引き上げた。最大実測に対して約54%の余裕である。

得られた実測は3点になった。

| プロンプト | codex | cursor | claude |
|---|---|---|---|
| 設計レビュー（7.4KB・#293 の証跡） | 296秒 | 254秒 | 652秒 |
| 実装レビュー（1.4KB・自分で実測） | 241秒 | 334秒 | 777秒 / 739秒（2回） |

**所要時間はプロンプトの大きさで決まらない。** 小さいほうのプロンプトのほうが長くかかっており、効くのは要求される分析の深さである。同じプロンプトでも5%ほど振れる（777秒と739秒）ので、最大実測ぎりぎりに既定を置くのは危ない。

**受け入れ確認**: 環境変数を一切設定せず（`env -u SO_TIMEOUT -u SO_CLAUDE_TIMEOUT`）レビュー級のプロンプトを claude レーンへ投げ、`timeout_status=success` / 739秒 / 11748 bytes / 82行 / **リトライなし**で返ることを確かめた。実行時の表示も `claude=1200秒` で既定が効いていることを示している。3レーンの回帰（`TOTAL_LANES=3 SURVIVED=3`）も既定設定のまま通っている。

---

## 振り返り（closure）

tier: **heavy**。実行中に撤回があり（`本文: N5: 実装SO で自分の設計の穴が2つ出た`）、品質ゲート目的で `so-compare` を明示起動し、非自明な設計判断で棄却した案がある。

### closure gate

- **Context / なぜ**: `本文: 前提（この単位に入る前に決まっていたこと）` に自己完結で記載。
- **次の消費者**: (1) SO の判定を証跡として引くすべての作業 — `model_resolved` の強さがレーンで違うことを知る必要がある。(2) #263（4層ドキュメントの文体をモデル差で実測する調査）— どのモデルが何を言ったかが主題なので、この記録が前提になる。(3) `canonical/` の sync を回す人 — SKILL.md が変わったので配布が要る。
- **status**: draft → stable。達成度は **達成**（受け入れ基準は #295 の4条件すべてを実機証跡つきで満たした。ただし cursor の自動取得は owner 判断で範囲外なので、「取得不能を記録する」形での達成である）。
- **evidence anchor**: SO の生出力は `tmp/so-295-impl/`・`tmp/so-295-impl-cursor/`・検証は `tmp/so-295-verify/` に出したが、**`tmp/` は gitignored で消える**。したがって要点は本文と PR #296 本文へ転記済み（3レーンの meta 実測値、`TOTAL_LANES=3 SURVIVED=3`、engine テスト `pass=63` / `pass=64`、SO 指摘6件の内訳）。パスは再現用の記録であって参照先ではない。
- **SO 証跡**: 実装SO は `本文: N5: 実装SO で自分の設計の穴が2つ出た` に指摘と対応を転記。
- **Step4（closure の外部チェック）**: 実施した。`so-compare --codex-only` で closure 節だけを対象に、選択的省略 / routing 網羅 / 揮発パス / back-propagation の4点を確認させた（145 秒・`gpt-5.6-sol`）。**結果は「問題あり」で、2件の不備を検出された。** (1)「事実・失敗」節が指摘・撤回を pointer 付き2行に畳んでおり、6件の SO 指摘のうち実質2件しか項目として現れていなかった。(2)「残課題」の sync 項目の行き先が担当者と時期の記述にとどまり、指定された3形式（Issue / 別 doc / 追わない宣言）のいずれでもなかった。**どちらもこの closure で修正済み。** 揮発パスと back-propagation は問題なしと判定された。生出力は `tmp/so-295-closure/` にあるが `tmp/` は消えるので、要点はこの項目に転記した。
- **注入された knowledge の id**: `01KYA7C9NN4VDM7H2NYXZB2PSX` / `01KYA7C9M4EN1EZM3HBEN5WDRP`。両方に観測を1レコードずつ書き戻した（`#295`・`externally_verified` / `followed`）。`validate-knowledge` は両方 pass。

### 事実・失敗

指摘・撤回は1件ずつ項目として並べる（まとめて1行に畳まない）。詳細は pointer 先にある。

**実装SO の指摘（6件・すべて取り込み済み）**

- 生 json を `stdout.txt` へ退避する設計が逆だった。壊れた json を非空として置き、リトライを潰していた（両レーン共通の指摘）。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- 握り潰し禁止の規律を入口にだけ適用し、後段の `jq` 失敗を全部データ不在に落としていた（両レーン共通の指摘）。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- `model_resolved` を観測値と説明したのが過大主張だった。主モデルの選定は推定である（codex 単独）。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- 外部由来のモデル ID を検証せずに meta へ書いていた（codex 単独）。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- `.result` が文字列でない場合に `stdout.txt` が JSON になる（cursor 単独）。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- リトライ時のバックアップに `claude-raw.json` が入っておらず、1回目の証跡が消えていた（cursor 単独）。`本文: N5: 実装SO で自分の設計の穴が2つ出た`

**受け入れたトレードオフ**

- 生 json を捨てる方針にしたことで、完全な JSON の中に部分的な `.result` がある稀な場合に本文を捨てる。cursor の指摘を受けて受容した（生出力は `claude-raw.json` に残る）。`本文: N5: 実装SO で自分の設計の穴が2つ出た`

**自分で撤回したもの**

- `model_resolved_source` を無条件に `cli-json` と書いていた。jq が無くて記録を諦めた場合に出所として嘘になるため、条件分岐して `none` を書くよう直した。`本文: N2: claude レーンの実装`
- NK を守るつもりで書いた検証コードの中で、その NK が警告する早期終了 consumer（`printf | grep -q`）を自分で踏みかけた。`本文: N5: 実装SO で自分の設計の穴が2つ出た`

**Copilot の指摘（2件・すべて取り込み済み）**

- episode の N2 節の記述が実装の変更に追随しておらず、現行挙動と食い違っていた。`本文: N5 後半: Copilot レビュー`
- SKILL.md の出力ディレクトリ一覧から `model_resolved_source` が漏れていた。`本文: N5 後半: Copilot レビュー`

**この episode 自体の誤記と訂正**

- N5 節に一度「cursor は 300 秒でも 600 秒でも返らなかった」と書いたが、これは誤りで、600 秒設定では 334 秒で返っていた。次のコミットで訂正した（`8ef2ff1` → `891a130`）。**closure の外部チェック（Step4）がこの撤回の未記録を検出したので、ここに追加している。** `本文なし: 訂正が本文の書き換えとして入り、撤回の事実が本文に残っていなかったため`
- cursor レーンが 300 秒では返らず、タイムアウトを 600 秒へ上げる必要があった。`本文: N5: 実装SO で自分の設計の穴が2つ出た`

**scope 追加で判明した、この単位の外側の誤り**

- 「この環境では claude レーンは返らない」という通説が誤りだった。実際は共通の既定タイムアウト（240秒）が claude に短すぎただけで、720秒なら返っていた。`本文: N5c: scope 追加 — 「claude レーンは返らない」は通説であって事実ではなかった`
- 既定の240秒では**3レーンとも返らない**のに、リトライ（×1.5 = 360秒）が codex と cursor にだけ届いていた。claude だけが構造的に取り残される仕組みになっていた。`本文: N5c: scope 追加 — 「claude レーンは返らない」は通説であって事実ではなかった`
- その通説に基づいて、本単位の実装SO も codex + cursor で回した（brief が「claude は必須に数えない」としていたため）。**claude を入れれば3レーンで反証できたはずの単位を、2レーンで回していたことになる。** `本文: N5c: scope 追加 — 「claude レーンは返らない」は通説であって事実ではなかった`

**closure 自体の不備（Step4 が検出）**

- 上記の指摘・撤回を「事実・失敗」節で1件ずつ並べず、pointer 付きの2行に畳んでいた。**pointer は再掲を省くためのものであって、項目そのものを省いてよいという意味ではない。** Step4 の指摘を受けて全件を展開した。`本文なし: closure の執筆時点で起きた不備であり、本文層に対応する記述が無いため`

### 決定と根拠

- cursor の自動取得を実装しない（owner 判断）。棄却した案 = `store.db` を読む自動取得。`本文: 前提（この単位に入る前に決まっていたこと）`
- `unavailable` を種別で分ける。`no-modelname` は落ちた経路の種別なので `no-modelusage` が実体を担う。`本文: N3: cursor レーンの実装（自動取得はしない）`
- codex の `model_resolved` が観測値でないことを書くと決めた。`本文: N4: SKILL.md の更新`
- 主モデル選定は推定であると認め、`models_all` の併記で緩和した。`本文: N5: 実装SO で自分の設計の穴が2つ出た`

### わかったこと

- claude の `--output-format json` は `.modelUsage` に解決後のモデル ID を持ち、エイリアス解決の結果がそのまま観測できる。`本文: N2/N3 の検証（実コードの関数を取り出して実行）`
- cursor の `auto` は実行ごとに解決先が変わる（grok-4.5-high ×2、composer-2.5 ×1）。`本文: N4: SKILL.md の更新`
- `jq -e` は判定 false で 1、実行失敗で 2 以上を返す。この差がデータ不在と環境エラーの判別材料になる。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- SO のレーンごとに必要な時間が課題の重さで大きく変わる。既定の 240 秒は差分レビューには足りないことがある。`本文: N5: 実装SO で自分の設計の穴が2つ出た`

### 原則（Pattern / Anti-pattern）

- **Anti**: 失敗と正当な空を区別する規律を、経路の入口にだけ適用して満足する。**Pattern**: 同じ判定が複数の経路にあるなら、全経路に適用したかを一度数える。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- **Anti**: 出力形式を変えたとき、後段の分類器が持つ「非空＝部分的に成功」の前提をそのままにする。**Pattern**: 退避データが後段でどう解釈されるかまで見る。使えないデータを非空として置くと回復機構（リトライ）を潰す。`本文: N5: 実装SO で自分の設計の穴が2つ出た`

### 蒸留シグナル

- **decision / discussion への昇格: 不要**と判定した。理由は下記「昇格判定」。
- **negative knowledge の収穫候補: あり（2件）。** ただし本委譲は item 化を owner 判断としているので、ここでは候補の提示にとどめる（下記）。

### 残課題（routing）

- `canonical/` の sync が未実施。SKILL.md の変更を各ツールへ配る必要がある。**行き先: 追わない（この委譲では起票もしない）。** 理由は、sync がマージ後の定常運用であって本 issue の作業項目ではないこと、および子はマージに関与しないこと。**マージ時に owner が実行する必要があるため、PR 本文にも注意として明記した**（記録の場所を PR 側に置き、ここでは追跡しない）。
- SO の既定タイムアウト 240 秒が重いレビューに足りない件。**行き先: 追わない。** 今回は `SO_TIMEOUT` で回避できており、既定値の変更は SO のレーン構成・タイムアウト値に当たるため本 issue のスコープ外と明示されている。必要になった時点で別 issue にする。
- `cut -d= -f2` の既存の穴（値に `=` が入ると壊れる）。**行き先: 追わない（既存・本 PR では直さない）。** ただし新しく書く値がこれを踏まないことは検証済みで、外部由来のモデル ID は形を確かめてから書くようにした。`本文: N5: 実装SO で自分の設計の穴が2つ出た`
- cursor の `store.db` 経路が Cursor の更新で壊れる可能性。**行き先: 追わない（監視もしない）。** 自動依存を作っていないので壊れても実害が無く、手動手順が古くなるだけである。使うときに確かめればよい。

### negative knowledge の収穫候補（item 化は owner 判断・ここでは surface のみ）

- **候補1: 規律の部分適用。** 「失敗と正当な空を区別せよ」という既存 item を、入口の1箇所にだけ適用して完了と見なした。既存 item（`01KYA7C9NN4VDM7H2NYXZB2PSX`）は「区別せよ」を言うが、「**適用したつもりで経路の一部にしか当たっていない**」という失敗の形は言っていない。収穫基準は満たすと考える（非自明・再発しうる・行動を変える）。既存 item の refinement として扱うか独立 item にするかは判断が要る。
- **候補2: 出力形式の変更が後段の分類の意味を変える。** 「非空＝部分的に成功」という分類の前提は、本文が平文だったから成り立っていた。JSON 化すると壊れた JSON も非空になり、分類が意味を失ってリトライまで潰す。転用可能で、既存 item に同旨は見当たらない。

### 終端定義と子側ガードの試用結果

**ガードは発火しなかった（矛盾が無かったため）。** 本単位は N1〜N6 で、前の単位（M 系）とは別体系だと最初に明示されていた。親からの指示は「N1 から進めよ」「終端は N6」だけで、終端の再定義・未達 step の飛び越し・基準の弱体化のいずれにも当たらなかった。

**発火するか迷った箇所が1つある。** 受け入れ基準の「`query-failed` と `no-modelname` が別種別」は、owner が gate 3 で `store.db` 経路を落とした結果、`no-modelname` がコード上到達しなくなり、字面どおりには満たせなくなった。これを「基準の弱体化」として指摘するか迷ったが、**基準を弱めたのは親の指示ではなく owner 自身の範囲判断であり、(a)(b)(c) のどれにも当たらない**と判断して発火させなかった。基準が求めている実体（環境エラーとデータ不在を混ぜない）は `query-failed` と `no-modelusage` の対で満たし、読み替えを PR 本文と本 episode に明記する形にした。**過発火を避けつつ、判断の痕跡は残した**という扱いである。

### 昇格判定（decision / discussion）

**昇格は不要と判定した。** `auto` が実行ごとに変わるという実測は durable な知見だが、**その消費者は「SO のレーン多様性をどう読むか」であり、行き先は `canonical/skills/so-compare/SKILL.md` である。** すでにそこへ書いた（`本文: N4: SKILL.md の更新`）。skill は各ツールへ sync され、SO を使うときに読まれる場所なので、消費者に届く経路として decision より近い。

decision に昇格させる価値があるのは「非公開の内部形式に自動依存しない」という判断のほうだが、**これは owner が gate 3 で下した判断であって、この単位が新たに決めたものではない。** 子が勝手に decision へ昇格させる筋ではないので、判断の記録は本 episode に残すにとどめる。owner が durable にしたいと考えるなら別途起票するのが順序として正しい。
