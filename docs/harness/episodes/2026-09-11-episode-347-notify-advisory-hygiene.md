---
id: "01M28491PJBMVBPZFKJ19E4KKZ"
title: "#347 notify.sh の advisory 衛生3件"
date: 2026-09-11
type: episode
status: stable
related:
  - type: derived_from
    ref: "https://github.com/stlwolf/ai-development-hub/issues/347"
    reason: "本 episode の作業対象"
  - type: relates_to
    ref: "docs/harness/plans/2026-09-11-plan-347-notify-advisory-hygiene.md"
    reason: "この episode が記録する作業の計画"
  - type: relates_to
    ref: "docs/harness/discussions/2026-09-11-discussion-347-approval-baseline-drift.md"
    reason: "昇格 required と判定した1件の昇格先"
tags: [hooks, notify, advisory, home-unset, shell]
promotion:
  - subject: "notify.sh の HOME 可否を engine の述語へ揃え、他の hook は揃えない"
    verdict: not-required
    ref: "本文: closure（ゲート5・マージ前）"
  - subject: "engine と同じ述語のままにし、厳しさをずらさない"
    verdict: not-required
    ref: "本文: Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った"
  - subject: "借りてきた述語は写す / 写さないの2値でなく成分に割って要否を決める"
    verdict: not-required
    ref: "本文: Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った"
  - subject: "走査は当たっていたのに走査結果の読み取りで取りこぼした"
    verdict: not-required
    ref: "本文: Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った"
  - subject: "検証の足場が本番と違う環境を作ってしまう"
    verdict: not-required
    ref: "本文: Step 8: 回帰確認（証拠を表で残す）"
  - subject: "借りてきたガードは判定が同じでも置き場の条件まで同じとは限らない"
    verdict: not-required
    ref: "本文: Step 8: 回帰確認（証拠を表で残す）"
  - subject: "裁定に従うこと自体が承認時の baseline を動かす場合がある"
    verdict: required
    ref: "本文: Step 5: ゲート3の裁定と baseline の張り直し"
---

# #347 notify.sh の advisory 衛生3件

## なぜこの作業が始まったか

`canonical/hooks/scripts/notify.sh` は、エージェントの完了と入力待ちを macOS 通知へ流す advisory フックである。advisory なので「環境の事情で落ちない・雑音を出さない」が契約なのに、その契約を破る箇所が3つ残っていた。issue #347 は、#343（notify.sh の切り詰め）の実装子が範囲外として surface し、gate 4 の codex レーンが「メモに埋めず別 issue にすべき」と言ったことから起票された。

## Step 1: 3件を実測で再現する（2026-09-11）

issue の主張を鵜呑みにせず、着手時点の master（`04722ba`）で自分で再現した。

### 1. HOME 未設定で rc=1 になる

```text
$ printf '{"message":"test"}' | env -u HOME bash canonical/hooks/scripts/notify.sh >/dev/null 2>&1; echo $?
1
$ printf '{"message":"test"}' |            bash canonical/hooks/scripts/notify.sh >/dev/null 2>&1; echo $?
0
```

stderr に出るのは `notify.sh: 行 112: HOME: 未割り当ての変数です`（メッセージは locale に従う）。`set -u` の下で未定義の変数を展開したので、シェルがその場で終了している。末尾の無条件 `exit 0` へ到達していない。

該当はデバッグ判定の行で、`notify-hook-debug` で検索すると当たる。隣の `NOTIFY_DEBUG` は `${NOTIFY_DEBUG:-}` で guard されているのに、`$HOME` だけが素である。

### 2. 制御端末を持たない起動で /dev/tty の雑音が出る

フックは制御端末を持たない文脈で起動されうる。`os.setsid()` で制御端末を手放した子プロセスから、tmux 変数を外して起動すると再現した。

```text
rc = 0
stdout+stderr = canonical/hooks/scripts/notify.sh: 行 125: /dev/tty: Device not configured
```

終了コードは 0 なので契約違反ではないが、advisory が毎回 stderr を汚す。`-w /dev/tty` の guard を通過している点が肝心で、macOS では制御端末が無くてもデバイスノードへの `access` は成功し、`open` だけが `ENXIO` で失敗する。guard では防げない。

### 3. jq の下限がコード中のコメントにしかない

`canonical/hooks/README.md` の「前提条件」節は `jq` と `grep` が必要とだけ書いていて、版の下限は書いていない。下限 1.4 の根拠は notify.sh のコメントにだけ在る。

## Step 2: リダイレクトの順序が診断を消すかを確かめる

issue は「`2>/dev/null` がリダイレクトより後にあるため効いていない」と書いている。これを単独で確かめた。

```text
$ bash -c 'printf x > /nonexistent-dir/foo 2>/dev/null'; echo "rc=$?"
bash: 行 1: /nonexistent-dir/foo: No such file or directory
rc=1
$ bash -c 'printf x 2>/dev/null > /nonexistent-dir/foo'; echo "rc=$?"
rc=1
```

リダイレクトは左から右へ処理されるので、先に stderr を `/dev/null` へ向けておけば、後続のリダイレクト失敗の診断もそこへ落ちる。終了コードはどちらも 1 のままで、`&& delivered=1` の判定は変わらない。issue の読解は正しい。

## Step 3: 同族が他に無いかを走査する

#347 は「`oe-*` 側は #341 で揃えたが hook 側に残っていた」という取りこぼしから生まれた issue なので、hook 側に他の同族が無いかを見た。

`canonical/hooks/scripts/` の8本に対して `$HOME` と `${HOME` を走査した結果、`set -u` の下で素の `$HOME` を展開しているのは `notify.sh` の1箇所だけだった。他の5箇所（`cc-lint.sh` / `block-destructive.sh` / `block-force-push.sh` / `session-name.sh` / `oe-prompt-receipt.sh`）はいずれも `${HOME:-}` で受けている。

この走査だけでは「grep のパターンが見落とす形」を排除できない。走査パターンの外から対照を取る手立ては plan の検証 step に置いた（8本すべてを `env -u HOME` で実際に起動して終了コードを見る、という振る舞い側の対照）。

リダイレクトの順序については、`notify.sh` の中に同じ形が2箇所ある（tmux のペイン TTY へ書く側と `/dev/tty` へ書く側）。**この数え方は誤りだった。実際は3箇所である（Step 4 で訂正した）。**ファイルの外にも `session-name.sh` と `oe-prompt-receipt.sh` に同じ形があるが、これらは通常ファイルへの追記なので #347 のスコープ外である。

昇格の印: hook 側と engine 側で `HOME` の可否の基準が違う（engine は「絶対パスかつ `/` でない」、hook は「非空」）。どちらが正しいかではなく「読むだけか書くか」で分かれている可能性がある。判断が固まったら decision へ上げるか検討する。

## Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った（2026-09-11）

`so-compare --with codex,claude` で plan の初稿を反証にかけた。**期待2者・成功2・部分0・失敗0**（codex は初回タイムアウトで1回リトライののち 298 秒で成功、claude は 491 秒）。結果は `so-347-design-out/` に残っている（作業ツリー外の一時領域なので commit しない）。

**自分で見つけられていなかった反証が3つ出た。**

### 覆った1: 「非空で足りる」は `HOME=/` で崩れる

初稿の DJ-2 は「`notify.sh` は `-f` を1回するだけで書き込みも走査もしないので、`HOME` の可否は非空で足りる」と書いていた。両レーンが同じ反例を出した。

初稿が `${HOME:-}` を退けた理由は「空文字のとき `/` 直下を見に行くから」である。**その理由は `HOME=/` にそのまま当てはまる。** `-n "/"` は真なので guard を通り、`//.notify-hook-debug` を評価する。つまり初稿は、空文字を退けた理由で `HOME=/` を退けていなかった。自分の論法の中に反例があったのに気づいていない形である。

レーンの主張を鵜呑みにせず実測で当て直した。

```text
$ HOME=/  bash -uxc '[[ -n "${HOME:-}" && -f "${HOME}/.notify-hook-debug" ]]'
+ [[ -f //.notify-hook-debug ]]
$ HOME=// bash -uxc '[[ -n "${HOME:-}" && -f "${HOME}/.notify-hook-debug" ]]'
+ [[ -f ///.notify-hook-debug ]]
```

claude レーンの指摘がもう一段効いた。engine が `/` と `//` を名指しで落としているのは「書き込むから」ではなく「`/` を家として扱わない」からで、読むだけの用途にも同じ理由で効く。初稿は engine の厳しさを丸ごと「書き込み側の事情」に帰していたが、区別すべきなのは「絶対パスを要求するかどうか」であって「`/` を弾くかどうか」ではなかった。

**負の知識 `01M1272GA8CRXQKQWMF005NHCF` の当たり方が、予想と逆だった。** あの item は「借りてくる先の書き方が何をしていないかを確かめてから写せ」で、初稿はそれに従って engine の `_oe_home_usable` を「書き込み前提だから写さない」と判断した。確かめる向きは合っていたが、**分解の仕方を間違えた**。engine の述語は「絶対パス要求」と「`/` 排除」の2つの成分でできていて、前者だけが書き込みの事情である。借り物を「写す / 写さない」の2値で見たのが誤りで、成分に割ってから要否を決めるべきだった。

### 覆った2: 同じ欠陥は2箇所でなく3箇所

両レーンとも、デバッグログの追記 `>> /tmp/notify-hook.log 2>/dev/null || true` が同じリダイレクト順の欠陥を持つと指摘した。`|| true` が握り潰すのは終了コードだけで、診断は握り潰さない。

**Step 3 の走査はこの行を出力に含めていた**（`>> ... 2>/dev/null` のパターンで当たっていた）。にもかかわらず「配信の2箇所」とだけ数えたのは、grep の結果を配信のブロックに限って読んだからである。**走査は当たっていて、読み方で落とした。** 負の知識 `01M07QDKE73BTK0Q3K90FE4G9T` は「N 箇所だけ」の対照を走査パターンの外から取れという教訓だったが、今回の取りこぼしは走査の外ではなく**走査の結果を読む段**で起きた。

### 覆った3: 陽性対照が本番と違う分岐を通る

初稿の DJ-6 は `bash -x` のトレースで「配信の分岐を通り末尾の `exit 0` に達している」ことを見る、としていた。claude レーンの指摘は、**対話端末から走らせると `/dev/tty` が開けるので `delivered=1` になり、本番のフック文脈（制御端末なし）が通る経路を通らない**というものだった。手元が tmux の中なら今度は tmux 分岐が成立する。どちらにしても、対照の環境が検証したい環境と違う。

負の知識 `01M00KCCHNMFPHP5HAGX2DZ1MK`（期待した色が出た理由まで確かめよ）を初稿で当てたつもりだったが、**当て方が浅かった**。「rc=0 が正しい理由で出たか」を見る道具として `bash -x` を選んだところまでは合っていて、その道具が**本番と違う環境で走る**ことを見ていなかった。両レーンが独立に、偽の `terminal-notifier` を `PATH` に置いて引数を記録する形を薦めた。副作用で確定するので判定が機械的になり、`HOME` 設定時の記録と比べれば「同じものが届いた」まで言える。

### 訂正: `jq` の下限を「前提条件」節に書くと他フックと矛盾する

両レーンが同じ指摘をした。README の「前提条件」節は全フック共通なのに、同じディレクトリの `session-name.sh`（3箇所）と `oe-prompt-receipt.sh`（1箇所）が `jq -cn` という短オプションの連結を使っている。`notify.sh` のコメントが述べている規則をそのまま当てると、これらは 1.5 を要求する。grep で当て直した。

```text
canonical/hooks/scripts/session-name.sh:49:  out="$(jq -cn --arg t "$1" \
canonical/hooks/scripts/session-name.sh:130:    jq -cn --arg b "$branch" '{last_branch:$b}' >"$tmp" 2>/dev/null \
canonical/hooks/scripts/session-name.sh:156:if jq -cn --arg b "$branch" '{last_branch:$b}' >"$tmp" 2>/dev/null; then
canonical/hooks/scripts/oe-prompt-receipt.sh:169:line="$(jq -cn \
```

claude レーンの整理が要点を突いていた。1.4 という値が意味を持つのは `notify.sh` を改変する人だけで、その人に伝えたい中身は「下限は 1.4」ではなく「`-R -r` を `-Rr` に縮めるな」である。読者が改変者に揃う `notify.sh` 節へ置くのが筋である。

### 範囲外として拾った指摘

- デバッグログの追記先が FIFO だと `open(2)` でブロックし、フックが止まる。README の「発火記録」節は止める側3本について同じ危険を明文で扱い、3つの追記先すべてにガードを通したと書いている（実装は `block-destructive.sh` の `[ -e "$1" ] && [ ! -f "$1" ]`）。`notify.sh` には無い。**受け入れ条件に無いので owner の裁定に預ける**（plan の「owner に預ける判断」節）。
- `input="$(cat 2>/dev/null || true)"` は stdin が閉じられない場合に戻らない。`[[ -t 0 ]]` が弾けるのは端末だけである。ハーネスが実際にそういう起動をするかは未確認なので推測扱い。
- `tr` や `basename` が `PATH` に無いと stderr に診断が出る。README の「外部呼び出しは全て `|| true`」は実装の厳密な説明になっていない。

### Cursor は `notify.sh` を呼ばない

codex レーンの指摘で README のカバレッジ表を読み直した。hook スクリプトは3ツールの `hooks/` へ symlink で配られるが、通知に `notify.sh` を使うのは Claude Code と Codex の2つで、Cursor は「既存の通知機構があるため対象外」と明記されている。回帰確認で要るのは「3ツール分の同じテスト」ではなく、実在する2つの入力プロトコル（Claude Code の引数 + stdin JSON と、Codex の第1引数 JSON）である。

昇格の印: 「借りてきた述語を写す / 写さないの2値で見ず、成分に割ってから要否を決める」は、負の知識 `01M1272GA8CRXQKQWMF005NHCF` の適用範囲を一段広げる内容である。closure で item への観測書き戻しに含めるか、別の item にするかを判断する。

昇格の印: 「走査は当たっていたが結果の読み方で落とした」は、`01M07QDKE73BTK0Q3K90FE4G9T`（対照を走査パターンの外から取れ）とは別の失敗段である。走査の健全性と、走査結果の読み取りの健全性は別に扱う必要がある。

## Step 5: ゲート3の裁定と baseline の張り直し（2026-09-11）

owner が plan を承認し、預けた判断2件に裁定が下りた。判断1（デバッグログの追記先にガードを足すか）は「足す」、判断2（README の「前提条件」節に誘導の1行を足すか）は「足さない・採らない理由を DJ-4 の後半に残すこと」である。

承認時に plan・brief・ゲート表の3つで sha256 の baseline が固定された。3つとも照合して承認時の値と一致することを確かめてから着手した。

**その直後に問題が出た。** 裁定は「DJ-4 の後半に1行残せ」と指示しており、これに従うと plan のバイト列が変わって baseline と合わなくなる。step を削ったわけでも要件を弱めたわけでもないが、照合の母集団が動く。黙って進めると、完了前の照合が古い値と突き合わせて空回りする。plan の digest が `a4226a76…` から `ffaa5fc2…` へ変わったことと、変更が3行の置換だけであることを親へ `oe-send` で報告した。親が差分を実見して張り直しを承認した。

指示に従うこと自体が baseline を動かす、という形は brief の「指示矛盾ガード」が並べている3つ（終端の再定義・未達 step の飛び越し・要件の弱体化）のどれでもない。矛盾ではないので止まる必要はないが、報告しないと検査が壊れる種類の変更だった。

## Step 6: 検証の足場を先に組み、修正前に壊れることを確かめた

コードを触る前に足場を組んだ。偽の `terminal-notifier` と偽の `osascript`（受け取った引数を記録するだけ）、偽の `tmux`（`display-message` の書式に応じて擬似端末のパスか loc を返す）、そして3つの起動モードを持つ実行係である。

- `headless`: `os.setsid()` で制御端末を持たない起動。本番のフック文脈と同じ。
- `ctty`: 擬似端末を制御端末として持つ起動。`/dev/tty` へ書かれたバイト列を取る。
- `panetty`: 擬似端末を1本用意し、そのパスを偽 `tmux` に返させる。tmux のペイン TTY 経路用。

**足場そのものが2回壊れた。どちらも「本番と違う環境で測る」という DJ-6 が警戒したのと同じ型だった。**

1つ目。`panetty` で `setsid` した子が擬似端末のパスを `open` すると、書き込みが返らなくなった。制御端末を持たないセッションリーダーが端末デバイスを `O_NOCTTY` なしで開くと、それを制御端末として獲得する。獲得が絡んだ結果として `communicate` が戻らない。この経路は `TMUX` が立っていて `/dev/tty` に触れないので、`setsid` は要らなかった。外したら 0.6 秒で通った。

2つ目。`ctty` で `/dev/tty` が `Device not configured` になった。`pty.fork()` の子は擬似端末を制御端末として持って始まるが、その後 fd 0/1/2 をパイプで上書きしたので、擬似端末への参照が1本も残らなかった。最後の参照が閉じると端末が解放され、`/dev/tty` が開けなくなる。参照を1本 `dup` して残したら通った。

足場が直ったところで、修正前の `notify.sh` に当てて**期待する失敗が出ること**を確かめた。

| ケース | rc | 配信 | stderr |
|--------|----|------|--------|
| `HOME` 未設定 | 1 | **なし** | `行 112: HOME: 未割り当ての変数です` |
| `HOME` 空 / `/` / `//` / 相対 / 通常 | 0 | あり | `行 125: /dev/tty: Device not configured` |
| `HOME` 未設定 + `NOTIFY_DEBUG=1` | 0 | あり | 同上 |
| Codex の呼ばれ方・`wait` モード | 0 | あり | 同上 |

`HOME` 未設定のとき**配信そのものが起きていない**ことが記録で確定した。rc=1 だけを見ていたときより、壊れ方が一段はっきりした。`NOTIFY_DEBUG=1` の行は、短絡評価のせいで修正前でも rc=0 になるという DJ-6 の警告が実際に起きることを示している。

## Step 7: 3件 + ゲート3で足した1件を直した

### HOME の可否を見る述語（`_notify_home_usable`）

engine の `_oe_home_usable` と同じ `case` を単体配布のためインラインで持つ。実測は次のとおり。

- `HOME` 未設定で rc=0 になり、**配信まで到達した**（偽 `terminal-notifier` の記録で確定）。
- 配信の中身が `HOME` 設定時と**一致**した。到達しただけでなく同じものが届いている。
- `HOME` が `/` ・ `//` ・空文字・相対のとき、マーカーのパスを**一度も評価しない**（`bash -x` のトレースで確認）。修正前は `/` ・ `//` ・空文字のいずれでも1回評価していた。
- `HOME` が通常のとき、マーカーの有無による記録の有無は修正前と一致する。

### リダイレクト失敗の診断（3箇所）

`2>/dev/null` を書き先のリダイレクトより前へ移した。全13ケースで stderr が空になった。

### 追記先が通常ファイルでないなら触らない（ゲート3の判断1）

**FIFO のハングは実在した。** 修正前は 8 秒で打ち切るまで戻らず（rc=124）、しかも **stderr は空のまま静かに止まる**。雑音より見つけにくい壊れ方である。ガードを足したあとは 0.05 秒で rc=0 になり、通常ファイルのときは今までどおり記録された。

止める側3本（`block-destructive.sh` の `hfr_appendable`）と同じ形にしたが、**同じ限界も引き継いでいる**。確認してから開くまでの間に相手が差し替わる隙は閉じていない。既存のガードもそこは閉じていないので、本 PR で新しい方式を持ち込むことはしない。

### jq の下限（README）

`notify.sh` 節の「advisory 安全性」に1行足した。「前提条件」節には触れていない（裁定どおり）。

## Step 8: 回帰確認（証拠を表で残す）

### 使った足場

作業ツリーには残さない使い捨ての足場で、中身は次の4つである。

- **偽の `terminal-notifier`**: 受け取った引数をタブ区切りで記録ファイルへ追記して `exit 0` するだけのシェルスクリプト。`PATH` の先頭に置く。
- **偽の `osascript`**: 同じ形。フォールバックの2段目が呼ばれたときに分かるようにしてある。
- **偽の `tmux`**: `display-message` に対して、書式に `pane_tty` が含まれていれば擬似端末のパスを、それ以外なら `window番号:window名 [セッション名]` の文字列を返す。
- **実行係**（Python）: 3つの起動モードを持つ。`headless` は `os.setsid()` で制御端末を持たない起動、`ctty` は `pty.fork()` で擬似端末を制御端末として持つ起動、`panetty` は擬似端末を1本用意してそのパスを偽 `tmux` に返させる起動である。stdout と stderr はパイプで分離し、擬似端末へ書かれたバイト列は別に読む。フォールバックは `( ... ) &` で背景に投げられるので、記録ファイルは短いポーリングで待つ。

### 13ケースの結果（最終状態）

| # | 入力・環境 | 足場 | rc | stdout | stderr | 配信の記録 |
|---|---|---|---|---|---|---|
| 1 | `HOME` 未設定 | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 2 | `HOME` 空文字 | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 3 | `HOME=/` | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 4 | `HOME=//` | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 5 | `HOME` 相対（`rel`） | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 6 | `HOME` 通常 | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 7 | `HOME` 未設定 + `NOTIFY_DEBUG=1` | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 8 | 擬似端末を制御端末として持つ・非 tmux | 擬似端末が制御端末 | 0 | 空 | 空 | OSC 777 を擬似端末で受信 |
| 9 | tmux・`TMUX_PANE` あり | ペイン TTY を用意 | 0 | 空 | 空 | OSC 777 を擬似端末で受信 |
| 10 | tmux・`TMUX_PANE` なし | ペイン TTY を用意 | 0 | 空 | 空 | OSC 777 を擬似端末で受信 |
| 11 | Codex の呼ばれ方（第1引数 JSON） | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 12 | `wait` モード・日本語120文字 | 制御端末なし・非 tmux | 0 | 空 | 空 | 偽 `terminal-notifier` が記録 |
| 13 | `jq` が PATH に無い | 制御端末なし・非 tmux | 0 | 空 | 空 | なし（仕様どおり） |

**`osascript` の分岐は、Copilot の2回目のレビューを受けた見直しで、それまで一度も通していなかったと分かった。** 13ケースの足場は偽の `terminal-notifier` を `PATH` に置いているので、フォールバックは必ず1段目で止まる。「配信は4経路すべて維持」と書いていたが、4つ目は通っていなかった。`terminal-notifier` を `PATH` から外して測り直したところ、`osascript - "Claude ✅ <repo名>" "<body>"` が呼ばれ、`rc=0`・stdout 空・stderr 空だった。どちらも無い環境では何も呼ばずに `rc=0` で返る。**これは Copilot が名指しした4件と同じ型（確かめた範囲より広く書いている）を、指摘を受けて自分で探して見つけたものである。**

`-w "$pt"` が偽になる分岐（tmux の中なのに書けないペイン TTY）も別に測った。読み取り専用のファイルを渡した場合と、存在しないパスを渡した場合の両方で `rc=0`・stdout 空・stderr 空になり、フォールバックへ落ちて偽 `terminal-notifier` が記録した。

配信の中身も見た。`HOME` 未設定のときと `HOME` 通常のときで、偽 `terminal-notifier` が受け取った引数は**完全に一致**した（`-title` が `Claude ✅ <repo名>`、`-message` が `<ブランチ名>`）。到達しただけでなく同じものが届いている。

`wait` の切り詰めは、日本語120文字を入れて body 末尾が「あ」80個になり、UTF-8 として妥当なまま返ることを確かめた。

### 追記先の6通り（ゲート3で足したガード + Copilot 指摘後）

| 追記先の形 | 修正前 | 修正後 |
|---|---|---|
| 存在しない | 作成して追記 | 変わらず作成して追記 |
| 通常ファイル | 追記 | 変わらず追記 |
| ディレクトリ | `Is a directory` が stderr へ漏れる | 静かに飛ばす |
| FIFO | **8秒で打ち切るまで戻らない（`rc=124`）・stderr は空のまま** | 0.05秒で `rc=0` |
| 通常ファイルへのシンボリックリンク | **リンク先へ追記された** | リンク先に触れない |
| 壊れたシンボリックリンク | **リンク先が作られて書かれた** | リンク先を作らない |

後ろ2つは Copilot の指摘で見つかった。ゲート3で足したガードは `-f` がリンクを辿ることを見ておらず、壊れたリンクは `-e` が偽になって「存在しない」側へ回る。どちらも `/tmp` から任意のパスへ追記させる経路になる。`-L` で別に弾いた。

**借りてきた元とは置き場の条件が違う。** `block-destructive.sh` の `hfr_appendable` の追記先は `${HOME}/.claude/state/hook-firing` で利用者が持つディレクトリだが、`notify.sh` が書くのは world-writable な `/tmp` である。同じファイルのコメントが「`HOME` が無いときに `/tmp` へ落とさない」と避けるべき場所を名指ししているのに、`notify.sh` はそこへ書いている。判定を同じにしても条件は同じにならない。ガードではなく置き場を `${HOME}` 配下へ移すのが本筋だという指摘を実装SO の claude レーンから受けた。受け入れ条件の外なので本 PR では触らず、follow-up として surface する。

### 走査の対照（DJ-7）— 8本を `env -u HOME` で実起動

| script | `HOME` を読む位置 | rc | stdout | stderr |
|---|---|---|---|---|
| `block-destructive.sh` | 冒頭の代入 | 0 | 空 | 空 |
| `block-force-push.sh` | 冒頭の代入 | 0 | 空 | 空 |
| `cc-lint.sh` | 冒頭の代入 | 0 | 空 | 空 |
| `commit-gate.sh` | 参照なし | 0 | 空 | 空 |
| `hypothesis-gate.sh` | 参照なし | 0 | 空 | 空 |
| `notify.sh` | デバッグ判定（入力に依存しない） | 0 | 空 | 空 |
| `oe-prompt-receipt.sh` | 冒頭の代入 | 0 | 空 | 空 |
| `session-name.sh` | 冒頭の代入 | 0 | 空 | 空 |

**この対照について、当初は弱い方向と強い方向の両方に書き間違えていた。** 実装SO の claude レーンが両方を指摘した。

強い方向の間違いは、「grep の結論が振る舞いの側からも支持された」と書いたことである。**支持されていない。** DJ-7 がこの対照に期待した役目は「grep のパターンが見落とす形」を振る舞いから捕まえることだったが、`env -u HOME` で捕まるのは `set -u` が殺す形、つまり素の `$HOME` だけである。DJ-7 が名指ししたチルダ展開は `HOME` 未設定でも passwd の値へ落ちるので落ちないし、間接参照も変数が設定済みなら静かに通る。**この対照は DJ-7 が挙げた穴のどちらも塞いでいない。** 穴が実際に空であることは別に確かめたが、確かめた手段はやはり grep（`~/` の出現はコメント4件と引用符付きリテラル1件だけ、`${!` は0件）であって、振る舞いではない。

弱い方向の間違いは、「`done` と `{}` という `notify.sh` の呼び方を他フックにも与えているので、各フックの本来の処理を通したわけではない」と書いたことである。これは自分を過小に書いていた。**`HOME` を参照するのは8本のうち6本で**（`commit-gate.sh` と `hypothesis-gate.sh` には参照が無い。上の表のとおり）、その6本の参照はすべて入力に依存する分岐より上にある。だから `env -u HOME` で起動すれば、入力の形に関係なくその行が実行される。言えるのは「起動直後に落ちる本は他に無い」ではなく「**8本を起動し、`HOME` を参照する6本ではその参照箇所を通した**」である。

### 手を出さなかったこと

`./scripts/sync.sh` はこの worktree から実行していない。3ツールの symlink が worktree へ張り替わり、worktree を消すとリンクが壊れるためである。稼働中のフックは master の作業ツリーを指したままなので、この作業はマージされるまで手元の通知に影響しない。

`/tmp/notify-hook.log` は着手時に存在しなかった。検証で作ったものは毎回消し、最後にも存在しないことを確かめた。

昇格の印: 検証の足場が本番と違う環境を作ってしまう失敗を、この作業だけで2回踏んだ（`panetty` の制御端末獲得と `ctty` の端末解放）。DJ-6 が警戒したのは「対照が本番と違う分岐を通る」ことだったが、実際に起きたのは「足場自身が壊れる」で、同じ根から出ている。足場を組むときの一般則として残す価値があるかを closure で判断する。

昇格の印: 借りてきたガードは、判定が同じでも**置き場の条件まで同じとは限らない**。`hfr_appendable` は利用者が持つディレクトリ、`notify.sh` は world-writable な `/tmp` で、同じ判定の安全性がまったく違う。負の知識 `01M1272GA8CRXQKQWMF005NHCF`（借り物が何をしていないかを確かめよ）の一段深い形として、closure で観測に足す。

## Step 9: ゲート4（実装SO + Copilot）

### Copilot

`gh pr edit --add-reviewer @copilot` も API 直接も 200 を返すのに `requested_reviewers` が空のままだったが、しばらく待つと自動でレビューが付いた。この repo では依頼ではなく自動で走る仕組みらしい。

指摘は4件。シンボリックリンクの穴（moderate）は本物で、対応した（上の表）。plan の Step 2 の見出しが「`HOME` 4通り」なのに6つ並べていた誤りも直した。残り2件は対応しない理由を書いて返信した。1つは設計SO の記録の「4通り」で、あれは claude レーンが実際に当てた組み合わせの報告なので6に書き換えるとレーンがやっていないことを報告することになる。もう1つはゲート5が未完了という指摘で、事実だが closure はマージ前に置く決まりなので順序どおりである。

### 実装SO（弱・2レーン）

期待2者・成功2・部分0・失敗0（codex 336秒、claude 634秒）。**両レーンとも、Copilot が見つけたシンボリックリンクの穴を独立に挙げた。** 3者が同じ1点に当たったことになる。

両レーンが一致して挙げた指摘は次の3つで、いずれも取り込んだ。

1. **述語が落とすのは2つの綴りだけで、「`/` を指すパスの集合」ではない。** `///` ・ `/.` ・ `/..` ・ `/./` ・ `/tmp/..` が通る。engine と同じ述語のままにする判断は変えないが、DJ-2 の書き方が実装より強かったので直した。
2. **episode に実測の証拠が残っていない。** 「全13ケースで rc=0」と書いてあるだけで、13ケースが何だったかも足場の中身も復元できない。plan の Step 5 が「足場の中身と出力を episode に記録する」を実行項目として挙げていたので、これは未履行だった。上の表がその履行である。
3. **`hfr` から借りたのは判定であって置き場の条件ではない。** コメントに1行足した。

claude レーンだけが挙げた指摘も2つ取り込んだ。コード中のコメントの「短絡に頼らず」は不正確（新しい形も短絡に頼っている。変わったのは素の `${HOME}` の展開が条件式の外へ出たこと）で、書き直した。README のデバッグ節に「書けなかったときは無言で諦める」を足したのは、この PR がデバッグログの失敗を完全に静かにした結果、「デバッグを立てたのにログが空」と「フックがそもそも呼ばれていない」が区別できなくなるためである。**これは plan の Step 4 が挙げていない追加なので、PR で明示する。**

`jq` の下限の記述は、両レーンが独立に jq のソース（`jq-1.4` の `jv_aux.c` と `main.c`、`jq-1.5` の `main.c`）まで当てて正確だと確認した。claude レーンは `."last-assistant-message"` と `//` の構文も `jq-1.4` の `parser.y` にあることまで当てている。置き場については claude レーンが「版の下限は依存の話で advisory 安全性とは関係が無い」と言うので、`notify.sh` 節の中に「依存」の小節を作って移した。ゲート3の裁定（「前提条件」節には書かない）とは衝突しない。

**レーンの主張のうち、自分で当て直したもの**: 述語が通す値の一覧（実行して確認）、シンボリックリンク2通りの実害（実行して再現）、8本の `HOME` 参照位置（grep）、`~/` と `${!` の出現（grep）、`/private/tmp` が `drwxrwxrwt` であること（`ls -ld`）。

## closure（ゲート5・マージ前）

tier は **heavy**。下の「事実・失敗」のとおり撤回と外部指摘が複数あり、品質ゲート目的で `so-compare` を3回明示起動し、棄却した選択肢のある設計判断があり、昇格候補もある。

### Context / なぜ

冒頭「なぜこの作業が始まったか」節にある。`notify.sh` は advisory なのに、環境の事情で落ちる・雑音を出す箇所が3つ残っていた。#343 の実装子が範囲外として surface し、gate 4 の codex レーンが別 issue にすべきと言ったことから #347 が起票された。

### 事実・失敗

実行ログ（コミット列・PR #389 のレビューと返信・`so-compare` 3回分）にある失敗・撤回・指摘を、起きた順に列挙する。詳細は再掲せず本文を指す。

| # | 何が起きたか | 誰が見つけたか | 本文 |
|---|---|---|---|
| 1 | 「読むだけだから `HOME` の可否は非空で足りる」が崩れた。`${HOME:-}` を退けた理由が `HOME=/` にそのまま当てはまるのに気づいていなかった | 設計SO の両レーン | 本文: Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った |
| 2 | 同じ欠陥を「2箇所」と数えた。3箇所目は grep の出力に含まれていたのに、配信のブロックに限って読んで落とした | 設計SO の両レーン | 本文: Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った |
| 3 | `jq` の下限の置き場を「前提条件」節と決めていたが、全フック共通の節なので他フックの `jq -cn` と矛盾する | 設計SO の両レーン | 本文: Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った |
| 4 | 陽性対照を `bash -x` のトレースで取る設計が、本番と違う分岐を通っていた | 設計SO の claude レーン | 本文: Step 4: 設計SO（ゲート2・弱・2レーン）で3つの判断が覆った |
| 5 | 検証の足場が2回壊れた（`panetty` で制御端末を獲得して書き込みが返らない・`ctty` で端末が解放され `/dev/tty` が開けない） | 自分（修正前の版に当てて期待した失敗が出ないことで気づいた） | 本文: Step 6: 検証の足場を先に組み、修正前に壊れることを確かめた |
| 6 | ゲート3で足したガードがシンボリックリンクを見ておらず、`/tmp` から任意のパスへ追記させる経路を新しく作っていた | Copilot と実装SO の両レーン（3者が独立に） | 本文: Step 8: 回帰確認（証拠を表で残す） |
| 7 | **plan の Step 5 に自分で入れた「足場の中身と出力を episode に記録する」を履行していなかった。** 結論だけ書いて証拠を残さず、外部レビューで指摘されて初めて書いた | 実装SO の両レーン | 本文: Step 9: ゲート4（実装SO + Copilot） |
| 8 | 走査の対照についての記述が強い方向と弱い方向の両方に間違っていた | 実装SO の claude レーン | 本文: Step 8: 回帰確認（証拠を表で残す） |
| 9 | コード中のコメントの「短絡に頼らず」が不正確だった（新しい形も短絡に頼っている） | 実装SO の claude レーン | 本文: Step 9: ゲート4（実装SO + Copilot） |
| 10 | 借りてきたガードの「判定」は写したが「置き場の条件」（world-writable な `/tmp` か、利用者が持つディレクトリか）を見ていなかった | 実装SO の claude レーン | 本文: Step 8: 回帰確認（証拠を表で残す） |
| 11 | plan にない README の追記（デバッグ節の「無言で諦める」）が必要になった | 自分（実装SO の指摘を受けて判断） | 本文: Step 9: ゲート4（実装SO + Copilot） |
| 12 | **closure の初版に「事実・失敗」節が無く、上の失敗の大半がどこからも指されていなかった。** 昇格の判定にも理由を書いていなかった | closure の外部チェック（ゲート5）の両レーン | 本文: closure（ゲート5・マージ前） |
| 13 | **issue #347 と plan への反映が漏れていた。** issue は崩れた形を「正確な形」として推奨したまま、plan は全 step 未チェックの `draft` のままだった | closure の外部チェック（ゲート5）の両レーン | 本文: closure（ゲート5・マージ前） |
| 14 | **「配信は4経路すべて維持」と書いていたが、`osascript` の分岐は一度も通していなかった。** 足場が `terminal-notifier` を必ず先に見つけるため、フォールバックの2段目に到達していなかった | 自分（Copilot の2回目のレビューが挙げた4件と同じ型を探して見つけた） | 本文: Step 8: 回帰確認（証拠を表で残す） |

### ゲート5の外部チェック（heavy tier・Step 4）

`so-compare --with codex,claude` で closure だけを対象に回した（確認対象は選択的省略・follow-up の網羅・evidence anchor・back-propagation の4点）。両レーンとも実返却あり。**上の 12 と 13 はこの回で出た。** 指摘を受けて closure を書き直し、plan の状態を確定し、issue #347 へ訂正のコメントを出した。

生出力は作業層の一時領域にあり、マージ後は残らない。指摘の中身と対応は本節に転記した。

### 次の消費者

- **`canonical/hooks/scripts/` を次に触る人**（本文: Step 8: 回帰確認（証拠を表で残す））。hooks にテストの置き場が無いので、回帰を見るには足場を組み直すことになる。何を組んだかが書いてある。
- **engine の `_oe_home_usable` の穴を閉じる人**（本文: Step 9: ゲート4（実装SO + Copilot））。同じ述語が2箇所にあり、engine 側は判定のあと `mkdir -p` まで進む。
- **`hfr_appendable` を他所へ写そうとする人**（本文: Step 8: 回帰確認（証拠を表で残す））。判定を写しても置き場の条件は写らない。

### follow-up の行き先

| 残課題 | 行き先 |
|--------|--------|
| engine の `_oe_home_usable` が `///` ・ `/.` ・ `/..` ・ `/./` を通す（engine は `mkdir -p` まで進むので実害が大きい） | **Issue を起票する**（本 PR のマージ後・親 / owner の判断を仰ぐ） |
| `notify.sh` のデバッグログの置き場を `${HOME}` 配下へ移す（world-writable な `/tmp` をやめる） | **同じ Issue に含める**（述語が使えるかの判定はもう手元にあるので、同じ単位で扱える） |
| `block-destructive.sh` の `hfr_appendable` にも同じシンボリックリンクの穴がある | **同じ Issue に含める**（置き場が `${HOME}` 配下なので優先度は低い） |
| `cc-lint.sh` / `block-destructive.sh` / `block-force-push.sh` が `HOME` を非空止まりで受けている | **同じ Issue に含める**（述語を揃えるかどうかの判断が engine 側の穴と同じ単位になる） |
| 確認してから開くまでの隙（追記先が差し替わる窓）が閉じていない | **追わない**。`block-destructive.sh` の既存ガードと同じ限界で、閉じるには方式ごと変える必要がある。上の「置き場を `${HOME}` 配下へ移す」が入れば露出面は下がる |
| `canonical/hooks/` にテストの置き場が無い | **追わない**。置き場を作るのは規約の決定で、この単位では決められない。必要になった人が起票する |
| stdin が閉じられない場合に `cat` が戻らない | **追わない**（推測のまま。ハーネスが実際にそう起動する証拠が出たら起票する） |
| `tr` や `basename` が無いときの stderr、README の「外部呼び出しは全て `|| true`」が厳密でない | **追わない**（通常の macOS では起きない） |
| 承認ゲートの裁定に従うと baseline が動く構造を、規範（委譲の固定節 / `implementer-contract`）へどう落とすか | **#391 を起票済み**。この枝では discussion までで止める |
| `session-name.sh` と `oe-prompt-receipt.sh` のリダイレクト順 | **追わない**（書き先が通常ファイルで、`/dev/tty` のような「guard を通るのに開けない」相手ではない） |

### 昇格の判定

7件。frontmatter の `promotion` に1判定 = 1エントリで置き、理由はここに書く。

1. **`notify.sh` の `HOME` 可否を engine の述語へ揃え、他の hook は揃えない** — `not-required`。単体配布されるスクリプトが engine の述語をインラインで複製する形は `oe-hookfire` に先例があり、新しい原則ではない。他の hook を揃えるかどうかは判断でなく残課題なので follow-up 表へ回した。
2. **engine と同じ述語のままにし、厳しさをずらさない** — `not-required`。棄却案は plan の表に具体名で並んでおり、判断としては非自明だが、根拠は 1 と同じ先例に乗っている。ずらさない理由（同じ名前の判定が2つの意味を持つ）は plan の DJ-2 に書いてあり、decision へ上げても内容が増えない。
3. **借りてきた述語は写す / 写さないの2値でなく成分に割って要否を決める** — `not-required`。negative knowledge item `01M1272GA8CRXQKQWMF005NHCF` の行動変更にそのまま当たるので、観測として書き戻した。新しい原則を立てるより、既存 item の適用例が増えるほうが正確である。
4. **走査は当たっていたのに走査結果の読み取りで取りこぼした** — `not-required`。`01M07QDKE73BTK0Q3K90FE4G9T` の観測へ「走査の健全性と読み取りの健全性は別の段である」として着地させた。着地先が決まっているので判定の材料は足りている。
5. **検証の足場が本番と違う環境を作ってしまう** — `not-required`。`01M00KCCHNMFPHP5HAGX2DZ1MK` の行動変更 (2)「足場は測りたい対象と同じ場所・同じ解決規則の下に置く」がそのまま当たる。新設は重複になる。
6. **借りてきたガードは判定が同じでも置き場の条件まで同じとは限らない** — `not-required`。3 と同じ item の一段深い形として観測に書いた。
7. **裁定に従うこと自体が承認時の baseline を動かす場合がある** — `required`。棄却した扱い方が実在し（黙って進む・矛盾として止まる）、覆すには委譲契約のガードの設計を議論し直す必要がある。既存の指示矛盾ガードが並べる3つ（終端の再定義・未達 step の飛び越し・要件の弱体化）のどれでもない。**昇格の第一段として `docs/harness/discussions/2026-09-11-discussion-347-approval-baseline-drift.md` を書き、この枝の PR に載せた。** 規範そのもの（委譲の固定節・`implementer-contract` の弁別子）の改訂は別単位なので、#391 を起票して surface した（close はしない）。

### negative knowledge の収穫

**収穫なし。** 候補2件はどちらも既存 item に着地済みだった（上の 3・5・6 の理由）。

### 観測の書き戻し

注入された item は4つで、id は次のとおりである（brief は作業層なので消える。分母を復元できるのはこの行だけである）。

- `01M1272GA8CRXQKQWMF005NHCF`（借り物が何をしていないかを確かめよ）→ `followed`
- `01M07QDKE73BTK0Q3K90FE4G9T`（走査の対照はパターンの外から取れ）→ `followed`
- `01M00KCCHNMFPHP5HAGX2DZ1MK`（期待した色が出た理由まで確かめよ）→ `followed`
- `01KZKBS28N0D8JF7DKYF9MW56C`（既定動作を書き換えるスクリプトにガードを足したときの挙動不変の確認）→ `followed`

4件目は brief が「自然文としては注入していない」と断ったうえで名前を挙げていたものである。**slot に載っていたかの線引きが曖昧だったので、落とさない側に倒して1レコード足した。** 4件とも `validate-knowledge` が通ることを確かめた。

### evidence anchor

本文の表は揮発する一時領域で測ったものだが、**数値と結論は本文へ転記済み**で、一時領域を開かなくても読める。足場の中身も文章で書いてある。`so-compare` 3回分の生出力も同じ一時領域にあるが、指摘の中身と対応は本文（Step 4・Step 9・本節の外部チェック）に転記した。

### back-propagation

- **issue #347**: 本文が `${HOME:-}` を使う形を「正確な形」として推奨しているが、その形は `HOME=/` を防げない。受け入れ条件の「3ツール同時反映」も、実際に `notify.sh` を呼ぶのは2ツールである。**どちらも issue へコメントで訂正した**（close はしない。owner の判断）。
- **plan**: 全 step を実施済みに更新し、`status` を `stable` にした。DJ-4 が裁定の出所として `.oe/addendum-347-gate3.md` を指していたが、`.oe/` は git 管理外で plan だけを読む人が到達できないので、裁定の内容を plan 本文へ写した。
- **README**: この PR がデバッグログの失敗を完全に静かにしたので、デバッグ節に「書けなかったときは無言で諦める」を足した（plan の Step 4 にない追加であることを PR に明示した）。
- **knowledge item 4件**: 観測として書き戻した（上記）。

### status

`stable` / 達成。issue #347 の受け入れ条件6つすべてに対応がある（plan の「受け入れ条件との対応」表）。ゲート3で足した1件と、Copilot・実装SO で見つかった追加の1件も入っている。マージは owner が判断する。
