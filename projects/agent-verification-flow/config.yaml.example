# AI駆動検証フロー設定ファイル
# このファイルをコピーして config.yaml を作成し、実際の値を設定してください
# cp config.yaml.example config.yaml

# ===========================================
# 対象Webサービスのアーキテクチャ前提:
#   - フロントエンド: SPA (Single Page Application)
#   - バックエンド: REST API
#   - API仕様: OpenAPI / Swagger
#   - 認証: JWT (OAuth2/OIDC) または Session + CSRF
# ===========================================

# API設定
api:
  # APIベースURL（必須）
  base_url: "https://your-app.example.com/api"

# JWT認証設定（IdPを使用する場合）
# AWS Cognito の例。Auth0, Firebase Auth 等の場合は適宜調整
cognito:
  region: ap-northeast-1
  user_pool_id: "YOUR_USER_POOL_ID"
  client_id: "YOUR_CLIENT_ID"

# 認証情報
auth:
  username: "your-email@example.com"
  password: "your-password"

# エラー監視設定（オプション）
# Sentry, Datadog, New Relic 等
# 環境変数でも設定可能: SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT
error_monitoring:
  provider: sentry  # sentry, datadog, newrelic など
  org: your-org
  project: your-project
  # auth_token: "your-auth-token"  # 環境変数推奨
