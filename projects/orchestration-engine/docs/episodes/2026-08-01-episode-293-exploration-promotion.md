---
id: "01KYYH276FGBTBACF0NX9WFRSW"
title: "#293 探索の昇格 — 受け渡し経路での書面検査を止め、知見を committed 層へ移す"
date: 2026-08-01
type: episode
status: stable
related:
  - type: refs
    ref: "https://github.com/stlwolf/ai-development-hub/issues/293"
    reason: "本 issue。owner の gate 3 判断 = 受け入れ条件2 を強く読む・止めて keep-open・本命は #291 へ合流"
  - type: refs
    ref: "https://github.com/stlwolf/ai-development-hub/issues/291"
    reason: "合流先。承認時の義務コンパイルを扱う単位"
tags: [orchestration, brief-verification, negative-knowledge, so-lane]
---

# #293 探索の昇格 — 実行記録

**この episode はリアルタイム追記である**（後追い再構成ではない）。判断・撤回・棄却をその場で書き、closure から pointer で指す。

## Y1. worktree と episode 枠（2026-08-01）

- ブランチ `docs/#293_exploration_promotion` を `master`（`ceeccd38`）から作成し、worktree を子が自作した（統括は hands-off）。
- `wt switch --create` は worktree を作るが Claude セッションの cwd は動かない（既知の挙動）。以後の編集はすべて絶対パスで行う。
- 本単位は **Y1〜Y6 で、前の単位（Z1〜Z5・Z5 で終端済み）とは別体系**である。

### 本単位に入る前の状態（前単位の到達点）

前単位は `.oe/plan-293-brief-transport-check.md`（`status: halted-unconverged`）で止まった。反証を2ラウンド回し、どちらも refuted だった。

- ラウンド1（gate 1・`oe-refute --lanes 3 --rubric exploration`）: refuted 3/3
- ラウンド2（gate 2・設計SO・`so-compare --with codex,claude,cursor`）: refuted 3/3

owner の gate 3 判断（本単位の前提・議論の対象ではない）:

1. 受け入れ条件2 は字面どおり強く読む（子が受領時に**起動する**検査）。**したがって送信側で検査するどの案も未達である。**
2. #293 は止める・keep-open。本命は #291 へ合流する。
3. advisory lint は切り出さない。**P5 の実測は #291 の設計材料として残す。**
4. canonical は1行も変更しない（規範を積まない・#290 と同じ形）。

## Y2. discussion の起草

成果物: `projects/orchestration-engine/docs/discussions/2026-08-01-discussion-293-brief-transport-verification.md`

### 選別の判断（7候補すべてを検討し、落としたものにも理由を書いた）

昇格したのは8件（搬送路17経路 / ULID の非引用専用性 / 選定基準の崩壊 / P8 と P5 の実測 / #289 を覆せなかったこと / claude レーンのタイムアウト / gate 0 の矛盾 / 三つの arc の一致と不一致）。落としたのは4件で、内訳は discussion §1 の表にある。

**落とす判断で迷った1件**: 案H（凍結コピー）の仕様の穴の一覧。**穴の一覧は落としたが、「なぜ送信側の位置では構造的に不能か」は残した**（§7.2）。理由は、凍結案が直観的に出やすく、**同じ位置に再提案されるのを防ぐ**必要があるためである。穴の一覧そのものは、将来 spool 型を検討する単位が再導出できる。

### 測定のやり直し（統括との数値の食い違いを解消した）

統括は ULID の実数を独立に数え、`145件中 knowledge item は14件` と報告した。委譲子の前単位の報告は `132件中117件が非 item` だった。

**この2つの関係を Y2 の時点では「範囲と単位が違うだけ」と書いたが、それは正確でなかった。** 統括の値は1つの方法で取った1つの比率だが、**委譲子の `132件中117件` は範囲も単位も違う2つの数を1つの比率として並べたもので、単なる別測定ではなく不正な比較だった** [verified]（Y4 の実装SO が指摘し、認めた）。

- `132` は `projects/orchestration-engine/docs/` + `canonical/` の**行数**。
- `117` は `canonical/` を**含まない**範囲の**ファイル数**。

**Y4 で方法を1つに固定して測り直した**（`^id:` 行・引用符の有無を問わない・作業ツリーの実体）。結果は範囲A（engine docs + canonical）が 141 / 15 / 126、範囲B（repo 全体）が 162 / 15 / 147 で、discussion §4 に測定コマンドごと置いた。**独立の再計数（実装SO 2レーン）とは ±1 で一致し、差は正規表現の細部に由来する。** どちらかを正と断定していない（brief の受け入れ基準）。

### 過大主張を避けるために明示的にやったこと

この arc は rev.1 と rev.2 で計4件の「効果の言い切り」で崩れた。本 doc では効果を書いた箇所に成立条件を併記し、とくに次の3つは範囲を明示的に限定した。

- §5.3（摩擦の誘因）: 「期待値が負になりうる」までにとどめ、「負である」とは書かない（誘因が実際に効くかは未測定）。
- §8.3（過去の SO 証跡の読み替え）: 「2レーンだった」という事実と「3レーン目が前提を崩しうる」という1事例までにとどめ、**「過去の判断が誤っていた」とは書かない**（落ちていたレーンが何を言ったかは検証できない）。
- §9（gate 0 の矛盾）: 1事例であり #290 とは観測範囲も違うので、**一般法則としては書かない**。
- §10（三つの収束）: 収束は確信を上げるが証明ではない。**3 arc は同じ repo・同じ spec・同じ統括系統の下にあり共通の盲点を共有しうる**こと、**義務コンパイルが効くかはどの arc も検証していない**ことを明記した。

### 参照の張り替え

`#289` と `#290` の discussion の frontmatter に、本 doc への `related` を追加した。三つの収束をどの入口からでも辿れるようにするため（brief の指定）。**本文は変更していない。**

## Y3. negative knowledge の判定

統括が挙げた候補2件を、既存14件と対照して判定した。**1件を採用し、1件を棄却した。**

### 採用: `01KYYHGNRJ926N4CTTS4PE8GQH`（遅れて返るレーンは判定の前提を崩しうる）

統括の候補1 をそのままではなく、**2つの教訓に分けたうえで片方だけを採った。**

- **(1a) 空返却を「このレーンは返らない環境だ」という恒常的な性質として読んだこと。** → **既存 `01KYMRE1N7N0J8VP3CBZZEZ8Q3`（ヒット0件を「対象が存在しない」と読むな）と同型である。** レーンという単位での再生産にすぎないので、新 item にせず、新 item の本文でその item を参照する形にした。
- **(1b) partial を disclose して判定を閉じたあと、遅れて返ったレーンが判定の前提そのものを崩したこと。** → **既存14件のどれにも該当しない。** ここを新 item にした。

重複の対照（`knowledge-list --strict` で14件を列挙し trigger を突合した）:

| 既存 item | 近さ | 採否への影響 |
|---|---|---|
| `01KYMRE1N7N0J8VP3CBZZEZ8Q3`（0件を不在と読む） | (1a) と同型 | (1a) は新 item にしない。新 item 本文から参照 |
| `01KYSKQHDT86GZW87E8EDMSE46`（遮断の下流に口が残る） | 遠い（経路の残余の話） | 影響なし |
| その他12件 | 該当なし | — |

`landing: nl` にした理由: 機械化できる部分（runner の完了を待ってから出力を読む）は `oe-refute` が既に実装している。**残るのは「待たずに進むと決めたなら、遅延レーンが返ったときに判定を読み直す」という判断の規律**なので NL に置いた。機械側の話は `exclusions` に書いた。

**範囲の限定を本文に明記した**: 「作業量の多いレーンほど遅い側に偏る」は本 arc の1事例から立てた仮説であって、複数事例で確かめていない。

### 棄却: 「摩擦を一部の経路にだけ足すと、無検査の経路へ流れる誘因を作る」

**棄却の理由は、この arc がその誘因を観測していないことである。**

- 検査は実装されていないので、**書き手が摩擦を避けて経路を変える様子は誰も見ていない。** 昇格 doc §5.3 も「期待値が負になりうる」までにとどめ「未測定」と明記している。
- 既存 `01KYSKQHDT86GZW87E8EDMSE46`（遮断を1層で決めても下流の口が残る）と `01KYCQV7SD6BVBWVCRFYJH9TYG`（deny-list は同じ family の迂回路が残る）は「穴が残る」を扱う。候補2 は「穴が**相対的に魅力的になる**」という追加の因果を主張するもので、**その因果こそが未観測の部分である。**
- 既存 `01KYMRE1NE4HSGZR7T4XPA9JW8`（自分が導出した一般化を書くとき、根拠を示す）が警告する型に、この候補自身が当たる。

**棄却しても知見は失わない**: 誘因の論点は昇格 doc §5.3 に「未測定」と明記して残した。**観測が出た時点で item 化すればよい**（store に速く入れることが目的ではない）。

### 既存 item への観測書き戻し（4件）

前単位で注入された4件すべてに観測を書き戻した。

| item | state | 要点 |
|---|---|---|
| `01KYW0BPJNHP94TNX0VG4G11TX` | **`externally_verified`** | 予測が的中した。型を名指しした直後の doc で1件、次版で3件、**自己検出は4件すべてゼロ**。同時に教訓の処方（外部の目を必ず入れる）は守っており、その外部レーンが4件全部を捕まえた。予測と処方の双方が外部判定で確認された形 |
| `01KYSKQHDT86GZW87E8EDMSE46` | `followed` | 搬送路を統括の3件から数え上げ、最終的に17経路（flag を通る4 verb ＋ 通らない13経路）を列挙して「迂回可能である」を明記した。**ただし最後の1件（`oe-kick` の issue モード）は自力では出ず、外部の目で初めて閉じた** |
| `01KYJ76D830XME16ZFXC2XRPZZ` | `followed` | 述語をトークン類の側から定義した。**ただし母集団側から取っても網羅にはならず、「尽くした」の主張は取り下げた** |
| `01KYMRE1NC7XX6N66RQ0MGGHF1` | `followed` | 統括の断定を実測で訂正した。**ただし転記時の条件落ちを1件やっており、書き写しの向きでは効いたが転記の網羅では取りこぼした** |

**4件とも「効いた」だけを書かず、効かなかった側も同じ観測に書いた。** `state` は最も強いもの1つを選ぶ規約なので、取りこぼしは `note` に載せている。

### 検証

- `validate-knowledge projects/orchestration-engine/docs/knowledge/items/` → **OK・exit 0**。
- `knowledge-list --strict` → `listed: 14 / skipped: 0`（**git-head を読む実装なので、未コミットの新 item はまだ数に入らない**。コミット後に15件になる）。
- 制御バイトの混入チェック（`grep -nP '[\x00-\x08\x0B\x0C\x0E-\x1F]'`）→ 検出なし。

## Y4. 実装SO（gate 4）

`so-compare --with codex,claude,cursor` を `SO_TIMEOUT=900` で起動した（brief の「claude レーンを720秒以上で」を満たす設定）。出力は `tmp/so-293-y4/`。

### レーンの返却状況

| レーン | 経過 | 結果 |
|---|---|---|
| codex | 535 秒 | `success` / 11,504 バイト |
| cursor | 188 秒 | `success` / 8,410 バイト |
| claude | 1回目 958 秒 → リトライ 795 秒 | **`timeout_empty` → `error_partial` / 61 バイト**（`You've hit your session limit`） |

**他族2レーン（codex / cursor）が実返却したので「他族2以上・0レーン不可」は満たす。** claude レーンは720秒以上の設定で起動したが、**返らなかった。**

### claude レーンの失敗は3つ目の型だった（discussion §8 に反映）

前単位で観測したのは「480 秒では足りず 652 秒で返る」という時間不足だった。今回は **900 秒でも1回目が `timeout_empty`、リトライがアカウントのセッション上限に当たった。**

したがって discussion §8.2 に書いていた「480 秒での運用では**構造的に**空返却になる」は言い過ぎである。**空返却の原因は少なくとも3種類あり、`timeout_status` / `exit_code` / stdout の中身で区別できる**、という形に書き直した。**「タイムアウトを上げれば返る」も同時に否定された。**

### 判定を作る前に runner の完了を待った（Y3 で採用した item の実践）

Y3 で採用した `01KYYHGNRJ926N4CTTS4PE8GQH`（遅れて返るレーンは判定の前提を崩しうる）に従い、**so-compare のプロセスが終了するまで待ってから**レーン出力を読んだ。前単位では途中出力を読んで判定を作り、あとから claude レーンが返って前提が崩れている。**同じ失敗を繰り返していない。**

### verdict: 両レーンとも `refuted`

**指摘は妥当だった。実測で裏を取り、13箇所を修正した。** 主なものは次のとおり。

| 分類 | 指摘 | 対応 |
|---|---|---|
| 事実誤認 | **§10 の収束の主張が事実と違う。** #290 の到達点は消費時検証と committed カタログであって、義務コンパイルではない | §10 を書き直した。**「三つが同じ場所へ収束した」を撤回し、「否定側（規範の層では届かない）で一致・肯定側は2対1で割れている」に改めた。** これが最も重い修正である |
| 事実誤認 | ULID の数値が再現しない・内訳の合計が合わない（2+2+4+6=14 なのに差は13） | 測定述語をコマンドで明示し、方法を1つに固定して測り直した（§4.1）。当初の比較が不正だったことも記録した（§4.2） |
| 事実誤認 | 経路数の算術が合わない（表は13行、4 verb と足せば17なのに「14経路」） | 数え方を明示し 4 + 13 = 17 とした。前単位の「14」は別の数え方だと注記 |
| 事実誤認 | `#289` の引用行がずれている（`:137` は空行・`:147` は表ヘッダ） | `:140` / `:150` に修正（**自分が frontmatter に3行足したことでずれていた**） |
| 事実誤認 | `oe-select:163` はコメントで、正規例示は `:11` | 修正 |
| 撤回済み主張の復活 | §7 理由1「述語が成立しない」が、§6.2 の「形式 (f) が真陽性を出した」と矛盾し、撤回済みの無条件形を復活させている | 「一般的な受け皿にならない」に改め、(f) が実際に捕まえた事実を残した |
| 撤回済み主張の復活 | §4末尾「型判別を持たない存在検査は実装できない」 | 「実装はできるが意味のある判定にならない」に修正 |
| 成立条件の欠落 | §3.1 の見出し「検査を置けば覆える」が TOCTOU を落としている | 見出しを「検査を置ける位置がある」に変え、TOCTOU が残ることを本文に明記 |
| 成立条件の欠落 | §3.4「たいていの委譲で効かない」に頻度の実測がない | 頻度は測っていないと明記し、「通常の使い方の中にある」までに落とした |
| 成立条件の欠落 | §7 理由2「書面を誤るような親ほど `--brief` も省きやすい」は未観測の相関 | **削除し、削除した旨を残した**（§5.3 の限定と揃える） |
| 成立条件の欠落 | §7.2「送信側で構造的に不能」は、示せているのは「spawn 後の `oe-send` 内で凍結する形」まで | 否定の範囲を限定した |
| 成立条件の欠落 | §6.4「解決先が明示されれば実効性がある」— 解決可能性の条件であって低偽陽性の条件ではない | 区別を明記した |
| 範囲外の一般化 | 新 item の `prediction` が1事例から偏りを一般化している | `prediction` から偏りの主張を外し、`exclusions` に「偏りの向きを根拠にした運用」を追加 |

**反証不成立と判定された箇所もある**（cursor が明示）: §5.3（誘因を「未測定」と限定した書き方）、§6.2（形式 (f) の数の扱い）、§9（gate 0 の1事例限定）。**限定が機能していた箇所と、していなかった箇所が分かれた。**

### 両レーンが共通して指摘した、直せない限界

**`.oe/` と前単位の `tmp/so-293-gate2/` はこの worktree に存在しないので、§6（P8 / P5 の件数）と §8.1（タイムアウトの実測値）は再検証できない。** 本文への転記が唯一の根拠である。**これは昇格の設計上の帰結**（作業層は消える前提）であり、転記の方針自体は brief の指定どおりだが、**監査可能性は失われている**ことを限界として記録する。

## Y5. PR と Copilot

PR: https://github.com/stlwolf/ai-development-hub/pull/297（`Refs #293 #291`・**close しない**）。

### Copilot のレビュー依頼で観測したこと

`gh pr edit 297 --add-reviewer @copilot` は **exit 0 で PR の URL を返したが、レビュアーは登録されなかった**（`reviewRequests` が空のまま）。`gh api -X POST .../requested_reviewers -f 'reviewers[]=copilot-pull-request-reviewer[bot]'` で登録できた。**失敗が黙って通る形なので、依頼後に `reviewRequests` を確認しないと「依頼したつもり」で止まる。**

### Copilot の指摘3件 — すべて妥当で、すべて自分の取りこぼし

**3件とも「Y4 で数値を直したのに、他の場所へ伝播していなかった」型である。**

| 指摘 | 内容 | 対応 |
|---|---|---|
| discussion の title / §1 / §2 | §3.2 で「4 + 13 = 17」と確定したのに、タイトルと選別表と owner 判断の転記が「搬送路14件 / 14経路」のままだった | 17経路へ修正 |
| episode の Y2 / Y3 | 同じく「搬送路14件」と、観測表の「3→13件・14件目」が最終カウントと不整合 | 17経路（flag 4 ＋ 非 flag 13）へ修正 |
| 新規 knowledge item | ULID 比率が「14 対 119〜132」で、discussion §4 の再計測（15 対 126〜147）と食い違う | 再計測値へ修正 |

対応コミット `7c2f494`。3スレッドすべてに返信済み。**再リクエストはしない**（1ラウンドで止める）。

**この3件は Y4 の修正が不完全だったことを意味する。** 実装SO は「数値が再現しない」「算術が合わない」を検出し、私は指摘された箇所を直したが、**同じ数値が使われている他の箇所を探さなかった。** 直した値の伝播先を grep で洗う手順を踏んでいれば自力で気づけた。

## Y6. closure（マージ前・`episode-retrospective`）

**tier 判定: heavy。** heavy トリガに4つ該当する — 実行中の撤回・方針転回がある（`本文: Y4. 実装SO（gate 4）`）/ 意図的に外部レビューレーンを起動した（同）/ 知見の保全が主成果である（`本文: Y2. discussion の起草`）/ 非自明な設計判断で棄却した案がある（`本文: Y3. negative knowledge の判定`）。

### closure gate checklist（Step 2）

- **Context / なぜ**: #293 は「親が子へ渡す書面を子の受領時に検査する」を試みたが、gate 0 が発火位置を送信側に拘束していたため受け入れ条件2 と最初から両立していなかった。owner が gate 3 で止めると判断し、作業層にしか無い知見が消える前に committed 層へ移すのが本単位である。
- **次の消費者**: **#291（承認時の義務コンパイル）を設計する単位。** discussion §3（搬送路17経路・とくに issue 本文が brief として届く経路）・§6.3（spec が完全な順序を機械に渡していない）・§6.4（P5 の実測）・§7.2（envelope 系の先例）が直接の設計材料になる。副次的な消費者として、SO を回すすべての単位が §8（空返却の3原因）を読む。
- **follow-up routing**: 下表のとおり全件に行き先を付けた。
- **status 確定**: `draft` → `stable`。達成度は **部分達成**（下表・11件中10件を満たし、1件は前提が実測と食い違ったため読み替えて満たした）。

### brief の受け入れ基準との照合（11件・数え直した）

**closure に「9件すべて満たした」と書きかけたが、実数は11件だった。** 数えずに書いていたので訂正する（この arc で繰り返している型なので、ここにも残す）。

| # | 基準 | 判定 |
|---|---|---|
| 1 | 昇格対象が「実装差分から復元できないか」で選別され、選別の理由がある | 満たす（discussion §1 の表） |
| 2 | 作業層2本が消えても知見が成立する | 満たす。**ただし監査可能性は失われている**（上記「揮発する証跡の扱い」） |
| 3 | ULID の実数について測定範囲の差が明記され、どちらかを正と断定していない | 満たす（discussion §4.1〜§4.3） |
| 4 | `oe-kick` の issue モードの事実が `file:line` つき | 満たす（discussion §3.3・`bin/oe-kick:110-112`） |
| 5 | claude レーンのタイムアウトが実測値つきで、過去の SO 証跡の読み方への影響も | 満たす（discussion §8.1〜§8.3） |
| 6 | 効果を書いた箇所に成立条件が併記されている | 満たす（Y4 で5件・Copilot 対応で3件を修正した後の状態） |
| 7 | #289 / #290 から参照が張られ、**三つの収束**が読める | **読み替えて満たす。参照は張った（両 doc の `related`）。ただし「三つの収束」という前提が実測と食い違った** — 下記 |
| 8 | NK の採否が判定され、採用分は `validate-knowledge` を通る（重複確認は対照つき） | 満たす（`本文: Y3. negative knowledge の判定`） |
| 9 | canonical を1行も変更していない | 満たす（`git status --porcelain canonical/` が空） |
| 10 | 実装SO が0レーンでなく、claude レーンを720秒以上で起動している | 満たす（`SO_TIMEOUT=900` で起動）。**ただし claude レーンは返らなかった**（`本文: Y4. 実装SO（gate 4）`） |
| 11 | #293 は close せず keep-open（PR は Refs） | 満たす（PR #297 は `Refs #293 #291`） |

**基準7 について（前提を書き換えたので明示する）**: brief は「三つの収束が1箇所で読める」ことを求めたが、**実装SO の指摘で #290 の到達点が義務コンパイルではないと分かった**（#290 が生き残らせたのは消費時検証と committed カタログである）。したがって discussion §10 は「三つの収束」ではなく「**否定側で一致・肯定側は2対1で割れている**」と書いた。**基準の字面（収束が読める）ではなく、基準の狙い（三つの arc の関係が1箇所で読める）を満たす形にした。** これは子の判断で基準を弱めたのではなく、基準が前提していた事実が成り立たなかったための書き換えである。**owner が「収束」の記述を求めるなら差し戻してほしい。**
- **evidence anchor（条件付き）**: 該当あり。下記「揮発する証跡の扱い」。
- **SO 証跡リンク（条件付き）**: Step 4 を実施（下記）。
- **観測の書き戻し（条件付き）**: **本単位の brief に negative knowledge の注入 slot は無かった**（候補の提示のみ）。書き戻したのは**前単位で注入された4件**で、id は `01KYW0BPJNHP94TNX0VG4G11TX` / `01KYSKQHDT86GZW87E8EDMSE46` / `01KYJ76D830XME16ZFXC2XRPZZ` / `01KYMRE1NC7XX6N66RQ0MGGHF1`（`本文: Y3. negative knowledge の判定`）。**期待集合＝この4件で、4件すべてに1レコードずつ書き戻した。**

### follow-up の routing（行き先なしを残さない）

| 残課題 | 行き先 |
|---|---|
| 承認時の義務コンパイルの実効性 | **#291**（合流先・本単位では設計しない） |
| P5（節参照の検査）の偽陽性率の実測 | **#291 の設計材料**として discussion §6.4 に残した。単独では追わない |
| SO の claude レーンのタイムアウト / 利用上限への対処 | **#295**（owner 判断で別単位。本単位は事実の保全のみ） |
| 受領側（子が起動する検査）の設計 | **追わない。** #293 は keep-open だが owner が止めると判断した。再開する場合の出発点として discussion §11 に明記した |
| 経路12（`oe --task-file`）と envelope 系への述語適用 | **追わない**（discussion §11 に未探索として記録） |
| 「摩擦が無検査の経路への誘因を作る」の観測 | **追わない**（未観測のため item 化を棄却。discussion §5.3 に未測定と明記し、観測が出たら item 化する） |
| P2 / P6 の実測 | **追わない**（discussion §11 に未探索として記録） |
| `--add-dir` 経由で子が読む別 doc の検査 | **追わない。** 検査対象の境界の外に置いた（書面が参照する doc の中身は、書面を検査しても捕まらない）。discussion §11 に未探索として記録 |
| 過去の arc で落ちていた claude レーンが何を言ったか | **追わない。** 生出力が残っていないラウンドがあり復元できない。だからこそ discussion §8.3 は「過去の判断が誤っていたとは言えない」と限定している |
| 形式 (f)（矢印行のゲート順序検査）の実運用での誤検知率・感度 | **#291 の設計材料**として discussion §6.2 に母集団8行・真陽性1・偽陽性1 の実測ごと残した。単独では追わない |
| `--brief` の利用頻度（経路14 / 17 がどれだけ踏まれるか）が未測定 | **追わない。** discussion §3.4 に「頻度は測っていない」と明記した。#291 が母集団を定義するときに必要なら、そこで測る |
| spool 型を採る場合に、元書面と凍結コピーのどちらを `unmet-gate-check` の baseline にするか未規定 | **#291 の設計材料**として discussion §7.2 に「#289 v0 が塞いだ穴を開け直す経路」として明記した。本単位では決めない |

### 揮発する証跡の扱い（evidence anchor）

**`.oe/`（前単位の作業層）と `tmp/`（SO 出力）はこの worktree に存在せず、マージ後も残らない。** 実装SO の2レーンが独立に「再検証できない」と指摘した。

- 転記済み: discussion §6（P8 の5形式・P5 の件数）・§8.1（タイムアウトの実測値）・§4（ULID の測定コマンドと値）。**数値と `file:line` は本文に写してある。**
- **転記が唯一の根拠であり、監査可能性は失われている。** これは「作業層は消える前提」という昇格の設計上の帰結であり、隠さずに discussion §0 と §11 に限界として書いた。
- 一次コードへの引用（`file:line` 13箇所）は committed なので**再検証できる**。揮発するのは測定値の側だけである。

### Step 3: 出力型 × 消費チャネル

- **事実・失敗**（**Step 4 の指摘を受けて、実行ログにある失敗を型ごとに項目として立て直した**。当初は3項目に畳んでおり、pointer では辿れても closure 層で見えない型があった）:
  - 効果の言い切りを rev.1 で1件・rev.2 で3件犯し、**自己検出は0件**だった（`本文: Y4. 実装SO（gate 4）`）。
  - 実装SO の13指摘の内訳は、事実誤認5件（ULID の数値・経路数の算術・`#289` の引用行・`oe-select` の引用行・§10 の収束の主張）／撤回済み主張の復活2件（§7 理由1・§4末尾）／成立条件の欠落5件（§3.1 の見出し・§3.4 の頻度・§7 理由2 の未観測相関・§7.2 の否定範囲・§6.4）／範囲外の一般化1件（新 item の `prediction`）である（`本文: Y4. 実装SO（gate 4）`）。
  - **最も重い誤りは §10 で「三つの arc が同じ場所へ収束した」と書いたこと**で、#290 の到達点は義務コンパイルではなかった（`本文: Y4. 実装SO（gate 4）`）。
  - **`#289` の引用行がずれた原因は自分の編集である**（frontmatter に3行足したことで `:137`→`:140` / `:147`→`:150` へ動いた・`本文: Y4. 実装SO（gate 4）`）。
  - Y4 の修正が他の箇所へ伝播せず、Copilot に3件の取りこぼしを指摘された（`本文: Y5. PR と Copilot`）。
  - ULID の初回報告が範囲も単位も揃わない**不正な比較**だった（`本文: Y2. discussion の起草`）。
  - Y3 で自力の列挙が閉じず、最後の搬送路1件は外部の指摘で加わった。述語の「網羅した」という主張も撤回した。転記時に成立条件を1件落とした（`本文: Y3. negative knowledge の判定`）。
  - closure でも受け入れ基準の件数を数えずに「9件」と書きかけ、実数は11件だった（本節の上の照合表）。
  - `gh pr edit --add-reviewer @copilot` が exit 0 を返しながら登録されなかった（`本文: Y5. PR と Copilot`）。
  - **前単位の到達点**（gate 1・gate 2 とも `refuted 3/3`・`halted-unconverged` で停止）は本単位の失敗ではないが、本単位の前提なので `本文: Y1. worktree と episode 枠（2026-08-01）` に記録してある。
- **決定と根拠**: 昇格対象の選別で、案H の穴の一覧は落としつつ「なぜ構造的に不能か」は残した（`本文: Y2. discussion の起草`）。negative knowledge の候補2件のうち1件を採用し1件を棄却し、採用分も統括の候補をそのまま採らず2つの教訓に分割した（`本文: Y3. negative knowledge の判定`）。
- **わかったこと**: `oe-kick '#N'` の issue モードは `--brief` を乗せず、親が書いた issue 本文が `gh` 経由で子に届く（`本文: Y2. discussion の起草` 経由で discussion §3.3 が正本）。ULID は knowledge item 専用トークンではない。SO の空返却には少なくとも3原因がある（`本文: Y4. 実装SO（gate 4）`）。
- **原則（Pattern / Anti-pattern）**: 「未返却のレーンを『返らないレーン』と同一視しない」を新 item として収穫した（`本文: Y3. negative knowledge の判定`）。**`本文なし: 収穫基準の当て込みは Y3 で行ったが、そこで採らなかった型を1つここで名指しする` — 「数値を直したら、その数値が使われている他の箇所を grep で洗う」は今回3件の取りこぼしを生んだが、既存 item `01KYMRE1NC7XX6N66RQ0MGGHF1`（上流の断定を書き写すな）の観測として記録済みであり、独立の item にするほど新規性が無いと判断した。**
- **行動変更**: 判定を作る前に SO runner の完了を待つ（トリガ = 弱 SO で partial を disclose しようとするとき / 機構 = `oe-refute` の同期完了、または生 `so-compare` ならプロセス終了待ち / 着地先 = 新 item `01KYYHGNRJ926N4CTTS4PE8GQH`）。**本単位で実際に実行した**（`本文: Y4. 実装SO（gate 4）`）。
- **蒸留シグナル**: knowledge store へ1件収穫済み（`01KYYHGNRJ926N4CTTS4PE8GQH`）。**Decision / skill / rule への昇格は「なし」** — owner の gate 3 判断が「規範を積まない・canonical は1行も変更しない」なので、規範側への昇格は意図的に行わない。
- **残課題**: 上の routing 表。

### Step 4: heavy tier の外部チェック — 実施した（両レーン `refuted`）

`so-compare --with codex,cursor` で closure の focused check を実施した（確認対象は失敗の選択的省略 / routing の網羅 / evidence anchor / back-propagation の4点に限定）。出力: `tmp/so-293-step4/`（codex 1,988 バイト / cursor 1,716 バイト・167秒）。

**claude レーンは起動しなかった。** Y4 でアカウントの利用上限に当たっているため、同じセッション内での再起動は空返却になると判断した。**これは brief の「claude レーンを720秒以上で起動せよ」への違反ではない** — その指示は gate 4 の実装SO（Y4）に対するもので、Y4 では `SO_TIMEOUT=900` で起動している。本 Step 4 は `episode-retrospective` が heavy tier に課す別のチェックである。**他族2レーンが実返却しており「0レーンでない」は満たす。**

| 観点 | codex | cursor |
|---|---|---|
| 失敗の選択的省略 | **指摘あり**（closure が3項目に畳んでおり、13指摘の内訳・Y3 の自己失敗・前単位の停止が項目として見えない） | 問題なし（pointer で辿れる） |
| routing の網羅 | **指摘あり**（5件） | **指摘あり**（3件） |
| evidence anchor | 問題なし | 問題なし |
| back-propagation | **指摘あり**（`related.reason` が撤回済み主張を運んでいる・§8.3 の読み替えが #290 にしか張られていない） | **指摘あり**（`canonical/skills/so-compare/SKILL.md` の記述との衝突が未処理） |

**2レーンの判定が割れた観点が1つある**（選択的省略）。cursor は read/write 契約の pointer を根拠に問題なしとし、codex は closure 層で型が見えないことを問題とした。**厳しい側を採り、失敗を型ごとに項目へ立て直した。**

**対応した指摘**: 失敗の項目化（上記）／routing に6行追加（`--add-dir` 経由の別 doc・過去レーンの内容・形式 (f) の運用誤検知率・`--brief` の利用頻度・spool の baseline 未規定）／back-propagation に3件追加（`so-compare` skill の衝突を #295 へ申し送り・§8.3 を #290 にだけ張った理由の明示・`related.reason` の撤回済み表現の修正）。

**`related.reason` の指摘が最も重い。** §10 で「三つの収束」を撤回したのに、#289 と #290 へ足した参照の理由文はその表現のままだった。**撤回した主張が本文ではなく参照のメタデータに残る**という形は、この arc で初めて観測した。

### Y6 報告の内容（親へ送る1行の裏付け）

**採用 negative knowledge の効果**（本単位で注入された item は無い。前単位で注入された4件への観測は `本文: Y3. negative knowledge の判定`）:

| item | 帰結 |
|---|---|
| `01KYW0BPJNHP94TNX0VG4G11TX`（名指しした型を書き手に回って免れると仮定するな） | **効いた（予測が的中する形で）。** `externally_verified` で書き戻した |
| `01KYSKQHDT86GZW87E8EDMSE46`（遮断を1層で決めても下流の口が残る） | **効いた。** 搬送路17経路の列挙に直結。ただし最後の1件は外部の目で閉じた |
| `01KYJ76D830XME16ZFXC2XRPZZ`（述語は母集団の側から定義せよ） | **効いた。** ただし「網羅した」の主張は撤回した |
| `01KYMRE1NC7XX6N66RQ0MGGHF1`（上流の断定を書き写すな） | **効いた。** 統括の ULID 計数をそのまま採らず、範囲差を明記した |
| `01KYYHGNRJ926N4CTTS4PE8GQH`（新規・遅れて返るレーンは判定の前提を崩しうる） | **本単位で実践した。** Y4 で runner の完了を待ってから判定を作った |

**終端定義と子側ガードの試用結果**: 終端 Y6 を守り、マージ・worktree 掃除・issue close はしていない。**子側ガードは1回発動した** — brief の受け入れ基準7「三つの収束が読める」が実測（#290 の到達点は義務コンパイルではない）と食い違ったため、基準の字面ではなく狙いを満たす形に書き換え、**その判断を closure に明示して owner の差し戻しを求めた**（上記の照合表）。基準を黙って弱めていない。

**claude レーンが720秒で返ったか**: **返らなかった。** Y4 で `SO_TIMEOUT=900`（要求の720秒以上）で起動したが、1回目は958秒で `timeout_empty`、リトライ（1350秒）はアカウントのセッション上限に当たり61バイトの `You've hit your session limit` を返した。**720秒以上にすれば返る、という前提は本単位では成立しなかった。**

### back-propagation（他文書で見つけた欠陥の反映）

- **#290 の discussion は claude レーンの空返却を「この環境の既知症状」と記録している。** 本単位の実測でこれは不正確だと分かった（原因は3種類あり、480秒では時間不足だっただけの回も、900秒でも返らない回もある）。**#290 の本文は変更せず**（規範を積まない・他 arc の記録を書き換えない）、`related` に本 doc §8 への参照を足して**読み替えの導線を張った**。
- **`canonical/skills/so-compare/SKILL.md:205` は「大きなプロンプトでタイムアウトする場合は値を増やす」と書いており、本単位の実測（900秒でも `timeout_empty`・リトライは利用上限）と衝突する。** **owner の gate 3 判断で canonical は1行も変更しないため、修正しない。** 現象への対処は **#295**（owner が指定した単位）であり、**この skill 記述の是正も #295 の射程に含めて申し送る。** 本単位は discussion §8.2 に「タイムアウトを上げれば返る、も誤りである」と実測つきで残すところまでを担う。
- **§8.3 は #284 / #289 / #290 の設計SO を「事実上2レーン」と読み替える必要があると書いた。読み替えの導線を張ったのは #290 だけである。** 理由を明示する: **#290 だけが「この環境の既知症状」という誤った前提を doc 本文に記録している**ので、そこには前方参照が要る。**#284 と #289 はその前提を記録しておらず、欠陥があるのは doc ではなく「当時のレーン数の読み方」の側**なので、個別の doc へ pointer を足すのではなく discussion §8.3 が横断的な読み替えとして持つ。**#284 / #289 の doc は変更しない（追わない）。**
- **#289 の discussion には欠陥を見つけていない。** `related` の追加のみ。
- **Step 4 の指摘で自分の前方参照が撤回済み主張を運んでいると分かった。** #289 と #290 に足した `related.reason` が「三つの arc の収束」と書いており、§10 で撤回した表現のままだった。**両方の `reason` を「否定側で一致・肯定側は割れている」に直した。** 撤回した主張が、本文ではなく**参照の理由文**に残るという形で生き延びていた。
