---
id: "01KZGJSJNN04YFT3JDYEBMDX2D"
title: "#309 フックの発火記録を残す — 記録の失敗をブロックにも欠測隠しにもしない設計"
date: 2026-08-08
type: episode
status: stable
related:
  - type: parent_issue
    ref: "https://github.com/stlwolf/ai-development-hub/issues/309"
    reason: "本単位の起点。止める側のフック3本に発火記録が無い"
  - type: parent_epic
    ref: "https://github.com/stlwolf/ai-development-hub/issues/37"
    reason: "制御ループ5要素のうち Sensor が空であるという中核課題。本単位はその最小増分"
tags: [hooks, sensor, observability, engine]
promotion:
  - subject: "記録の失敗を診断ファイルという第2の経路へ逃がし、判定経路に触れさせない"
    verdict: required
    ref: "本文: Step 1 — 触る対象の実物を読んだ（一次確認）"
  - subject: "テスト文脈は set -e を抑止するが set -u は抑止せず、隔離が要る"
    verdict: required
    ref: "本文: Step 2 — 中心の仮定を実測して、半分が崩れた"
  - subject: "止める側のフックの陽性対照を、payload をファイルの中で組み立てて撃つ"
    verdict: required
    ref: "本文: Step 4 — 陽性対照に使える具体的なコマンドを確定した"
  - subject: "errexit の抑止文脈はサブシェルへ伝播するがプロセス境界を越えない"
    verdict: required
    ref: "本文: Step 5 — gate 1（ゼロベース探索 SO）と、その指摘の検算"
  - subject: "2レーンが独立に一致した読解を、実測せずに採らない"
    verdict: required
    ref: "本文: Step 6 — claude レーンが設計を作り替えた（gate 1 の本体）"
  - subject: "性能を根拠に形を選ぶとき、測定した構成と主張する構成を一致させる"
    verdict: required
    ref: "本文: Step 7 — gate 2（設計SO）で、自分の測定の当て方が誤っていたと分かった"
  - subject: "実測から引く結論を、実際に測った事柄の範囲に留める"
    verdict: required
    ref: "本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった"
  - subject: "trap と tally を、担当する故障で対にして表に起こす"
    verdict: required
    ref: "本文: Step 9 — gate 3（owner 判断）で trap を採り、plan を改訂した"
  - subject: "#310 の1行の欠陥を、過剰ブロックと静かな素通しの両方向で塞ぐ"
    verdict: required
    ref: "本文: Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ"
  - subject: "検査が赤いとき、実装より先に検査台本の側を疑う"
    verdict: not-required
    ref: "本文: Step 11 — 実装（owner が gate 3 で承認・コミット順が要件そのもの）"
  - subject: "FIFO ガードを追記先3経路すべてへ通す"
    verdict: not-required
    ref: "本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた"
  - subject: "コミットメッセージを -m ではなくファイルから渡す"
    verdict: not-required
    ref: "本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた"
  - subject: "観測器を在るものの列挙から、期待する集合を先に置いて欠を見る形へ変える"
    verdict: required
    ref: "本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた"
  - subject: "ツール判別を環境変数ではなく $0 で行う"
    verdict: required
    ref: "本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった"
  - subject: "#310 の修正・trap・tally のコミット順を要件として固定する"
    verdict: required
    ref: "本文: Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ"
  - subject: "三重化した共通区間の drift を規律ではなく byte 一致のテストで閉じる"
    verdict: not-required
    ref: "本文: Step 11 — 実装（owner が gate 3 で承認・コミット順が要件そのもの）"
  - subject: "配備2段目（dispatcher 層）の確認を、マージ後の owner 観測に委ねる"
    verdict: not-required
    ref: "本文: 実機確認の限界（正直に書く）"
  - subject: "注入された negative knowledge への観測書き戻しを本単位で行わない"
    verdict: unknown
    ref: "本文なし: 8月8日の brief が残っておらず、slot に載っていた item の集合を復元できない"
  - subject: "設計内容を decision へ昇格させず、plan と episode に留める"
    verdict: not-required
    ref: "本文なし: decision を作らない判断は本文に明示がなく、closure で独立に問うて判定した"
---

# #309 フックの発火記録を残す

## なぜこの作業が始まったか

止める側のフック3本（`block-destructive.sh` / `block-force-push.sh` / `cc-lint.sh`）は、発火したことを1行も記録しない。そのため「強制点が本当に発火したか」を当事者以外が確かめる手段が無い。#305 のアンカー設計が3ラウンド続けて反証された原因は、遡ると全部この1点に帰着した。閉ループ制御の5要素でいうと、フックは Actuator を担えているのに Sensor を担っていない。

本単位は plan-first である。計画を作って STOP し、plan doc のパスを親へ報告するまでが終端で、実装はそこから親が位置を指定する。

## この単位の設計上の中心 — 正面から衝突する2つの要求

1. 記録の失敗で操作をブロックしてはいけない。3本とも先頭に `set -euo pipefail` があり、Cursor は3本すべてに `failClosed: true` を付けている。記録の1行が失敗してスクリプトが非ゼロで落ちると、普通のシェルコマンドがブロックされうる。3本は3ツール全部に配られているので、影響範囲は日常の作業全体である。
2. しかし失敗を握り潰すと、欠測が見えなくなる。`|| true` や `2>/dev/null` で潰すと、「記録できなかった（環境エラー）」と「記録すべきイベントが無かった」が区別できない。それはこの issue が解こうとしている問題そのものの再生産である。

## Step 1 — 触る対象の実物を読んだ（一次確認）

読んだもの: `canonical/hooks/scripts/block-destructive.sh` / `block-force-push.sh` / `cc-lint.sh` の全文、`cursor.hooks.json` / `codex.hooks.json` / `claude.hooks.json`、`canonical/hooks/README.md`、`canonical/hooks/scripts/oe-prompt-receipt.sh` の全文。

確定した事実を、後段の設計が乗る土台として列挙する。

- **3本とも、終了経路は `allow()` と `deny()` の2つの関数に集約されている。** どちらも末尾で `exit` する。`block-destructive.sh` と `block-force-push.sh` は `main` の最後が `allow`、`cc-lint.sh` は `main` の最後が `deny` で、途中の分岐もすべてこの2関数のいずれかを呼んでから抜ける。つまり**記録を差し込む一点は `allow()` / `deny()` の中である**。判定ロジック本体には触らなくてよい。
- **ただし「すべての終了が allow/deny を通る」は `set -e` が発動しない限りの話である。** 3本とも `set -euo pipefail` なので、途中のコマンドが失敗すれば allow/deny を通らずに非ゼロで落ちる。この経路は既存の挙動であって本単位が作るものではないが、記録を足すときに**新しくこの経路を増やしてはいけない**という制約になる。
- **stdout は制御プロトコルの通信路である。** `allow()` は `CURSOR_PROJECT_DIR` か `CLAUDE_PROJECT_DIR` が立っているとき `{"permission":"allow"}` を stdout に出す。記録は絶対に stdout へ出せない。
- **Cursor の `failClosed: true` は3本すべてに付いている**（`cursor.hooks.json` の実物で確認）。README の「fail-open リスク」節にも「Cursor: `failClosed: true` を設定済み。フック失敗時もブロックする」と明記されている。Claude Code / Codex には fail-close 機構が無く、exit code 0/2 以外は non-blocking である。**つまり同じ1本のスクリプトが、ツールによって失敗時の意味が正反対になる。**
- **`oe-prompt-receipt.sh` が、ほぼ同じ問題を既に解いている。** 追記は best-effort で `exit 0` 固定、環境エラーは `oe-receipt-diag.jsonl` という**別の診断ファイル**と stderr に落とす。ヘッダコメントに「失敗を握り潰さない: 環境エラーとデータ不在を別に扱う」と明記されている。これは本単位が探している両立の実例である。ただし同スクリプトは `set -uo pipefail`（`-e` 無し）の advisory hook であり、fail-closed な3本とは前提が違う。

昇格の印: 記録の失敗を第2の経路（診断ファイル）へ逃がして判定経路に触れさせない型は、#309 固有ではなく fail-closed なフック一般に効く設計かもしれない

## Step 2 — 中心の仮定を実測して、半分が崩れた

gate 1 の探索へ入る前に、設計全体が乗っている bash の仮定を実機で測った。測ったのは「記録処理の失敗が、`set -euo pipefail` の呼び出し元を殺さないようにできるか」である。台本は `tmp/probe-errexit.sh` と `tmp/probe-errexit2.sh`。

当初の仮定は「記録関数をテスト文脈（`record || :`）で呼べば、`set -e` は関数本体の中では抑止されるので安全」だった。**この仮定は半分しか正しくなかった。**

- 抑止された（`record || :` で呼び出し元は生きる）: 関数内の `false`、深くネストした失敗、書けないパスへの追記、`command not found`、`pipefail` + 早期終了 consumer。
- **抑止されなかった: `set -u` 違反（未割り当て変数の参照）。呼び出し元ごと rc=1 で死ぬ。** これが致命的なのは、Cursor が `failClosed: true` を付けているため、**記録処理の中でうっかり `${TMUX_PANE}` のような変数を既定値なしで参照しただけで、普通のシェルコマンドがブロックされる**からである。しかも変数参照は記録処理でいちばん書きたくなる部分である。

さらに追試したところ、**サブシェルへ隔離する形（`( record ) || :`）は `set -u` 違反も `exit N` も封じ込める**ことが分かった。自分自身へのシグナル（`kill -TERM $$`）だけは貫通するが、これは記録経路の現実的な失敗ではない。

macOS の system bash 3.2.57 と Homebrew の bash 5.2.37 の両方で同じ結果になることも確かめた。フックが最小 PATH で起動されて `/bin/bash` に解決される場合を考えると、ここは確かめておく必要があった。

この実測の結果、DJ-1 の書き方は「テスト文脈で呼ぶ」から「**サブシェルへ隔離して呼ぶ**」へ変わった。規律（全変数に `:-` を付ける）でも回避できるが、規律は破られたことを検出できないのに対し、サブシェル隔離は破られても封じ込む。fail-closed な面を触っている以上、規律ではなく構造で止める側に倒す。

昇格の印: 「テスト文脈は set -e を抑止するが set -u は抑止しない」は bash 一般の非自明な落とし穴で、fail-closed なフックを触る作業すべてに効く negative knowledge の候補

## Step 3 — 記録量を推測でなく実測で見積もった

DJ-3（allow を毎回1行残すか）とローテーション方針は、記録量の桁が分からないと決められない。桁を推測で置くとローテーションの要否そのものが恣意的になるので、実測した。

Claude Code の transcript から Bash tool の呼び出しを日別に数えた（直近14日・transcript 200 件を上限にサンプリング）。

- 1日あたり約 200〜670 回。中央値はおおよそ 450 回。
- これは **下限**である。サンプリング上限で切っているうえ、Cursor と Codex の分を含んでいない。3本のフックは3ツール全部へ配られているので、実際の母数はこれより大きい。

フック3本がそれぞれ全 Bash 呼び出しで発火するので、毎イベント1行なら **1日あたり約1500行**になる。1行 180 バイト程度として **1日あたり約 270KB** である。

比較の基準として `~/.claude/state/oe-events.jsonl` を見ると、983 行 / 328KB を数か月かけて溜めた量である。owner はこの 317KB を「読み出しが互いのノイズを踏む」理由に挙げて別ファイル化を指示している。**毎イベント1行の allow 記録は、その台帳とほぼ同じ量を1日で作る。**

この数字は DJ-3 の判断を変える。allow を残す目的は owner が挙げたとおり陽性対照（一定期間0件＝フックが動いていない、と読めるようにする）であって、毎回の forensics ではない。**目的が生存確認なら、必要なのは毎回の1行ではなく「その日そのフックが動いた」という印1つで足りる。** 探索木 DJ-3 の案C2（deny は1行 JSONL・allow は日単位の集計）を本命へ上げる根拠がここで数値として立った。

## Step 4 — 陽性対照に使える具体的なコマンドを確定した

「記録0件」を「発火しなかった」と読むには、経路が生きていることを別に示す対照が要る（NK item 01KYMRE1N7N0J8VP3CBZZEZ8Q3）。対照は **発火するが、万一フックが死んでいて実行されても無害** でなければならない。ここが難しいのは、止める側のフックの対象がまさに破壊的コマンドだからである。`rm -rf /` を対照に使うわけにはいかない。

実スクリプトへ payload を通して確かめた（台本 `tmp/probe-hooks.sh`）。3本すべてに無害な対照が在る。

| フック | 対照コマンド | 結果 | フックが死んでいた場合 |
|---|---|---|---|
| `block-destructive.sh` | `echo "<SQL の DROP TABLE 文> sentinel309"` | deny (rc=2) | `echo` が文字列を表示するだけ |
| `block-force-push.sh` | `git push --force no-such-remote-309` | deny (rc=2) | 存在しない remote で git がエラー終了する |
| `cc-lint.sh` | `git commit -m "sentinel309 bad"` | deny (rc=2) | 使い捨て repo で実行すること（staged 変更があると本当にコミットしうる） |

`block-destructive.sh` が使えるのは、SQL の判定だけが行頭固定でなく**文字列の途中で一致する**ためである。`mkfs` は `^mkfs`、`dd` は `^dd` なので無害な対照を作れない。SQL 判定のこの性質が、たまたま安全な対照を1つ残していた。

この検査を組む過程で実地の証拠が1つ増えた。**最初に対照コマンドを普通に shell から流したら、生きている `block-destructive.sh` がその検査コマンド自体をブロックした。** 検査の payload を外側のコマンド行に置いた結果、フックが外側の行を読んで発火したためである。避けるには payload をファイルの中で組み立てる必要がある。これは実装フェーズのテスト設計に直接効くので記録しておく。

昇格の印: 止める側のフックを検査するとき、検査コマンド自身がフックに引っかかる。この「検査器が被検査対象に食われる」構造は他の強制機構の検査でも起きうる

## Step 5 — gate 1（ゼロベース探索 SO）と、その指摘の検算

探索木を `tmp/dj-309-exploration-tree.md` に外部化し、選択肢拡張テンプレを付けて3レーンへ投げた。プロンプトは `tmp/so-309-gate1-prompt.md`。

### レーンの実返却状況（正直に書く）

| レーン | モデル | 結果 |
|---|---|---|
| cursor（1回目・`auto`） | `Auto Balance` に解決 | `success_empty`。11秒で「Auto routed to Cursor Grok 4.5」の1行だけを返し、本文なし。実返却なしと扱う |
| cursor（2回目・明示指定） | `cursor-grok-4.5-high` | 実返却あり（13.7KB）。出力 `tmp/so-309-gate1-cursor/` |
| codex | `gpt-5.6-sol`（`model_resolved_source=config` なので観測値ではない） | `timeout_empty`（480秒）。最終回答を stdout へ出さないまま打ち切り |
| claude | `opus` / effort=high | 本節執筆時点で実行中 |

`auto` のまま投げた cursor レーンが空で返ったので、モデルを明示して投げ直した。so-compare スキルが警告しているとおり、`auto` は実行ごとに解決先が変わるので、レーンの多様性を主張するなら明示指定のほうが確実だった。

codex レーンは最終回答こそ出さなかったが、stderr に残った作業痕を見ると**私と同じ bash の実測を独自に行い、同じ結論に達していた**（`nounset_direct` で親が死に、`nounset_subshell` で親が生きる）。実返却としては数えないが、Step 2 の実測を独立に裏付ける材料にはなっている。

### cursor レーンの指摘3点を、鵜呑みにせず自分で検算した

台本は `tmp/probe-so-claims.sh` と `tmp/probe-bypass.sh`。

**指摘1（採用・設計が変わった）**: 「`( record ) || :` でもサブシェル内の `set -e` は効かない」。検算して**そのとおりだった**。`( echo STEP1; false; echo STEP2 ) || :` は STEP1 と STEP2 の両方を出す。`||` の左辺という文脈が、サブシェルの内側まで errexit を抑止する。

これは Step 2 で私が出した結論の穴だった。サブシェル隔離は「親を殺さない」ことは保証するが、「記録処理が途中で失敗したらそこで止まる」ことは保証しない。失敗したまま後続が走り、壊れた状態で記録を書きうる。

そこで**別プロセス（独立したスクリプト）ならどうか**を追加で測った。結果は `( bash recorder.sh ) || :` で STEP1 だけが出て STEP2 は出ない。**プロセス境界を越えると errexit の抑止文脈は伝播せず、レコーダーは自分の `set -e` を保てる。** つまり別プロセスのレコーダーは、親を殺さないことと、自分の中で失敗したら止まることを同時に満たす。同一シェル内の関数では両立しない。

この検算の結果、DJ-4 の本命が「各スクリプトに内蔵する関数」から「**独立したレコーダースクリプトを子プロセスとして起動する**」へ変わった。これが gate 1 で出てきた、初期案セットの外にある案である。

**指摘2（採用）**: 「サブシェルでも stdout は親へ貫通する」。検算して そのとおり。記録側が1行でも stdout へ出すと `{"permission":"allow"}` に混ざる。呼び出し側で `>/dev/null` を付ける二重の防御を入れる。

**指摘3（棄却・SO が誤り）**: 「`cc-lint.sh` は `&&` を含む非 commit コマンドで allow/deny に達する前に落ちる」。実スクリプトへ実際に通して検算したところ、`cd /tmp && ls` / `echo a && echo b` / `make build && make test` はすべて rc=0 の allow だった。**この指摘は成立しない。**

ただし指摘3が指していた「allow/deny を通らずに落ちる経路が在る」という論点自体は正しい。別の入口で実証できた。**`jq` が PATH に無いと3本とも rc=127 で落ち、allow/deny のどちらも通らない。** これは案B1（allow/deny の中に記録を置く）が原理的に記録できない経路である。Cursor では failClosed なのでコマンドはブロックされるが、記録は1行も残らない。

**指摘4（採用・DJ-7 が変わった）**: 「allow の定常流を陽性対照にするのは不十分」。理由は2つで、どちらも私が見落としていた。(a) allow が0件なのは「フックが死んでいる」だけでなく「エージェントが Bash を使っていない（idle）」でも起きる。(b) issue 本文が既に書いているとおり、Codex は trust されていない hook を警告ゼロで黙って skip する。どちらの場合も allow は0件になり、区別できない。

owner が gate 0 で確定した「止めたときも通したときも記録する」という制約は動かさない。動かすのは**その記録に何を言わせるか**である。allow の流量は受動的な傍証にとどめ、陽性対照の主柱は**意図的に撃つ既知のプローブ**（Step 4 で無害なものを3本分確定済み）に置く。プローブなら idle と死を区別できる。

昇格の印: 「errexit の抑止文脈はサブシェルには伝播するがプロセス境界は越えない」という性質は、fail-closed な機構に副作用のある処理を足す一般的な型として使える

## Step 6 — claude レーンが設計を作り替えた（gate 1 の本体）

claude レーン（`opus` / effort=high・725秒・32.9KB）が、cursor レーンとは別の3点で初期案を崩した。gate 1 を回した価値はここに出た。

### 指摘① 要求Aには鏡があり、そちらが危険である（採用・制約に追加）

owner が確定した制約は「記録の失敗で操作をブロックしない」だけを言っている。**鏡側、つまり「記録の失敗でブロックが解除されない」が書かれていない。**

`deny()` は `exit 2` に到達して初めてブロックになる。到達前に死ぬと exit code は 1 や 127 になり、Claude Code と Codex では 0/2 以外が non-blocking なので、**止めるべきコマンドが通る**。要求Aの違反（余計に止まる）は騒がしくてすぐ気づくが、鏡側の違反（止まらない）は静かで、しかも取り返しがつかない。

私は要求Aの側だけを見て設計していた。制約に1行足す。

### 指摘② 二層構造には中抜けがある（採用・設計が変わった）

案A3 の層1は「個別イベントの記録失敗」、層2は「フックが動いていない」を担う。この2つの間に第3の故障が挟まっている。**呼ばれたが、判定を出す前に死んだ場合**である。この経路では allow/deny を通らないので記録が増えない。しかし他のコマンドでは記録が増えるので層2の「一定期間0件」も鳴らない。沈黙のまま強制点が抜ける。

私は探索木で案B1 の弱点として「この経路は記録されないまま残る」と正直に書いていたのに、**それが陽性対照の穴でもあることを接続できていなかった**。そして解決を DJ-2 の中（ラッパーを入れるか）だけで探していた。

claude レーンの解は、ラッパーを持ち出さずに**入口にも記録を置く**ことだった。`inv − (allow + deny)` が「呼ばれたのに判定を出さずに死んだ回数」になる。1本のログの有無を眺めるのではなく、独立に取った2点の差が機械的に欠測を指す。Step 1 で実証した `jq` 不在の rc=127 経路も、これで初めて数として現れる。

複式記入という発想が初期選択肢に無かったのは、DJ-2 が「記録を差し込む位置」を `allow()` / `deny()` の**中**に限定した時点で、入口という地点が選択肢から消えていたからである。

### 指摘③ 記録の表現を1バイトまで落とす（採用・DJ-3 の対立が消えた）

私は DJ-3 を「毎回1行の JSONL か、日単位の集計か」で対立させていた。claude レーンは**1イベント1バイトの追記**を出した。ファイルサイズが件数、mtime が最終発火時刻になる。

実測で確かめた（`tmp/probe-a1.sh`）。並行60プロセス × 50回の追記で 3000/3000 バイト、ロストなし。所要は 0.102 ms で、子プロセス起動（8.6 ms）の約85分の1。100万コマンドで 1MB なので**ローテーションが不要**になる。

これで DJ-3 の対立そのものが消えた。日単位集計に必要だった `date` の exec も日境界という概念も要らない。私が Step 3 で量を実測して案C2 へ寄せた判断は方向としては正しかったが、**表現を落とすという軸を探索していなかった**。

なお私が Step 5 で採った「独立したレコーダースクリプトを子プロセスで起動する」案は、これで不要になった。プロセス境界は errexit の抑止文脈を遮断するという利点があったが、tally はサブシェル隔離だけで足り、fork のコスト（3本 × 全 Bash 呼び出しで1コマンドあたり約25 ms）を払う理由が無くなった。**gate 1 の中で自分の案が二度差し替わったことになる。**

### 指摘④ DJ-4 の却下根拠が崩れた（未決へ差し戻し）

私は共通ライブラリ案（D1）を「`source` の失敗が新しい fail-closed 経路になる」という理由で落としていた。claude レーンは「隔離サブシェルの中で source すればその失敗も封じ込まれるはずで、しかもあなたはそれを測っていない」と指摘した。

測った。壊れた lib も存在しない lib も、隔離の中なら呼び出し元は生き残る。**却下の根拠が消えたので、DJ-4 は未決へ戻す。** 12行程度の三重化は、後で鏡側（指摘①）の修正が必要になったときに3か所のうち1か所を直し忘れる形の事故を作る。

### 指摘⑤⑥ 検算して扱いを変えたもの

- **⑤「隔離サブシェルの中では最初の失敗で abort し、診断行に到達しない」→ 反証。** 実測では関数形・直書き形のどちらでも診断行に到達した。`||` の左辺という文脈が errexit を抑止するためである。ただし呼び出し文脈が変われば話が変わるので、`set +e +u` を中に明示するのは保険として残す。**確認済みの中断の修正ではなく防御である**と書き分ける。
- **⑥「陽性対照が配備物を通っていない」→ 採用。** 私の `tmp/probe-hooks.sh` は repo のコピーを直接叩いていた。実際に走るのは `~/.claude/hooks/` の symlink 経由である。repo コピーを叩く検査は dangling symlink・sync 漏れ・`settings.json` からの脱落という、いちばん怖い故障モードを検出できない。この repo は過去にまさにその形（worktree から sync してリンクが張り替わり、削除で dangling になってフックが無言で止まる）を踏んでいる。**対照は配備先のパスを叩く形に変える。**

### 両レーンが同じ誤りをした件

cursor と claude が**独立に**「`cc-lint.sh` は `&&` を含む非 commit コマンドで落ちる」と読んだ。2レーン一致は強い signal だったが、実スクリプトへ通すと rc=0 の allow だった。

原因まで特定した。`extract_commit_segment` は `commit_segment="$(extract_commit_segment "$cmd")"` とコマンド置換の中で呼ばれており、**`inherit_errexit` が既定 off なのでコマンド置換の中へ errexit が伝播しない**。`shopt -s inherit_errexit` を付けると実際に rc=1 になることまで確かめた（`tmp/probe-inherit.sh`）。

学びは2つある。**2レーンの一致は正しさの証拠にならない**（同じ読解の癖を共有していれば同じ穴に落ちる）。そして**このガードは既定に依存して偶然動いている** — 誰かが `inherit_errexit` を有効にした瞬間、Cursor では `&&` を含むコマンドが全部ブロックされる。本単位のスコープ外なので実装せず surface する。

昇格の印: 複数の SO レーンが独立に同じ結論へ達しても、それは合意であって検証ではない。一致を根拠に実測を省く判断は誤りうる

## Step 7 — gate 2（設計SO）で、自分の測定の当て方が誤っていたと分かった

改訂設計を `tmp/design-309-revised.md` にまとめ、gate 2 として3レーンへ投げた（プロンプト `tmp/so-309-gate2-prompt.md`）。cursor レーン（`cursor-grok-4.5-high`）が最初に返した指摘が重かった。

### 指摘1（採用・私の測定誤り）: 「fork なし・0.102 ms」は測る対象を間違えていた

私は設計書に「tally は fork も exec も起きない。0.102 ms」と書いた。しかし**測ったのは素の `printf 'x' >> file` であって、設計書自身が書いた `hfr` の形ではない**。設計の `hfr` は `( set +e +u; mkdir -p ...; printf ... ) >/dev/null || :` であり、サブシェルは fork するし `mkdir` は外部コマンドの exec である。

測り直した（`tmp/probe-hfr-cost.sh`）。

| 形 | 実測 | 1 Bash コマンドあたりの追加（6回分） |
|---|---|---|
| 素の `printf` 追記（当初測った形） | 0.176 ms | 1.1 ms |
| **設計書が書いた形**（サブシェル + 毎回 `mkdir -p`） | **2.189 ms** | **13.1 ms** |
| サブシェルのみ（`mkdir` を外す） | 0.567 ms | 3.4 ms |
| 独立レコーダーを子プロセスで起動 | 7.450 ms | 44.7 ms |

設計書の主張は実測の12倍ずれていた。**自分に都合のよい数字を、別の構成から持ってきていた。** 指摘されるまで気づかなかった。

### そこから出た形（採用）

速い経路を builtin だけにして、失敗したときだけ隔離へ落とす。

```bash
printf 'x' >> "$T_INV" 2>/dev/null || { ( set +e +u; mkdir -p ...; printf ... ; 診断 ) >/dev/null 2>&1 || :; }
```

実測 0.186 ms で、素の追記とほぼ同じ。1 Bash コマンドあたり 1.1 ms（現行のフック3本ぶん約35 ms に対して +3%）。書けない状態でも呼び出し元が生き残ることも確認した。

この形は `set -u` の心配も要らない。参照する変数はスクリプト先頭で `${...:-}` 付きで代入済みのものだけで、hot path は単一の builtin だからである。**Step 5 で採った「独立レコーダーを子プロセスで起動する」案は、これで最終的に不要になった。** プロセス隔離が要るのは複数段の処理を保護したいときで、hot path が1操作しかないなら守るべき途中が存在しない。複雑な処理（`mkdir`・診断）は稀な fallback 側に寄せ、そちらは隔離する。

なお fast path の `2>/dev/null` は握り潰しではない。失敗は直後の `||` が捕まえて fallback へ渡し、fallback が診断を残す。抑えているのはシェルが出す重複のエラー文言だけである。

### 指摘2（採用）: 陽性対照が Claude の配備パスしか見ていない

設計書には「対照は `~/.claude/hooks/` 側を叩く」と書いた。しかし配備先は3系統ある（`~/.cursor/hooks/` / `~/.claude/hooks/` / `~/.codex/hooks/`）。Claude だけ叩いて緑にすると、**Cursor や Codex の側だけが脱落している状態を見逃す。** 受け入れ基準の「3ツールへの影響を確認した」に正面から関わるので、対照は3配備パスすべてに置く。

### 指摘3（採用）: 失敗とハングを混同していた

設計書に「隔離は exit code を変えないので鏡側も満たす」と書いたが、`( ... ) || :` が捨てるのは**終了した子の exit code** である。子が終わらなければ親も進まない。`deny()` の中で記録がハングすると `exit 2` に到達せず、Claude と Codex では non-blocking になって**止めるべきコマンドが通る**。要求Aの鏡が破れる。

これは正しい指摘である。緩和はするが消えない（記録経路をローカル FS の単一 builtin に限る・ネットワークやロックを持ち込まない）ので、**残る危険として正直に書く**。

### 指摘4（採用）: tally には時刻が無いので窓判定ができない

「直近の窓で判定する」と書いたが、tally が持つのは累計サイズと mtime ひとつだけで、イベントごとの時刻が無い。窓の中の増分を見るには**前回サイズのスナップショット（ウォーターマーク）が要る**。これが無いと、過去に1回起きた中抜けが累計差分を永久に汚し続ける。読み出し側にウォーターマークを持たせる。

### 指摘5（採用）: `inv − (allow + deny)` は純粋な指標ではない

この差には3つの異なる事象が混ざる。(a) 判定前に死んだ。(b) 判定は配送したが出口の tally 追記だけ失敗した。(c) 制御出力の後・tally の前で kill された。(b) は diag に痕が残るので、**差分と diag を併読して切り分ける**規約を読み出し側に置く。差分だけを見て「中抜け」と読まない。

### 指摘6（採用）: ツールごとの帰属ができていない

置き場をツール横断で共有すると、Cursor が死んでいても Claude が撃てば mtime が新しくなり、**偽の緑**になる。tally のパスにツール次元を足す（`tally/<tool>/<hook>.<slot>`）。ツールは `CURSOR_PROJECT_DIR` / `CLAUDE_PROJECT_DIR` の有無で判別し、どちらも無ければ `unknown` にする（Codex を名乗らせない。判別できないものを推測で埋めない）。

昇格の印: 「主張の根拠にした測定値が、主張している構成とは別の構成のものだった」という誤りの型は、性能を根拠に設計を選ぶ場面すべてで起きうる

## Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった

gate 2 は3レーンとも実返却した（cursor `cursor-grok-4.5-high` 143秒 / codex `gpt-5.6-sol` 422秒 / claude `opus` effort=high 898秒）。gate 1 で `auto` のまま投げて空返しになった反省からモデルを明示したのが効いた。

### 3レーンが独立に一致した点（重い順）

- **記録がハングすると `deny()` が `exit 2` に到達せず、鏡が破れる。** `( ... ) || :` が捨てるのは**終了した子**の exit code であって、子が終わらなければ親も進まない。私は「隔離は exit code を変えないので鏡も満たす」と書いていたが、これは**失敗とハングを混同**していた。
- **`hfr` の性能主張が構成と合っていない**（Step 7 で対応済み）。
- **陽性対照が Claude の配備パスしか見ていない。**

### claude レーンが単独で出した、私の論証の誤り（採用）

**DJ-4 を「未決へ戻す」と判断した根拠そのものが間違っていた。**

私は gate 1 で「隔離の中で `source` すれば壊れた lib も封じ込まれる」と実測し、共通ライブラリ案の却下根拠が消えたと書いた。しかし claude レーンの指摘は「**サブシェルの中で定義した関数は親に残らない**」である。つまり `( . lib.sh ) || :` は失敗を封じ込めるが、**その形では `hfr` が定義されない**。私が測ったのは「壊れた lib を source しても呼び出し元が死なないこと」であって、「その形で関数が使えること」ではなかった。**測定は正しく、そこから引いた結論だけが間違っていた**（Step 7 の誤りと同じ型が、別の場所でもう一度出た）。

検算した。隔離 source では `hfr` は未定義。素の `. lib.sh || :` にすると、lib が無いときに `hfr` が未定義のまま `hfr inv` に達して command not found で死ぬ。Cursor では `failClosed` なので全コマンドがブロックされる。生き延びさせるには lib + ガード + stub を結局3本に書くことになり、「1箇所 vs 三重化」の比較が成り立たない。さらに `source` は `hfr inv` より前に起きるので、source の失敗は台帳に現れず「配線されていない」と区別できない。

**DJ-4 は内蔵で確定。** 三重化の drift は規律ではなく機械的なテスト（3本から同じ区間を抜き出して byte 一致を確かめる）で閉じる。

### 無料で強くなった点（採用）

- **ツール判別を `$0` で行う。** `$0` はホストが使った呼び出しパスであって symlink の解決先ではない（実測で確認）。`~/.codex/hooks/...` と `~/.cursor/hooks/...` を見分けられるので、環境変数では識別できなかった **Codex を名指しできる**。fork も要らない。Codex の silent skip はこの設計がいちばん恐れている故障型なので、それを識別できない判別方法を使い続けるところだった。
- **記録先が通常ファイルでなければ触らない builtin ガード。** `HOOK_FIRING_DIR` という差し替え可能な指定を新設したこと自体が、**この単位が自分で作った fail-open のレバー**だった。記録先に FIFO を置かれると `open` がブロックし、`deny` が `exit 2` に到達しない。実測でブロックすることを確かめ、ガードを入れて rc=2 に到達することも確かめた。`${HOME:-/tmp}` という world-writable へのフォールバックも消した。
- **スロット名の白名簿。** 引数を忘れると末尾がドットのファイルへ静かに追記し続ける欠陥になるが、検出器が無い。`case` 1行で閉じた。

### 自分で踏んだ罠

ガードを書いたとき `[ -e X ] && [ ! -f X ] && return 0` と書いた。**このプラン自身が「`a && b` の形で書くな、条件が偽のとき `set -e` が発動する」と書いている、その規約を書いた直後に破った。** `if / then` へ直した。規約を知っていることと守れることは別だという実例なので、plan の該当項目に「執筆中に1度踏んだ」と書き添えた。

### 反映しきらず surface に回したもの

claude レーンは **「死を数えるのをやめて死を消す」**（3本に `trap` を張り、想定外の終了を必ず deny へ落とし、`jq` 依存を判定の手前から外す）を主柱に推した。採れば複式記入そのものが不要になり、本プランの複雑さの大半が消える。ただしこれは **Actuator の挙動を変える**変更で、今日は `jq` が無いと素通りしているものが止まるようになる。#309 は Sensor を足す issue なので、スコープ外として owner へ surface した。**プランの前提を置き換えうるので、実装着手前に判断してもらう項目の筆頭に置いた。**

昇格の印: 「測定は正しいが、そこから引いた結論が測定していない事柄に及んでいる」誤りが1つの単位で2回起きた。実測を根拠にする作業一般の失敗型として収穫する価値がある

## Step 9 — gate 3（owner 判断）で trap を採り、plan を改訂した

2026-08-09、owner が gate 3 で判断を下した。**私が「実装に入る前に決めてほしい」と surface した trap 案を採る。** 理由は「`jq` は実際に環境へ入っており、パッケージ化するなら必須にすればよい」で、私が引き換えとして挙げた「`jq` 不在で日常が止まる」はこの環境では成立しない、というものだった。

### 統括が足した論点 — trap は tally の置き換えではない

私が surface した書き方は「trap を採れば複式記入が不要になる」までで止まっていた。統括はそこに**私の書き落としを1つ足した**。

> trap は複式記入の置き換えであって、発火記録の置き換えではない。走らないフックに trap は張れない。

これは正しい。私は trap の効果を「死を数える必要がなくなる」と書きながら、**その死が「走った上での死」に限られること**を明示していなかった。本設計がいちばん恐れているのは Codex の silent skip のような「そもそも走らない」型で、そこには trap が届かない。もし統括がこれを足さなければ、「trap を入れたから tally は要らない」と読める余地が残っていた。**#309 の中心を落としかねない書き落としだった。**

改訂版では、2つの機構を「担当する故障」で対にして表に起こし、混同できない形にした。

### 実測してから書いた

trap は強制の挙動を変えるので、書く前に実機で確かめた（`tmp/probe-trap.sh` / `tmp/probe-trap2.sh`）。

- 正常系は壊れない（allow は rc=0・deny は rc=2）。**EXIT trap は正常終了でも発火する**ので、`0` と `2` を素通しする分岐を忘れると allow が deny に化ける。
- 内部エラー・`set -u` 違反・想定外の exit・**`jq` 不在**がすべて rc=2 へ収束する。今日 `jq` を外すと rc=127 で素通りするので、ここが本命だった。
- 収束時の stdout が**妥当な JSON** であることを確認した（`jq .` に通した）。**収束先の deny を `jq` で組んではいけない** — `jq` 不在こそ捕まえたい死因なので、収束先が `jq` を要求すると trap 自身が同じ死に方をする。素の `printf` で組んだ。
- `trap` は `HFR_*` の代入より**後**に張る必要がある。前に張ると、収束時に記録先の変数が未定義になって空パスへ飛ぶ（実測で確認）。
- 記録先も同時に壊れている最悪ケースでも再入せず rc=2 で終わる。

### 統括の見立てを1点訂正した

統括は「cc-lint の `inherit_errexit` の穴は、trap を採っても**ブロックすること自体は変わらない**」と見立て、違うと思えば指摘してよいと書いていた。**Cursor では正しいが、他2ツールでは正しくない。**

今日この穴が踏まれると、Cursor は `failClosed` でブロックするが、**Claude Code と Codex では rc=1 が non-blocking なので素通りする**。trap を入れると rc=2 になるので、3ツールすべてでブロックされるようになる。つまり trap は、この穴が踏まれたときの影響範囲を Cursor だけから3ツール全部へ**広げる**。trap の意図どおりの動作（内部エラーを黙って素通りさせない）ではあるが、別 issue の優先度を上げる材料なので plan に表で書いた。

### 副次的に良くなった点

複式記入を落としたことで、gate 2 が指摘していた弱点がまとめて消えた。累計差の解釈（残高であって死因の件数ではない）、ウォーターマーク、2回 snapshot の上下限、負値の扱い — これらは全部、差を数えるからこそ必要だったものである。**代わりに内部エラーが `deny.jsonl` に1件ずつ時刻つきで残るので、分解能はむしろ上がった。**

記録呼び出しも1起動あたり2回から1回に減ったので、ハングしうる面も減った。統括から「trap でハング面が増えていないか再評価せよ」と指示があり、**減っている**と答えられる形になった。

昇格の印: 「機構Aが機構Bを不要にする」と書くとき、A と B が同じ故障を担当しているかを確かめずに書くと、B が担当していた別の故障ごと落としかねない。今回は統括が拾ったが、書き手側の型として残す価値がある

## Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ

owner が gate 3 で追加判断を下した。**`cc-lint` の `inherit_errexit` 依存（#310 として起票済み）を、この単位で先に塞ぐ。** 理由は私が Step 9 で統括の見立てを訂正した内容そのもので、trap を入れるとこの穴の影響範囲が Cursor 1本から3ツール全部へ広がるためである。owner は公式ドキュメントで `PreToolUse` が実際にブロックするのは exit 2 だけであることも確認していた。

**コミット順は「#310 の修正 → trap → tally」に固定する。広がった穴を抱えた状態が一瞬も存在しないようにする。** これは順序そのものが要件になっている珍しい例で、3つを1コミットにまとめると要件を満たせない。

### 訂正が仕様になった

前版で私が書いたのは「統括の見立ては Cursor では正しいが他2ツールでは正しくない」という訂正で、そのときの扱いは「別 issue の優先度を上げる材料」だった。owner はそこから**順序の制約**を導き、本単位のスコープを1つ広げた。**指摘が「参考情報」ではなく「実行順序の要件」に変わった**ので、plan の Step 構成ごと組み替えた（Step 1 に #310 を挿し、trap 以降を1つずつ後ろへ送った）。

### 修正方針は「握り潰さない」で縛られている

owner が名指しした negative knowledge item `01KYA7C9NN4VDM7H2NYXZB2PSX` は「失敗を `|| true` で握り潰して空出力を後段へ流すな」である。`grep` の非マッチ（1）と環境エラー（2以上）は、握り潰すと同じ見え方になる。

そこで採った形は次の3つである。

1. **非マッチは正常なデータ不在として扱い、環境エラーとは分ける。** `case` で 0 / 1 / それ以外を書き分け、環境エラーは呼び出し側へ返す。
2. **パイプラインをやめる。** `head` の早期終了は `pipefail` 下で SIGPIPE 由来の非0を生みうる（もう1つの item `01KYA7C9M4EN1EZM3HBEN5WDRP`）ので、非マッチと環境エラーの区別を壊す。`grep` を here-string で1回呼び、`head -1` と `sed` は bash のパラメータ展開に置き換えた。外部プロセスが2つ減るので、全 Bash 呼び出しで走るフックとしては速くもなる。
3. **正規表現は変えない。** マッチ意味論を触ると非回帰の証明が難しくなる。

呼び出し側で環境エラーを deny に倒すとき、**`deny` を `extract_commit_segment` の中で呼んではいけない**ことに気づいた。この関数はコマンド置換の中で走るので、そこで出した制御 JSON は本物の stdout に届かず置換の戻り値に化ける。呼び出し側で処理する形にした。

### 差分比較で非回帰を証明した

実スクリプトは触らず、`tmp/` に旧版と新版を並べて同じ入力を流した（`tmp/probe-310-fix.sh`）。

- **`inherit_errexit` off（今日の状態）で、16ケースすべて判定が一致した**（rc の差分 0 件）。`&&` の有無、commit の有無、CC 形式の可否、クォート内の `&&`、単一の `&`、`git -C . commit` などを含めた。
- **`inherit_errexit` on では、旧版が4ケースで rc=1 を返し、新版は 0 件だった**（`cd /tmp && ls` / `echo a && echo b` / `make build && make test` / `cd x && git -C . commit -m "bad"`）。受け入れ条件が「有効・無効の両方を測る」ことを求めているので、両方測った。
- `shellcheck` は新版で指摘なし。

### 実測で issue 本文に無い穴が1つ出た

**今日は `grep` が PATH から消えると、`cc-lint` は黙って全コマンドを allow する。** 旧版に `grep` の無い PATH を渡すと rc=0 だった。パイプラインの非0が「マッチ無し」に化け、関数が `echo "$cmd"` へフォールスルーし、呼び出し側の正規表現に当たらず allow へ抜けるためである。

これは #310 の本題（`inherit_errexit` 有効時にブロックする）とは**逆向きの故障**で、強制が静かに消える型である。本 issue が潰そうとしているものと同種なので、修正で一緒に閉じる（新版は rc=2）。PR 本文に挙動変化として書く。

昇格の印: 同じ1行の欠陥が、設定次第で「過剰にブロックする」と「静かに素通しする」の両方向へ倒れていた。片方だけを見て修正方針を決めると、もう片方を残す

## Step 11 — 実装（owner が gate 3 で承認・コミット順が要件そのもの）

owner が実装を承認したので plan の Step 0 から着手した。コミットは要件どおり「#310 → trap → tally」の順で、3つとも別コミット・別検証にした。

### #310 の修正で、issue 本文に無い穴も一緒に閉じた

plan の段階で見つけていたとおり、**今日は `grep` が PATH から消えると `cc-lint` が黙って全コマンドを allow する**。実装後に実スクリプトで測り直して確認した（旧版 rc=0 → 新版 rc=2）。#310 の本題（`inherit_errexit` 有効時の過剰ブロック）とは逆向きで、強制が静かに消える型である。同じ1行が設定次第で両方向へ倒れていた。

非回帰は20ケースで確認した。`inherit_errexit` off では旧版と全ケース rc 一致、on では旧版が4ケースで rc=1 に対し新版は0件。

### trap は tally より前に置く必要があった（順序が要件）

trap を先に入れると #310 の穴の影響範囲が Cursor 1本から3ツール全部へ広がる。owner はそこから「広がった穴を抱えた状態を一瞬も作らない」という順序の制約を立てた。**順序そのものが要件になっている珍しい例で、3つを1コミットにまとめると要件を満たせない。**

実装して確かめた点が3つある。

- **EXIT trap は正常終了でも発火する。** `0` と `2` を素通しする分岐を忘れると allow が deny に化ける。ここは実装前に probe で踏んでいたので織り込めた。
- **収束先の deny を `jq` で組んではいけない。** `jq` 不在こそ trap が捕まえたい死因なので、収束先が `jq` を要求すると trap 自身が同じ死に方をする。素の `printf` で組み、stdout が妥当な JSON になることを `jq .` に通して確認した。
- **`trap` は `HFR_*` の代入より後に張る。** 前だと収束時に記録先の変数が未定義になり、記録が空パスへ飛ぶ。

Step 2 の時点では `HFR_*` がまだ存在しないので、trap ハンドラは deny を出して `exit 2` するだけにし、記録の行は Step 3 で足した。**段を分けたおかげで、各コミットが単独で検証可能なまま保てた。**

### 3本の共通部分を byte 一致で縛った

gate 2 で「三重化は3か所のうち1か所を直し忘れる事故を作る」と指摘され、「規律ではなく機械的なテストで閉じよ」と言われていた。`# --- shared begin ---` / `# --- shared end ---` で挟み、区間の shasum が3本で一致することを各コミットで確認している。スクリプトごとに違う唯一の行（`HFR_HOOK`）は区間の外へ出した。

### 自分の検査が間違っていて、実装が正しかった

Step 3 の GATE で「ツール判別が `unknown` にしか落ちない」と出て、一瞬コードを疑った。**間違っていたのは検査のほうだった。** `bash script.sh <arg>` は `$1` を渡すだけで `$0` は変わらない。`$0` は「ホストが使った呼び出しパス」なので、**配備と同じ symlink 経由で叩かないと再現しない**。配備を模した symlink を作って叩き直したら claude / cursor / codex / unknown に正しく分かれた。

検査台本の側にその理由をコメントで残した。同じ誤りをもう一度踏むと、今度は「実装が壊れている」と誤診して直しにいくおそれがある。

### 読み出しで自分が踏んだ罠

`oe-hookfire` で `grep -c` の結果を `n="$(grep -c ... || printf '0')"` と書いた。**`grep -c` は「0 件」でも `0` を印字したうえで exit 1 を返す**ので、`|| printf '0'` が余計な `0` を足して変数に改行が混ざり、以降の比較（`[ "$n" != "0" ]`）も表示も壊れた。0 件なのに `broken` を出し、detail が途中で切れた。

**これは #310 で直したのと同じ型である**（コマンドの exit code の意味を取り違え、非マッチと失敗を混ぜた）。同じ arc の中で、直した側と踏んだ側の両方をやったことになる。

昇格の印: 「検査が赤いとき、疑う順序を実装→検査ではなく検査→実装にすべき局面がある」。今回は検査の誤りを実装の誤りと読みかけた

### 実機確認の限界（正直に書く）

plan の GATE は「Claude Code で実機確認する」と書いていたが、**完全な end-to-end はこのセッションでは実施できなかった。**

- `.claude/settings.local.json` にプロジェクト単位で登録する案は、**Claude Code がフックをセッション開始時にしか読まない**ため反映されなかった（実測）。
- 配備先の symlink を一時的に張り替える案は、**auto mode の分類器にブロックされた**。配備済みハーネスの書き換えは保護対象であり、これは正しいガードである。**迂回しない。**

そこで到達できる最強の水準に置き換えた。**配備と同じ symlink 経由**（`$0` が `~/.claude/hooks/...` になる形）で、**実際の Claude Code の payload 形**（`session_id` / `transcript_path` / `cwd` / `hook_event_name` / `tool_name` / `tool_input.command` / `description` / `timeout` を含む全フィールド）を流して確かめた。未知フィールドが増えても壊れないことも確認した。

**確かめられていないのは dispatcher の層である** — `settings.json` への登録、matcher、フックの順序と short-circuit、Codex の workspace trust。これは plan の陽性対照の節で「1段目では確かめられない」と明記した範囲そのもので、owner がマージ後に sync してから2段目（ツール自身にシェルを撃たせる）で確かめる必要がある。**「実機確認した」と丸めて書かない。**

## Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた

3レーンとも実返却した（cursor `cursor-grok-4.5-high` 252秒 / codex `gpt-5.6-sol` 478秒 / claude `opus` effort=high 625秒）。設計SO と違い、今回は**全レーンが返った**。

### いちばん重い指摘 — 要求Aの鏡が塞がっていなかった

**FIFO ガードを tally にしか掛けていなかった。** `deny.jsonl` と `diag.jsonl` は素通しだった。codex と claude が独立に指摘し、実測でも再現した。

- `deny.jsonl` を FIFO にすると deny 経路がハングして `exit 2` に到達しない（rc=TIMEOUT）。
- `diag.jsonl` を FIFO にし tally を書けない状態にすると allow 側がハングする。

**これが痛いのは、私が plan で「この単位が自分で作った fail-open のレバーだったので、同じ単位の中で閉じた」と宣言していた面だからである。** 閉じたと書いた場所が閉じていなかった。しかも `deny.jsonl` は **deny のたびに必ず**書かれるので失敗経路ですらなく正常経路で、allow 側は書かないため「allow は通るのに deny だけ止まらない」という、自分で「静かで取り返しがつかない」と書いた形そのものになる。

原因ははっきりしている。**FIFO の検討を tally に対してだけ行い、追記先が3つあることを見ていなかった。** ガードを書いた時点で「追記する場所すべて」に一般化していれば防げた。関数（`hfr_appendable`）に切り出して3経路に通した。

昇格の印: 「この経路は塞いだ」と書くとき、塞いだのが経路の1本なのか同種の経路すべてなのかを数えていない。ガードは書いた場所ではなく守るべき性質の側から数える

### 読み出しは Sensor として壊れていた

cursor レーンが最初に指摘した。**空の tally + 新しい mtime を「発火した」と読んでいた。** `touch` や truncate でセンサーが緑になる。「0件を発火と読まない」という本 issue の中心を、センサー自身が破っていた。

codex はさらに**ツール軸の欠落**を突いた。存在するディレクトリだけを回していたので、Cursor と Codex が丸ごと未配備・silent skip でも行が1つも出ず exit 0 に到達しうる。**いちばん恐れている故障型が緑になる。** 期待するツールを先に列挙する形へ変えた。

`--days` に値を書き忘れると無限ループする件も出た（`$#` が 1 のとき `shift 2` はシフトせず非 0 を返すが、`-e` が無いので回り続ける）。実測で rc=124 を確認した。

### 私が自分で踏んだ型を、また踏んでいた

内部エラーの判定を「累計件数 > 0 かつファイルの mtime が新しい」で書いていた。大昔の1件が残っていると、今日の通常 deny で mtime が更新されるだけで永久に `broken` を出す。**これは NK item 01KZ21M0N0FZ4YMR2R6DPHTWQ4（「実績が1件でもあれば ok」と書くな・直近の窓で見よ）の鏡像**である。私は ok 側でその罠を避けたのに、broken 側で同じ構造を作っていた。窓は行の `ts` で切る形へ直した。

### 疑いが晴れた指摘も1つある

claude レーンは「payload が malformed で jq が 2 を返すと、trap が『正常な deny』として素通しし、記録が1件も残らない」を挙げ、**自分では測れないので確かめてほしいと明記していた**。測った。`jq` は不正な JSON に **5** を返すので trap が収束させ、記録も残る。指摘は成立しない。

**レーンが自分の未検証を未検証と書き、こちらが測って落とせた**のは良い往復だった。3レーンの指摘のうち、実測で落ちたのはこれ1件で、残りはすべて成立した。

### 反映の結果

回帰テスト `projects/orchestration-engine/tests/test_hook_firing.sh` を 39 項目まで増やした。**3本の共通区間の byte 一致**を機械的に確かめるので、三重化のドリフトが落ちるテストになる（区間を意図的に壊すとテストが落ちることも確認済み）。gate 2 で「規律ではなく機械的なゲートで閉じよ」と言われた点に、ここで初めて実体が付いた。

### コミットメッセージを自分で壊した

`git commit -m "..."` の本文にバッククォートを入れたため、**二重引用符の中でコマンド置換と変数展開が走り**、`` `--days` `` や `` `$#` `` が消えた本文がコミットされた。`git log` を読んで気づき、メッセージをファイルに書いて `--amend -F` で直した。以後は `-F` を使う。

昇格の印: シェル経由でコミットメッセージを渡すとき、二重引用符の中のバッククォートと `$` は展開される。散文にコード片を含める運用とは相性が悪い

## フィードバック（closure・heavy tier・後追い）

**この節は後追いで書いた。** 本単位は 2026-08-08〜09 に走り、`status` を `draft` のまま、判定を1件も書かずにマージされた。この節を書いたのは 2026-08-10 の別単位（#288・報告経路の修復と closure の不履行の修復）である。**本文（Step 1〜12）は当時のリアルタイム追記のままで、書き換えていない。** 後から足したのはこの節と frontmatter の `promotion` だけである。したがって**本節の判定は、当時その場で下した判断ではなく、後から本文の印を辿って下した判定である。**

`reconstructed` を冒頭に宣言していないのは、宣言の対象が「episode 全体が後追い再構成である」というファイル単位の性質だからである。本件は本文が追記ログとして生きており、後追いなのは closure だけなので、範囲を偽らないようこの節に限って明示する。

### tier 判定

**heavy。** 意図的に起動した外部レビューを3回通している（gate 1 のゼロベース探索 SO・gate 2 の設計SO・gate 4 の実装SO）。加えて失敗・撤回・方針転回がそろっており（`本文: Step 5 — gate 1（ゼロベース探索 SO）と、その指摘の検算` で自案が2度差し替わり、`本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった` で却下根拠の誤りを撤回した）、非自明な設計判断も昇格候補もある。heavy トリガに複数該当するので opt-out と standard は検討しない。

### closure gate

- **Context / なぜ**: 本文冒頭に自己完結して在る（`本文: なぜこの作業が始まったか`）。止める側のフック3本が発火を1行も記録せず、強制点が働いたかを当事者以外が確かめられない、という起点である。
- **次の消費者**: (1) **止める側のフックを触る実装子** — 記録を足す位置と、記録の失敗を判定経路へ漏らさない形を読む。(2) **Sensor 側を伸ばす単位**（Epic #37 の制御ループ5要素） — 本単位が最小増分として何を残し何を残していないかを読む。(3) **negative knowledge の収穫者** — 下記「昇格の判定」で `required` にした12件を収穫する単位。(4) **fail-closed な機構に副作用のある処理を足す設計者** — bash の抑止文脈の実測（`本文: Step 2 — 中心の仮定を実測して、半分が崩れた` / `本文: Step 5 — gate 1（ゼロベース探索 SO）と、その指摘の検算`）を読む。
- **follow-up routing**:
  - **`required` 12件の収穫** → 本単位（#288）では実行しない。理由は下記「収穫を分離した理由」に書いた。**行き先＝親へ引き渡し、owner が follow-up issue を起票する**（本単位では起票しない）。**issue が未作成のあいだも収穫対象の集合は失われない** — 12件は上の判定節に1件ずつ pointer 付きで列挙してあり、これが committed な work-list になる（先送りが分母ごと消える形にはしていない）。
  - **`cc-lint` の `inherit_errexit` 依存** → **着地済み。** `本文: Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ` のとおり #310 を本単位へ取り込んで塞いだ（マージ済み・commit 91984dc の帯）。
  - **配備2段目（dispatcher 層）の実機確認** → **部分的に解消。** フックの起動とツール別の帰属は下記「evidence anchor」に後追いの観測を転記した。**残る3項目**（deny 後の短絡の有無／`PreToolUse` の stdout で未知キーを渡したときの扱い／`deny` 全文が Codex の transcript に残るか）は plan の「test plan に入れる実測」節にあり、**いまも未確認である。行き先＝親へ引き渡し、owner が #301（定期実行の配線）と束ねるか単独で起票するかを決める。** 本単位では測っていない（配備済みハーネスへ撃つ検査であり、本 PR の scope 外）。
  - **注入された negative knowledge への観測書き戻し（段5）** → **復元不能。** 下記「観測の書き戻し」に理由を書き、親へ surface する。
  - **`oe-send --help` が 1 を返す** → **既に routing 済みだった**（gate 4 の指摘で気づいた）。`projects/orchestration-engine/docs/plans/2026-08-09-plan-309-exit-contract.md` の「スコープ外」節が、`oe` / `oe-ack` / `oe-delegate` / `oe-report` / `oe-send` の5本について「`--help`→1 は別 PR」と設計SO 各レーンの一致つきで記録している。**本単位の発見は再発見であって新規の残課題ではないので、前方参照に置き換える。**
- **status 確定**: `draft` → **`stable`**。達成度は**達成**である。issue #309 の中核（止める側3本に発火記録を置き、記録の失敗を判定経路へ漏らさない）は実装され、内部エラーが `deny` へ収束することも、3ツールで tally が分かれることも実機で確認できている。当時 `未確認` として正直に残していた dispatcher 層は、**フックの起動とツール別の帰属までが後追い観測で埋まり、残る3項目は未確認のまま routing した**（丸めて「解消済み」とは書かない）。

### 昇格の判定

**印は12件あり、印以外に closure で独立に問うて見つけた判断が7件ある。合わせて19件を1件ずつ判定した**（frontmatter `promotion` の19エントリと `subject` で対応する）。集計行は置かない。

**印を辿るだけでは出てこない判断を、closure で独立に問うて7件足した**（観測器を期待集合の側から書く／ツール判別を `$0` で行う／コミット順を要件として固定する／三重化を byte 一致のテストで閉じる／配備2段目を owner 観測に委ねる／観測の書き戻し／decision を作らない）。**印の集合は分母の候補であって分母そのものではない。** うち最初の2件は **gate 4 の3レーンが独立に「まだ取りこぼしがある」と指摘して見つかった** — 印を辿る作業を自分で1周しただけでは出てこなかった。

**`required`（12件）— 行き先はいずれも型付き knowledge store。**

- **記録の失敗を第2の経路へ逃がす型**（`本文: Step 1 — 触る対象の実物を読んだ（一次確認）`）。`oe-prompt-receipt.sh` が既に同じ形を採っており、本単位で3本目・4本目の実装になった。**設計根拠そのものは committed な plan（`projects/orchestration-engine/docs/plans/2026-08-08-plan-309-hook-firing-record.md`）に在るので decision 昇格は要らない**（§13.2 ①）。要るのは再利用できる型としての着地で、そちらは rule / skill / guard / 既存 item のいずれにも無い。
- **bash の抑止文脈2件**（`本文: Step 2 — 中心の仮定を実測して、半分が崩れた` / `本文: Step 5 — gate 1（ゼロベース探索 SO）と、その指摘の検算`）。「テスト文脈は `set -e` を抑止するが `set -u` は抑止しない」と「抑止文脈はサブシェルへ伝播するがプロセス境界を越えない」である。**2件は1つの item に束ねるのが自然だが、印が別々に立っているのでエントリは分けた。** 2つの bash（3.2.57 / 5.2.37）で確かめており、`grep` した範囲では plan とフック実体の外に着地していない。
- **検査器が被検査対象に食われる**（`本文: Step 4 — 陽性対照に使える具体的なコマンドを確定した`）。止める側のフックを検査すると検査コマンド自身が発火する。母集団は「そのコマンドが強制点を通る形で検査される機構」で列挙できる。
- **レーンの一致は合意であって検証ではない**（`本文: Step 6 — claude レーンが設計を作り替えた（gate 1 の本体）`）。`evidence-verification-rule` §3 の「LLM judge だけに頼るな」と `reframe-on-stall-rule` の「収束は確信を上げるが証明ではない」に隣接するが、**独立した N レーンが同じ読解の癖を共有して同じ穴に落ちる形は、どちらにも着地していない。** 本件は原因まで特定できている（`inherit_errexit` が既定 off なのでコマンド置換へ errexit が伝播しない）ので材料が強い。
- **測定と主張の不一致2件**（`本文: Step 7 — gate 2（設計SO）で、自分の測定の当て方が誤っていたと分かった` / `本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった`）。**同じ型が1単位で2回出ている**（本文が自分でそう書いている）。1件目は測定した構成が主張する構成と別（12倍のずれ）、2件目は測定は正しく結論が測っていない事柄へ及んだ。**2件で1つの item に束ねるのが自然だが、印が別なのでエントリは分けた。**
- **機構Aが機構Bを不要にすると書くとき、担当する故障が同じか確かめる**（`本文: Step 9 — gate 3（owner 判断）で trap を採り、plan を改訂した`）。落としかけたのは #309 の中核（走らないフックには trap を張れない）で、拾ったのは統括だった。
- **同じ1行の欠陥が両方向へ倒れていた**（`本文: Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ`）。設定次第で「過剰にブロックする」と「静かに素通しする」の両方になる。片方だけ見て方針を決めるともう片方が残る。
- **観測器は「在るもの」を数えるのではなく「期待する集合」を先に置いて欠を見る**（`本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた`）。**印が無い判断で、gate 4 の指摘で見つけた。** 読み出しが存在するディレクトリだけを回していたため、Cursor と Codex が丸ごと未配備でも行が1つも出ず exit 0 に達しうる状態だった（本文が「いちばん恐れている故障型が緑になる」と書いている）。期待するツールを先に列挙する形へ変えて閉じた。**同じ型がこの closure 自身にも現れている** — 注入された item の期待集合を先に durable に書いておかないと、後から分母を復元できない（下記「観測の書き戻し」）。**在るものを数える検査は、丸ごと無いことを検出できない**という一つの型である。
- **ツール判別を環境変数ではなく `$0` で行う**（`本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった`）。**印が無い判断で、gate 4 の指摘で見つけた。** `CURSOR_PROJECT_DIR` / `CLAUDE_PROJECT_DIR` の有無では Codex を名指しできず `unknown` に落ちるが、`$0` はホストが使った呼び出しパスなので3ツールを見分けられる。**この設計がいちばん恐れている故障型（Codex の silent skip）を、識別できない判別方法のまま進めるところだった。** fork も要らない。棄却した案（環境変数）と採った案の差が本文に残っている。
- **修正が既存の穴の影響範囲を広げるとき、コミット順が要件になる**（`本文: Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ`）。**印が無い判断を closure で独立に問うて見つけた1件である。** trap を入れると `cc-lint` の既存の穴が Cursor 1本から3ツール全部へ広がるので、「#310 の修正 → trap → tally」の順を固定して、広がった穴を抱えた状態が一瞬も存在しないようにした。**再利用できる形は「順序が style の好みではなく正しさの要件になる条件」**で、`本文: Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ` が書いているとおり、この場合は3つを1コミットにまとめると要件を満たせない。当初この発見は「別 issue の優先度を上げる材料」として扱われ、owner が順序の制約へ読み替えた経緯まで残っている。

**`not-required`（6件）。**

- **検査が赤いとき検査台本の側を先に疑う**（`本文: Step 11 — 実装（owner が gate 3 で承認・コミット順が要件そのもの）`）。**効く具体（`$0` はホストが使った呼び出しパスなので、配備と同じ symlink 経由で叩かないと再現しない）は既に使う場所へ着地している** — `projects/orchestration-engine/tests/test_hook_firing.sh` の 7 行目と 39 行目のコメントがそれで、次に踏む人はそこで当たる。一般形（「検査を先に疑え」）は母集団を書けないので、収穫しても行動を変える形にならない。**当初これを `01KZ3MHMEQN5XEJGV9809QPH2A` の除外条件（「母集団を定義できない広い型」）を引いて根拠にしていたが、gate 4 の指摘を受けて引用を外した** — あの除外条件は「その item の処方（走査）が使えるか」を切るもので、別の知見を収穫してよいかの基準ではない（他 item の `exclusions` を収穫基準に転用したのは筋が違う）。**判定は前半の「効く具体が使う場所へ着地している」だけで立つ。**
- **FIFO ガードを追記先3経路すべてへ通す**（`本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた`）。**既出。** 直接一致するのは `01KZ3MHMEQN5XEJGV9809QPH2A`（外部指摘が名指しした1箇所を直した緑は、その型が消えた証拠ではない）である。本件は「同種の追記先が3つあるのに1箇所しか走査しなかった」型で、まさにこれに当たる。**当初は `01KYSKQHDT86GZW87E8EDMSE46`（遮断の決定と実装は別）を根拠に挙げていたが、gate 4 の指摘を受けて差し替えた** — あちらは「遮断した情報が下流の入力口から復活する」型であり、本件は上流の遮断決定を持たないので完全な同旨ではない（隣接はする）。**より価値があるのは新 item ではなく `01KZ3MHMEQN5XEJGV9809QPH2A` への観測レコード**である（同じ族の5件目で、しかも「隣を見つけたのは自分ではなく後続の外部ラウンド」という同 item の中核パターンにも一致する）。**当初これを「下記の書き戻しの穴に当たる」として見送っていたが、gate 4 の指摘で誤りだと分かった** — この item は下記の「注入されたかもしれない4件」のどれでもないので、**注入の分母が復元できるかとは無関係である。** 無関係な理由で行き先を消していた。**下記のとおり、この closure で観測を1件足した。**
- **三重化した共通区間の drift を byte 一致のテストで閉じる**（`本文: Step 11 — 実装（owner が gate 3 で承認・コミット順が要件そのもの）`）。**印が無い判断を closure で独立に問うて見つけた1件である。** 判断そのものは正しく、`projects/orchestration-engine/tests/test_hook_firing.sh` に実体がある。**ただし「規律ではなく機械的なゲートで閉じる」という方針はこの repo で既に確立しており**、本件では gate 2 がそう指示したものを実装した形なので、新しく昇格させる内容は増えない。
- **配備2段目の確認をマージ後の owner 観測に委ねる**（`本文: 実機確認の限界（正直に書く）`）。判断自体は正しく、下記のとおり既に果たされた。判定の対象として挙げるが、昇格する内容ではない。
- **コミットメッセージを `-m` ではなくファイルから渡す**（`本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた`）。**当初 `required` にしたが、gate 4 の指摘を受けて降格した。** 二重引用符の中でバッククォートと `$` が展開されるのは shell の直接の挙動なので、収穫基準の「非自明」を満たさない。**ただし行動変更としては実体がある** — トリガは散文にコード片を含めるコミット、機構は `git commit -F <file>`、着地先は `conventional-commits` スキルである。**併設は昇格ではない**ので、判定は `not-required` のままで行き先だけを持つ。姉妹の `pr-conventions` は PR 本文について既に `--body-file` を必須にしているのに、コミット側に同じ規律が無いという非対称が根拠になる。
- **設計内容を decision へ昇格させず plan と episode に留める**（`本文なし: decision を作らない判断は本文に明示がなく、closure で独立に問うて判定した`）。**印が無い判断を closure で独立に問うて見つけた1件である。** 58KB の committed な plan が探索・棄却・根拠を保持しており、§13.2 ① の「既出」に当たる。再利用できる部分は上の `required` として store へ回るので、decision 層に新しく置く価値は増えない。

**`unknown`（1件）。**

- **注入された negative knowledge への観測書き戻しを本単位で行わない**（`本文なし: 8月8日の brief が残っておらず、slot に載っていた item の集合を復元できない`）。**`not-required` に畳まない。** 本文は4つの item を名指ししている（`01KYMRE1N7N0J8VP3CBZZEZ8Q3` / `01KYA7C9NN4VDM7H2NYXZB2PSX` / `01KYA7C9M4EN1EZM3HBEN5WDRP` / `01KZ21M0N0FZ4YMR2R6DPHTWQ4`）が、**そのうちどれが brief の slot 経由で注入され、どれを owner が会話で名指しし、どれを子が自分で引いたのかが区別できない。** **何が分かれば決まるか**: 8月8日の brief に negative knowledge の slot があったか、あったならどの id が載っていたか。brief は作業層（gitignored）なので既に無く、期待集合を durable に残す1行も書かれていない。分母が復元できない状態で一部だけ書き戻すと、**偽の分母を作って「全 item に書き戻した」と読ませてしまう**ので書かない。

### 収穫を分離した理由（`required` 12件を本 PR で実行しない）

`episode-retrospective` の収穫 Step は in-PR 相乗りを既定とし、分離は例外として3条件を挙げている。**gate 4 はこの分離に不同意だったので、反論を書いてから判断を残す。**

**gate 4 の反論**: (a)「候補が多いから heavy」は、各 item がなぜ単独レビューを要するかを示していない。(b)「closure 修復 PR だから収穫は scope 外」は、収穫が closure Step 5 の既定作業である以上循環している。(c) owner の明示 defer の durable な証拠が見つからない。(d) split 先が未作成の issue なので「後で追記」の先送り（監査 L4）と同型である。

**(b) と (c) には答えがある。**

- **この単位は #309 の作業単位ではなく、後から入った修復単位である。** in-PR 相乗りの既定は「作業した本人が、その作業のブランチで、マージ前に収穫する」形を前提にしている。#309 は既にマージ済みで、本単位は #309 を実行していない。**したがって循環ではなく、前提が違う。**
- **owner の defer は本単位の委譲書面に在る。** 委譲時の書面がこの単位の scope を「判定まで」と定めており、収穫を含めていない。ただし**その書面は作業層（gitignored）なので、リポジトリの中だけを見る読み手には確かめられない。** gate 4 が (c) を挙げたのは、確かめられる場所に無いためである。**この行がその代わりの記録になる。**

**(a) には部分的に同意する。** 「件数が多いから heavy」は弱い根拠である。より強い根拠はこちらである。**本単位は #309 の作業を実行していないので、収穫の一次材料を持っていない。** 既存 item（例 `01KZ3MHMEQN5XEJGV9809QPH2A`）が持っている厳密さ — 確立と仮説の書き分け、隣接 item との軸の違い、`exclusions` の列挙 — は、実測した本人の手元にある材料から書かれている。**episode の記述だけから12件を書くと、一次材料を持たない者が伝聞で knowledge を作ることになり、store の品質基準を下げる。**

**(d) は正当な指摘なので緩和した。** 先送りが分母ごと消えるのを防ぐため、**収穫対象の12件を上の判定節に1件ずつ pointer 付きで列挙してある。** issue が未作成のあいだも、何を収穫すべきだったかは committed 層から復元できる。

**行き先**: 親へ引き渡し、**owner が follow-up issue を起票する**。**本単位では起票しない。** owner が「この PR で収穫せよ」と判断するなら、上の (a) の限界（伝聞になる）を承知のうえでの指示として受ける。

### 出力型（Step 3）

- **事実・失敗**（**gate 4 の指摘で列挙を主要5件から全件へ広げた**。本文への pointer で足りるが、項目としては落とさない）:
  - **外部レーン自体の失敗**: cursor が `success_empty`、codex が `timeout_empty`（`本文: Step 5 — gate 1（ゼロベース探索 SO）と、その指摘の検算`）。
  - **自分の案が gate 1 の中で2度差し替わった**（`本文: Step 6 — claude レーンが設計を作り替えた（gate 1 の本体）`）。
  - **gate 2 で採用した外部指摘5件**: 陽性対照が Claude の配備パスしか見ていない／失敗とハングの混同／tally に時刻が無く窓判定ができない（ウォーターマーク欠落）／`inv − (allow + deny)` が純粋な指標でない／ツールごとの帰属ができていない（`本文: Step 7 — gate 2（設計SO）で、自分の測定の当て方が誤っていたと分かった`）。
  - **性能主張が実測の12倍ずれていた**（`本文: Step 7 — gate 2（設計SO）で、自分の測定の当て方が誤っていたと分かった`）。
  - **却下根拠の誤りを撤回した**（`source` の隔離では関数が親に残らない・`本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった`）。
  - **規約を書いた直後に自分で破った**（`a && b` を禁じた plan の執筆中に自分で書いた・`本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった`）。
  - **統括が中心の書き落としを拾った**（trap は複式記入の置き換えであって発火記録の置き換えではない・`本文: Step 9 — gate 3（owner 判断）で trap を採り、plan を改訂した`）。
  - **検査側の誤り2件**: `$0` を再現しない検査台本を実装の誤りと読みかけた／`grep -c` の exit code を誤読して 0 件で `broken` を出し表示を壊した（`本文: Step 11 — 実装（owner が gate 3 で承認・コミット順が要件そのもの）`）。
  - **読み出しが Sensor として壊れていた4件**: 空 tally + 新しい mtime を発火と読む／期待するツールを列挙せず未配備でも緑になる／`--days` の値欠落で無限ループ／古い内部エラーで永久に `broken`（`本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた`）。
  - **要求Aの鏡が塞がっていなかった**（FIFO ガードが tally だけで `deny.jsonl` / `diag.jsonl` は素通し・`本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた`）。
  - **コミットメッセージを実際に破損させた**（`本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた`）。

- **決定と根拠**: `本文: Step 9 — gate 3（owner 判断）で trap を採り、plan を改訂した`（trap を採り、複式記入を落とした。棄却理由つき）と `本文: Step 10 — #310 を本単位に取り込み、コミット順で穴を塞ぐ`（コミット順が要件そのものになった経緯）。
- **わかったこと**: bash の抑止文脈の実測（`本文: Step 2 — 中心の仮定を実測して、半分が崩れた`）、1バイト追記の並行安全性と費用（`本文: Step 6 — claude レーンが設計を作り替えた（gate 1 の本体）`）、`$0` がツール判別に使えること（`本文: Step 8 — gate 2 の残り2レーンと、自分の論証の誤りが1件見つかった`）。
- **原則**（`本文なし: 個々の原則は上の「昇格の判定」節に pointer 付きで置いた。本行はその索引である`）: `required` の12件がこれに当たる。
- **行動変更**: **コミットメッセージをファイルから渡す** — トリガは散文にコード片を含めるコミット、機構は `git commit -F <file>`、着地先アーティファクトは `conventional-commits` スキル（`本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた`）。**本単位の修復コミット自身がこの形で書かれており、バッククォートと `$BIN` が展開されずに残ることを確認した。** **行き先**: `conventional-commits` への追記は本 PR の scope 外（配布スキルの変更は別単位）なので、**親へ引き渡し owner が起票する**。判定は `not-required`（昇格ではなく併設）である。
- **蒸留シグナル**（`本文なし: 昇格先の種類は closure で決めたもので、本文に対応する記述が無い`）: **knowledge store**（`required` 12件すべて）と、**併設の実装アーティファクト**（`conventional-commits` へ1行・併設は昇格ではない）。**decision 昇格はなし**（理由は上の `not-required` の「設計内容を decision へ昇格させず」の項）。
- **残課題**（`本文なし: 行き先の付与は closure の作業であり、本文には残課題の一覧が無い`）: 上の follow-up routing に全件の行き先を書いた（`required` 12件の収穫／dispatcher の残る3項目／観測の書き戻し／`conventional-commits` への追記／`oe-send --help` は既存記録への前方参照）。**行き先なしの箇条書きは無い。**

### evidence anchor（条件付き・後追いで埋めた）

**本文が参照している `tmp/` の台本はすべて失われている。** 確認したところ、`tmp/probe-errexit.sh` / `probe-errexit2.sh` / `probe-hooks.sh` / `probe-so-claims.sh` / `probe-bypass.sh` / `probe-inherit.sh` / `probe-a1.sh` / `probe-hfr-cost.sh` / `probe-trap.sh` / `probe-trap2.sh` / `probe-310-fix.sh`、探索木 `tmp/dj-309-exploration-tree.md`、設計書 `tmp/design-309-revised.md`、SO プロンプトと出力 `tmp/so-309-gate*` のいずれも残っていない（repo 直下 `tmp/` に残るのは #309 と無関係の1件と、**本 closure 自身の gate 4 SO 出力**である。後者も揮発するので下記「SO 証跡」で要点を本文へ転記した）。

**ただし要点は本文に転記済みで、判断が読めなくなってはいない。** 実測値（`0.186 ms` / `2.189 ms` / 並行60プロセス × 50回で 3000/3000 バイト・ロストなし / 非回帰20ケース / 回帰テスト39項目）はいずれも本文に数値として在る。**永続アーティファクトは `projects/orchestration-engine/tests/test_hook_firing.sh`**（39項目・3本の共通区間の byte 一致を機械的に確かめる）で、設計の詳細は committed な plan に在る。

**ただし「台本の役目を引き継いでいる」とは書けない**（gate 4 の指摘で訂正した）。回帰テストが確かめるのは実装後の振る舞いであって、判断の根拠になった測定を再実行するものではない。**回復不能になったものを正直に挙げる。**

- `probe-hfr-cost.sh` の反復回数・測定環境・集計方法（残っているのは結果の数値だけである）。
- `probe-errexit*` / `probe-so-claims.sh` 等の正確な入力と期待出力。bash 3.2.57 と 5.2.37 の micro-probe は再実行できない。
- gate 1 の探索木にあった代替案の全体像（本文に残るのは採否の結論だけである）。
- 3回の SO の正確なプロンプト、各レーンの未採用の指摘、出力の provenance。

**つまり判断の概要と結論は読めるが、独立に再検証できる水準では永続化されていない。**

**後追いの観測を1つ足す（本文の未確認を埋める証拠）。** `本文: 実機確認の限界（正直に書く）` は「dispatcher の層は確かめられていない」と正直に残し、owner がマージ後に sync してから2段目で確かめる必要があると書いていた。**2026-08-10 に確認したところ、2段目のうち「フックが起動され、ツール別に帰属される」部分は満たされている。** ただし2段目の全体ではない（下記「言えることの限界」）。

- 3系統の配備先すべてに発火記録つきの実体が入っている（`~/.claude/hooks/` / `~/.cursor/hooks/` / `~/.codex/hooks/` の `block-destructive.sh` に `HFR_` 参照が 27 件ずつ）。
- `~/.claude/state/hook-firing/tally/` がツール次元で分かれて溜まっている（`claude/` / `cursor/` / `codex/` / `unknown/`）。3本のフックそれぞれに `.allow` が在り、いずれも空でない。
- **この設計がいちばん恐れていた故障型（Codex の silent skip）は起きていない。** `codex/` に実データが在ることが、trust されないフックが黙って skip される状態でないことを示す。
- `unknown/cc-lint.deny` も在り、判別できないものを推測で埋めない設計どおりに機能している。

**言えることの限界**: 上は「3ツールとも dispatcher がフックを起動し、ツール判別が機能している」ことを示すだけである。**plan が「証拠として残っていないので1回測る」と挙げた3項目は、いまも未確認のまま残っている** — (a) 3本が deny の後に短絡されるか逐次か並行か (b) Claude Code が `PreToolUse` の stdout で未知キーの JSON を受け取ったときの扱い (c) `deny` の全文が Codex の transcript に残るか（`projects/orchestration-engine/docs/plans/2026-08-08-plan-309-hook-firing-record.md` の「test plan に入れる実測」節）。**したがって dispatcher 層を「解消済み」と丸めない。** この3項目の行き先は上の follow-up routing に書いた。

### SO 証跡（Step 4・外部チェック・**要点を本文へ転記する**）

**本 closure は #288 の単位で書かれており、その単位の gate 4（実装SO）が本 closure 自体を確認対象に含む。** 確認させたのは Step 4 が定める4点（失敗の選択的省略・routing の網羅・evidence anchor・back-propagation）に、判定の妥当性とコミット1の修復を足したものである。

**レーンの実返却状況（正直に書く）**: 3レーンとも実返却した。codex `gpt-5.6-sol`（430秒・13.1KB）／claude `claude-opus-5` effort=high（813秒・28.3KB）／cursor `cursor-grok-4.5-high`（277秒・13.2KB）。**cursor は既定の `auto` を避けてモデルを明示した**（#309 の gate 1 で `auto` が `success_empty` を返した反省である）。

**出力は `tmp/` にしか無い＝揮発する。** これは本 closure が数行上で「本文の `tmp/` 参照はすべて失われている」と診断した、まさにその層である。**同じ欠陥を自分の証跡で再生産しないため、要点をここへ転記する**（gate 4 の指摘で気づいた）。

**3レーンが独立に一致した指摘（すべて反映済み）**:

- **`promotion` と closure 散文の件数が食い違っていた。** 3レーンとも数え直して不一致を挙げた。**当初 `required` 9 / `not-required` 5 と書いていたが、実数は 10 / 4 だった**（数え間違いである）。現在は 19 件で 12 / 6 / 1 に揃えた。
- **撤回済みの設計が `required` になっていた。** 「レコーダーを別プロセスにして…」は本文が後に不要と確定した案で、subject が採用アクションの形だった。**durable なのは技術的性質（抑止文脈はプロセス境界を越えない）**なので subject を性質へ書き換えた。
- **`事実・失敗` に本文の失敗が項目として現れていなかった。** Step 5（レーン不返却）・Step 9（統括が中心の書き落としを拾った）・Step 11（検査側の誤り2件）・Step 12（Sensor 欠陥4件）が落ちていた。**主要5件から全件へ広げた。**
- **`oe-send --help` を昇格判定の欄に置いたのは分類の誤り。** 本単位の判断ではないのでエントリから外し、routing だけに残した。

**レーン単独の指摘（反映済み）**:

- **コミットメッセージの件は過剰収穫**（3レーンが一致・`required` から `not-required` へ降格した）。
- **印の無い判断がまだ2件あった** — 観測器を期待集合の側から書く（claude）／ツール判別を `$0` で行う（cursor）。**どちらも自分の1周では出なかった。**
- **既存 item の引用が2箇所で誤っていた** — FIFO の既出根拠と、除外条件を収穫基準に転用した箇所（claude）。両方直した。
- **`01KZ21M0N0FZ4YMR2R6DPHTWQ4` への観測を、無関係な理由で見送っていた**（claude）。下記のとおり1件足した。
- **「PATH に無い」の断定が `orchestration-toolkit` の `command -v` 規範と衝突する**（claude）。配布対象は将来変わりうるので、断定を落として確認する形へ寄せた。
- **evidence anchor の主張が過大**（codex / cursor）。回復不能なものを名前で挙げる形に直した。

**反映しなかったもの（不同意・理由を書く）**: **収穫を follow-up へ回す判断**に3レーンが程度の差はあれ異を唱えた。判断は保持し、理由を差し替えて上の「収穫を分離した理由」に書いた（claude も「結論は別の根拠で立つ」として同じ差し替えを提案している）。**owner が覆してよい。**

**本 episode 内で走った3回の SO（gate 1 / 2 / 4）を Step 4 の代替に数えていない。** それらは設計とコードを対象にしたもので、closure 品質は検証対象が別である。

### 観測の書き戻し（段5・条件付き・**1件だけ足した**）

**完全な書き戻しはしない。** 上の `unknown` に書いたとおり、注入された item の集合（分母）が復元できないので、「全 item に書き戻した」とは主張できない。

**しかし1件は足した**（gate 4 の指摘で判断を変えた）。当初は「分母が復元できないから何も書かない」としていたが、**それは `unknown` の過剰適用だった。** 分母が言えないことは網羅を主張できないという話であって、**本文が id を名指ししたうえで帰結まで書いている具体1件を抑止する理由にはならない。** 抑止したままにすると、測定台帳が理由なく1データ点を失う。

- **`01KZ21M0N0FZ4YMR2R6DPHTWQ4`（検査の合格条件を累計の実績で書くな）に1レコード足した。** 本文 `本文: Step 12 — gate 4（実装SO）が、塞いだつもりの穴をもう一度開けて見せた` が「これは NK item `01KZ21M0N0FZ4YMR2R6DPHTWQ4` の鏡像である」と id つきで書き、ok 側では避けたのに broken 側で同じ構造を作ったことまで記録している。**`state: followed`**（窓を行の `ts` で切る形へ実際に直した証拠が本文にある）とし、**適用が片側だけで反対側は外部指摘を待った事実**と、**slot 経由の注入かは確認できない事実**を `note` に明記した。`validate-knowledge` は通っている。
- **`01KZ3MHMEQN5XEJGV9809QPH2A`（1箇所直した緑は型が消えた証拠ではない）には書かなかった。** #309 の FIFO ガードの件はこの item の型そのもので、同じ族の5件目・かつ「隣を見つけたのは外部ラウンド」という中核パターンにも一致するので、**観測としての価値はいちばん高い。** それでも書かなかったのは、**観測台帳が「注入された knowledge の帰結」を記録する場と定義されており、この item が注入された形跡が本文に無い**ためである（4件の名指しにも含まれない）。前提を確かめずに書くと、台帳の意味を薄める。**行き先＝親へ引き渡し、owner が「注入の有無を問わず型の再発を記録してよい」と決めるならその単位で足す。**

**残る4件（`01KYMRE1N7N0J8VP3CBZZEZ8Q3` / `01KYA7C9NN4VDM7H2NYXZB2PSX` / `01KYA7C9M4EN1EZM3HBEN5WDRP`）については書いていない。** 本文は使った痕跡を書いているが、注入経路（brief の slot か、owner の名指しか、子の自発か）が区別できないので、`followed` を自己申告で足すと精度の無い記録になる。**行き先は上の follow-up routing に書いた。**

**本単位が学んだ形をここに1行残す**: 期待集合の1行（注入された item id）を episode か PR 本文に書くのは、brief が消える前に分母を durable にする唯一の場所である。それが無いと、後から誰も履行を判定できない。**これは上の `required` に挙げた「観測器は在るものを数えるのではなく期待する集合を先に置く」と同じ型で、#309 が Sensor の側で踏んだものを、この closure が台帳の側で踏んでいる。**
