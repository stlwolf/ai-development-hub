---
id: "01KZNMB5ZCXBZAYKVSA2RH6NTG"
title: "#291 — 報告に依存しない親側の検知（範囲を1点に絞った再開・設計単位）"
date: 2026-08-10
type: episode
status: stable
related:
  - type: parent_issue
    ref: "https://github.com/stlwolf/ai-development-hub/issues/291"
    reason: "本単位の起点。#291 のうち『報告に依存しない親側の検知』の1点だけを引き受ける。起票し直さず範囲を絞って再開する"
  - type: design_context
    ref: "projects/orchestration-engine/docs/discussions/2026-07-29-discussion-289-completion-gate-firing-position.md"
    reason: "この絞り方の根拠。#289 が『スコープ内に非循環な発火位置は存在しない』へ到達し、循環の外に居るのは親だけだという構造的発見を残した"
  - type: refs
    ref: "https://github.com/stlwolf/ai-development-hub/issues/301"
    reason: "定期実行の配線と exit 帯の整理。本単位のスコープ外だったが、結論として #291 から分離できないことが判明した先"
tags: [orchestration, delegation, detection, engine, negative-knowledge]
promotion:
  - subject: "「観測は生 fact まで・解釈はしない」（DJ-188-2 / DJ-206-2）の着地が発見されにくく、同じ engine を触る担当が独立に再発見した"
    verdict: required
    ref: "本文: 潰されたカテゴリの地図（9レーン分）"
  - subject: "実害を再定義したら、新しい述語が元の目標事例と交わるかを確かめる"
    verdict: required
    ref: "本文: 結論 — スコープの切り方そのものに問題があった"
  - subject: "呼ばれる保証の無い台帳を増やすと Sensor が埋まったと誤読される型が3本目に達しようとしている"
    verdict: required
    ref: "本文: 結論 — スコープの切り方そのものに問題があった"
  - subject: "brief の報告コマンドが裸呼び出しで exit 1 になる（`oe-send` は PATH 未配布）"
    verdict: not-required
    ref: "本文: 報告経路そのものの欠落を疑って、外れた（追いかけた価値のある空振り）"
  - subject: "反証3ラウンドすべて refuted の時点で押し通さず止めた"
    verdict: not-required
    ref: "本文: 反証3ラウンドで止めた判断"
---

# #291 — 報告に依存しない親側の検知（設計単位）

## なぜこの作業が始まったか

委譲した子が作業を終えても親へ報告しないことがある。親はそれに気づけないまま、その子を「まだ動いている」と思って待ち続ける。この経路を塞ぐ層は #289 で探索されたが、#289 が指定したスコープ（skill と運用で効く範囲）の内側には**非循環な発火位置が無い**という構造的発見に到達して park された。

報告するかどうかを決めるのが子自身であるかぎり、判定は循環している。循環の外に居るのは親だけである。だから本単位は「報告に依存しない親側の検知」の1点だけを引き受ける。**報告を発火させる機構は扱わない**（それは #291 の残りである）。

先行する #291 の plan（Alt-7 承認時の義務コンパイル）は `halted-unconverged` で止まり、owner が要件表ごとの破棄を受け入れている。だから全部やり直さず、範囲を1点に絞った再開として始めた。

## この単位の終端

plan を作って STOP し、パスを親へ報告した時点で終わる。実装は owner の gate 3 を通ってから、親が位置を指定する。**この episode はその設計単位の記録である。**

## 進行中の記録

### brief の一次データを自分で測り直した

brief は「上の表と `%cpu` の値は統括の実測である。自分の成果物へ写す前に確かめよ」と指示していたので、`~/.claude/state/oe-events.jsonl` から自分で数え直した。親 `%287` が spawn した子について、親→子の `message_sent` を指示、子→親の `message_sent` を報告として数えた。

| 子 | label | 受けた指示 | 送った報告 | brief の値 |
|---|---|---|---|---|
| `%288` | 305-fact-a | 1 | 1 | （記載なし） |
| `%289` | 305-fact-b | 1 | 1 | （記載なし） |
| `%293` | #288 | 5 | 3 | 5 / 3 一致 |
| `%300` | #309 | 4 | 4 | 4 / 4 一致 |
| `%304` | #309b | 2 | 2 | （記載なし） |
| `%308` | readme-enum | 1 | 0 | 1 / 0 一致 |
| `%309` | exit-contract | 4 | 0 | 4 / 0 一致 |
| `%314` | liveness-recipe | 2 | 2 | 2 / 2 一致 |

brief が挙げた5件はすべて一致した。表に無い3件も同じ向きの傾向を示している。

### 数え方の比率をそのまま述語にすると誤検出する（brief の読みへの訂正）

比率だけでは足りないことが、時系列を並べた時点で分かった。`%293` の 5/3 は**報告を2回落としたのではない**。

```text
%293  I 14:20:07  R 14:59:16  I 18:16:46  R 18:22:31  I 18:23:38  I 18:28:16  I 18:28:50  R 19:07:40
%309  I 12:59:16  I 14:22:39  I 18:07:43  I 08:17:54(翌日)
%300  I 18:48:41  R 12:26:07  I 17:19:33  R 17:30:27  I 19:06:15  R 19:13:53  I 07:01:17  R 07:35:46
```

`%293` は5分の間に3件の指示を受け、その39分後に1件の報告を返している。**複数の指示を1件の報告にまとめた形**であって、報告の欠落ではない。いっぽう `%309` は4件の指示に対して1件も返していない。

つまり「受けた指示 > 送った報告」という述語は、**まとめて返した子**と**一度も返さない子**を同じ扱いにする。前者は正常な振る舞いなので、比率を検知の材料にすると誤検出になる。検知は「最後の指示以降に報告が1件も無い」側で組む必要がある。

brief の「ほぼ二値・最初の1回が予測子」という読みは、一度も返さない子（`%309`）と毎回返す子（`%300`）の対比としては測定と一致している。`%293` を「3回中2回落とした」と読む部分だけが測定と合わない。

### 対象の子は既にペインごと消えている

これが設計の形を変えた。目標事例の `%309`（exit-contract）と `%308`（readme-enum）は、**仕事そのものは完遂している**。

- `%309` の成果は PR #315（2026-08-10T09:02:48Z マージ）。最後に受けた指示は同日 08:17:54Z。
- `%308` の成果は PR #314（2026-08-09T17:32:49Z マージ）。

そのうえで報告を1件も送っていない。そして現在、`tmux list-panes -a` に `%288` `%289` `%293` `%300` `%304` `%308` `%309` `%314` はいずれも存在しない。

したがって、この型を検知しようとする時点で **`delegate-task` の判定レシピの信号 (a) プロセスの生死と (b) `%cpu` の絶対値は、そもそも引く対象が無い**。残るのは (c) 終端成果物と、活動ログである。停止の形は「アイドルで生きている」と「ペインごと消えた」の2つに分かれ、実測された事例は後者だった。

### tmux display-message は不在ペインでも成功する（新規に測った罠）

判定レシピの信号 (a) を素直に書こうとして踏んだ。

```text
pane=%288     rc=0 len=0
pane=%309     rc=0 len=0
pane=%99999   rc=0 len=0
pane=%318     rc=0 out=[89907]   ← 対照（実在）
```

`tmux display-message -p -t "%N" '#{pane_pid}'` は、**ペインが存在しなくても終了コード 0 と空文字を返す**。`=%288` と厳密指定しても同じだった。存在しない番号 `%99999` でも同じである。

そのため `rc` を存在判定に使うと、消えた子を「生きている」と読む。さらに空の pid をそのまま `ps -p` へ渡すと `ps` 側が不定の文字列を掴んでエラーを出すので、**エラーの出どころが tmux ではなく `ps` に見えて原因を取り違える**（最初にこれを踏んだ）。

engine の既存実装は既にこれを避けていて、`oe-undelivered:145` は `tmux list-panes -a -F '#{pane_id}'` で集合を取ってから所属を見る形になっている。検知を書くときはこちらへ揃える。

### `oe-undelivered` がこの型を見られないことの確認（陽性対照つき）

brief の主張を、0件を不在の証明に使わない形で確かめた。窓を60秒・起点を全期間にして実行したところ、**25行以上を出力した**（`exit=0`）。つまり投影は生きていて、出力が空だから何も出ないのではない。

そのうえで、出力に `%309` と `%308` は**1行も無い**。理由は母集団の定義である。`oe-undelivered` が数えるのは「送られた `message_sent`」なので、**一度も送っていない子は母集団に入らない**。窓を広げても起点を戻しても、この型は原理的に出てこない。

副産物として、報告の受領印がほぼ機能していないことも確認できた。通算で `child_spawned` 118件・`message_sent` 880件（親→子 474 / 子→親 382）に対し、**`report_received` は通算1件**である。`prompt_received` は101件。したがって「親が受領印を打っていないこと」を報告の不在の根拠に使ってはいけない。使えるのは子→親の `message_sent` の有無だけである。

### 活動ログの読み側で気をつける傷

- スキーマ違反のレコードが1件ある。`role` は `parent` / `child` / 空 の enum なのに、`2026-07-01T11:18:29Z` の1件は `role` に label（`#92 verify-commit-range-inputs`）が入っている。
- `role` が空文字の `message_sent` が23件ある。これはスキーマが「関係不明」として許している正当な値である。

どちらも `role` を単独の向き判定に使えないことを意味する。向きは `child_spawned` の親子対応から引くのが確実で、これも `oe-undelivered` が既に採っている複合解決と同じ方針になる。

### 報告経路そのものの欠落を疑って、外れた（追いかけた価値のある空振り）

brief は報告の呼び方を `oe-send "$PARENT_TMUX_PANE" '...'` と裸で書いている。これを素直に実行したら **exit 1** になった。`oe-send` は `~/bin` に配布されていない。

- 配布対象の正本 `scripts/sync/sync-bin.sh:57` の `CMD_NAMES` は8件で、`oe-*` は `oe-tree` と `oe-hookfire` の2本だけである。
- `orchestration-toolkit` はこれを不変条件として明記している。「`oe` / `oe-*` は原則 PATH 未登録」で、起動方法を決める前に `command -v` で確かめよと書いてある。
- `delegate-task` の例示は `"$BIN/oe-send"` の形で正しい。裸で書いているのは brief 側である。

そこで「未報告の原因は、報告しようとしたのにコマンドが無くて落ちたことではないか」を疑った。**これは外れた。** 順に確かめた。

1. `%308` の transcript には「`oe-send` / `oe-selfcheck` は PATH に無い」という記述が確かにある。しかしこれは `%308` の担当が配布 CLI の列挙だったからで、**報告を試みて失敗した記録ではない**。同じ transcript の Bash tool_use を全部見ると、`oe-send` を含むコマンドは **0件**である。試していない。
2. hub 系の transcript 20ディレクトリを横断して、tool_result に現れる失敗文字列（`oe-send: command not found` / `oe-send: No such file`）を数えたところ **2件**だった。しかもこのうち少なくとも1件は**本セッションで私自身が今日出したもの**である。
3. 陽性対照として同じ探し方で成功記録（`oe-send: sent to`）を数えると **486件**取れた。探し方が壊れていて0に見えているのではない。

したがって **PATH の欠落は brief の書き方に潜む本物の罠ではあるが、観測された未報告の原因ではない。** 子は「送ろうとして失敗した」のではなく「一度も送っていない」。

この空振りには意味がある。未報告が**配送の失敗ではなく試行の不在**だと確かめられたので、配送側の検出器（`oe-undelivered` の系列）をいくら強くしてもこの型には届かない。試行が無いことを見られるのは、試行を期待している側＝親だけである。#289 が構造として述べたことが、ここで測定として裏づけられた。

昇格の印: brief に書かれた報告コマンドが裸呼び出しで exit 1 になる。委譲の固定節の側で `$BIN/` 形に直す価値がある（本単位のスコープ外・親へ surface する）

### 3ラウンドの反証がすべて refuted で終わった

gate 1（ゼロベース代替探索）を2ラウンド、gate 2（設計SO）を1ラウンド、いずれも3レーン（codex / claude / cursor）で回した。**計9レーン判定がすべて `refuted`。** ラウンドごとに material な新カテゴリと事実の訂正が出続け、収束の兆しは観測できなかった。

| ラウンド | audit_id | 崩れた主張 |
|---|---|---|
| 1 | `20260810105748903MP7ECCNNE` | `Stop` hook を完了の材料にする案 / 「ハーネスが撃つから循環の外」 / 親が状態を分類する案 / `broken` を事故発見に使う案 |
| 2 | `20260810111127CWT8JP2FGD54` | マージ済み PR を完遂の証拠にする案 / `pane-issue` に遡及性がある主張 / 判定3値と exit 帯の混同 |
| 3（設計SO） | `20260810112201X42NFH400FKZ` | 「到達不能 ∧ 送信0」述語 / observer 族は常に exit 0 という前提 |

**指摘は全件、自分で一次確認してから受け入れた。** 以下は確認済みの否定的事実で、この episode に置くのは **plan と探索木が gitignored な `.oe/` に在り、worktree の掃除で消えるため**である（#284 の arc が同じ位置で設計級を破棄し、復元に別 PR 1本を要した）。

| 主張 | 確認結果 |
|---|---|
| `Stop` hook は完了を意味しない | 公式 hooks ドキュメントで cadence が3分類され、`Stop` / `StopFailure` / `UserPromptSubmit` は once per turn、`SessionEnd` は once per session。`SessionEnd` はブロック不可・1.5秒の共有予算・**SIGKILL 時の発火は文書化されていない** |
| `Stop` hook の発火は一次未確認 | `notify.sh:103` はデバッグ有効時のみ記録し、そのファイルは存在しない。配線は確認できるが発火実績が無い |
| 親が観測から子の状態を分類する設計は**既に棄却済み** | `oe-activity:14` が「lifecycle-end / stall は推論しない ＝ DJ-188-2 尊重」。episode 206 は「最終イベント + mux 生存 query で終了 vs stall を分類する viewer」を落とし、理由を「対話子の非対称 lifecycle を engine 完了 enum に押し込む category error を観測側に持ち込まない」と記録している |
| `pane-issue` は遡及に使えない | `scripts/wt/wt-pane-issue.sh:60` が `-mmin +1440 -delete`（**24時間 GC**）。実件数6件・最古が前日 |
| マージ済み PR は遅行指標 | 人間ゲートの下流なので事故の最中には真にならない |
| 「送信0」から「報告しなかった」は導けない | `lib/event-bus.sh:88` は best-effort で常に return 0。かつ `delegate-task` 自身が「記録が無いことは証明にならない」とこの推論を禁じている |
| 「到達不能」は既に払ったバグの再輸入 | `oe-vitals:26-32` に設計SO 3レーンの反証記録があり、death は**確定 gone のみ**に絞られ、ペイン単位の3値（alive / gone / `?`）が正しい形と決まっている |
| observer 族は常に exit 0 ではない | `oe-undelivered:309` は投影失敗で exit 1、`oe-vitals` は常に0。**族として揃っておらず、揃っていない一点が今回の判断対象そのもの** |

### 自分の推論が2回崩れた

記録しておく価値があるのは、外から崩されたものより**手元に証拠があったのに推論しなかった**2件である。

1. **`pane-issue` の被覆。** 6件しか無いことを自分で印字していたのに、「119件の spawn に遡って効く」と書いた。6 対 119 が刈り取りを意味することを読まなかった。陽性対照が通ったのは目標事例が直近24時間内だったからで、**生存者バイアスだった。**
2. **PR オラクルの向き。** 「PR がマージ済み」を完遂の証拠として喜んだが、マージするのは owner である。**owner が既に関与した後でしか真にならない指標**を、owner に気づかせるための検知器の土台に置こうとしていた。

どちらも「測れた」ことに満足して「何を測ったのか」を問わなかった型である。

### 結論 — スコープの切り方そのものに問題があった

3ラウンドを経て残ったのは、実装可能な設計ではなく構造的発見だった。

> **本単位のスコープアウト（定期実行の配線＝#301）と、#291 が要求するもの（完了扱いの起動の保証）は分離できない。**

呼ばれない検知器は「親が起動を覚えている必要がある」分類に落ちる。#289 がその分類を**循環として8レーン全 refuted で棄却**している。したがって受け入れ条件「起動が親の記憶に依存しない形で保証されている」は実装した瞬間に未達が確定する。しかも同じ但し書き（「この台帳の値打ちは読み手が実際に走ることに条件付けられている」）を持つ verb が既に2本ある。

さらに、到達した述語は連言に「送信0」を含むため、**#291 の事例1（報告を送って終わった委譲）を構造的に排除する**。#291 が名指した事故とは別の事故を捕まえており、範囲の縮小ではなく別問題への乗り換えである。

**この2点は、範囲を1点に絞って再開したこと自体への反証である。** brief は「全部やり直さない・範囲を1点に絞る」と指示したが、絞った1点は単独では要求を満たせない位置にあった。

昇格の印: 「観測は生 fact まで・解釈はしない」（DJ-188-2 / DJ-206-2）を本単位が独立に再発見した。engine 横断の規範として昇格を検討する価値がある

昇格の印: 「呼ばれる保証の無い台帳を増やすと Sensor が埋まったと誤読される」型が3本目に達しようとしている。#301 の判断材料として蒸留する価値がある

## 潰されたカテゴリの地図（9レーン分）

**次に同じ設計をやる人のための地図である。** plan 本体は owner 裁定で破棄したので、探索木の内容のうち再利用できる部分をここへ取り込んだ。

読み方を先に書く。**「潰れた」と「未探索」を混ぜないこと。** 潰れた枝は理由ごと避けてよいが、未探索の枝はまだ生きている。とくに案P は3ラウンドを通じて一度も潰されておらず、**最有力の未探索候補**である。

### 潰れた枝

| 案 | 何をしようとしたか | なぜ潰れたか |
|---|---|---|
| **親が live 状態をポーリングして停止を推論する**（初期案の全体） | 最終イベントとペイン生存から「終了 vs stall」を分類する | **engine が既に棄却済み。** `oe-activity:14` が「lifecycle-end / stall は推論しない ＝ DJ-188-2 尊重」。episode 206 が同じ設計を落とし、理由を「対話子の非対称 lifecycle を engine 完了 enum に押し込む category error を観測側に持ち込まない」と記録している。観測は `alive` / `gone` / `?` の生 fact に留めると決まっている |
| **`%cpu` とプロセス生死で判定する**（レシピの信号 a / b） | 委譲子の状態判定レシピをそのまま当てる | **目標事例では引く対象が無い。** 対象の子はペインごと消えており、pid も `%cpu` も取れない。レシピが有効なのは live な子に対してだけである |
| **`Stop` hook で durable な停止印を出す**（Z3） | 停止の観測を親のポーリングから子ハーネスの emit へ移す | **`Stop` は公式契約で once per turn**（`SessionEnd` が once per session）。得られるのは「アイドルに入った時刻」までで、ゲート待ち・質問待ち・作業途中・真の終端が同じ帯に入る。加えて現行の `Stop` hook は発火記録を1行も書かないので発火実績が一次で確認できない |
| **「ハーネスが撃つから循環の外」という論拠** | モデルの判断を経由しないことを非循環の根拠にする | **モデル判断の外だが故障領域の外ではない。** `decision:"block"` / exit 2 で拒否できる、API エラー時は `StopFailure`（出力と exit を無視）、Codex は対話で発火しない既知ギャップ、worktree からの sync でフックのリンクが dangling になる、`lib/event-bus.sh:88` は `jq` 不在や追記失敗を成功扱いの no-op にする |
| **マージ済み PR を完遂の証拠にする**（案C） | 完遂の事実を親でも子でもない外部台帳に取る | **人間ゲートの下流なので事故の最中には真にならない**（遅行指標）。マージするのは owner なので、owner に気づかせるための検知器の土台に、owner が既に関与した後でしか真にならない指標を置くことになる |
| **`pane-issue` state を遡及の起点にする** | ペイン消滅後も残る durable な label → branch の橋として使う | **24時間 GC がある**（`scripts/wt/wt-pane-issue.sh:60` が `-mmin +1440 -delete`）。実件数6件・最古が前日。標的の上でだけ消えるので、遡及性は成立しない |
| **`registry` の workspace から branch を導く** | 子の作業場所から branch と PR を引く | **ペイン消滅で record ごと消える。** 実測で registry は7件すべて alive で、`child_spawned` 通算119件に対して残っていない |
| **`broken` を「未報告の疑いがある子」に使う** | `oe-selfcheck` / `oe-hookfire` と同じ3値契約に揃える | **意味が反転する。** 姉妹 verb の `broken` は「センサーの前提が壊れており観測を信用できない」であって事故の発見ではない。cron から見て engine 内で exit 1 が2通りの意味を持つ |
| **observer 族は常に exit 0 だから揃えればよい** | `oe-undelivered` / `oe-vitals` に倣う | **族として揃っていない。** `oe-undelivered:309` は投影失敗で exit 1、`oe-vitals` は常に0。しかも揃っていない一点が今回の判断対象そのものだった。帯の整理は #301 の議題 |
| **判定3値を捨てて observer 型にする** | exit を揃えるために3値をやめる | **判定3値と exit 帯は直交する。** #315 が変えたのは help と呼び方の誤りを `broken`(1) の帯から外したことだけで、判定の3値は保持されている |
| **「到達不能 ∧ 送信0 ∧ 非統括」述語** | 完遂の立証をやめ、実害（二度と喋らない子）で組む | **#291 の事例1 を構造的に排除する。** 事例1 は報告を送っている。連言に「送信0」があるので定義上ひとつも挙がらない。加えて「送信0」自体が欠落を黙認する記録の上に立てた否定である（`event-bus` は best-effort で常に return 0・`delegate-task` 自身がこの推論を禁じている） |
| **「到達不能」を連言に入れる** | 二度と喋らない子だけを拾う | **brief が指定した母集団を捨てる。** 再開 brief の対象は「**停止していて**成果物が在り報告が無い子」＝生きたままアイドルで黙る子を含んでいた。到達不能を必須条件にすると、その母集団が構造的に落ちる。3ラウンド目の cursor レーンの指摘 |
| **通知なし・cron なしで observer 常時0 に寄せる** | v1 を stdout のみの観測に絞る | **#309 F2 / #299 の方向と衝突する。** 発火や到達を記録する側の整備が進んでいるのに、こちらだけ「記録するが誰も読まない」形に寄せることになる。3ラウンド目の cursor レーンの指摘 |
| **「送信0」を連言に使う** | 一度も喋っていない子だけを拾う | 事例1 の排除（下記）に加えて、**途中まで送って最後の報告だけ落とした子が偽陰性になる。** 実測でも 5指示/3報告のように部分的に送る子が居る |
| **「分類は無状態でよい」** | ログから毎回再計算すれば状態を持たなくて済む | **通知の重複抑止と分けないと成立しない。** 姉妹 verb（`oe-undelivered` / `oe-vitals`）はどちらも二重通知を抑えるための seen cache を持っている。cron で回すと同じ陽性を毎回鳴らす。1ラウンド目の指摘 |
| **「到達不能」を1つの条件として書く** | ペイン一覧に居ないことを到達不能とする | **既に払ったバグの再輸入。** `oe-vitals:26-32` に設計SO 3レーンの反証記録があり、`?`（tmux 不在 / socket 不通）を `¬alive` として死扱いすると偽 ping を出すため、death は**確定 gone のみ**に絞られている。ペイン単位の3値が正しい形 |
| **「子を spawn したか」で統括を除く** | 振る舞いから統括を判定して継承を誤検出から外す | **宣言層が既にある。** `oe-vitals` は board の `現統括:` 宣言を消費している。振る舞いからの推測は弱く、**統括を兼ねる実装子を取りこぼす** |
| **「受けた指示 > 送った報告」を述語にする** | 比率で未報告を拾う | **正常形を誤検出する。** ある子の 5/3 は報告落ちでなく、5分内の3指示を1報告にまとめたものだった |

### 未探索のまま残る枝（生きている）

| 案 | 中身 | なぜ生きているか |
|---|---|---|
| **案P 親宣言の待ち期限** | spawn / kick 時に `expect_report_by` を焼き、期限超過かつ報告なしを見る | **3ラウンドを通じて一度も潰されていない。** 子の停止印が不要になり、`Stop` の意味論・`pane-issue` の GC・PR の遅行性のいずれにも依存しない。**最有力** |
| 案W wrapper / supervisor | 子を wrapper 配下で起動し `wait` の終了コードと signal を外側から永続化する | 「親のポーリング」でも「子の自己報告」でもない第三カテゴリ |
| 案X tmux `pane-died` / `remain-on-exit` | tmux server 側の hook を使う | 同じく第三カテゴリ。子の設定・trust に依存しない |
| 案T contract-first envelope | spawn 時に `delegation_id` + 終端 step ID + 成果物 path を機械可読に発行する | 成果物条件が供給されない根穴を塞ぐ。ただし発注側の義務なので別単位 |
| 案G 子の transcript | ハーネスが書く会話記録を完了の証拠に使う | ペイン消滅後も残り、新しい emit が要らない。本単位でも実際にこれを使って事実を1件確かめた |
| 案E `SessionEnd` | ターン終了ではなくセッション終了を印にする | 意味は `Stop` より近い。ただし1.5秒の共有予算があり、SIGKILL 時の発火は文書化されていない |
| `oe-heartbeat` | `~/.claude/state/oe-heartbeat`（#239）に同じ事実がある | 台帳として未探索 |

### 先に読むべき制約（ここを踏むと同じ道を辿る）

- **durable な基盤は活動ログだけである。** ペイン存在・pid・`%cpu`・workspace・branch はすべて live のみ。終端成果物の path はどこにも無い（brief の散文のみで、**実際に発行された brief 自身が機械可読な path を持たない**）。
- **親の受領印は信号として死んでいる。** 通算1件。報告の不在の根拠に使ってはいけない。
- **`tmux display-message` は不在ペインでも rc=0 と空を返す。** 存在判定は `tmux list-panes -a -F '#{pane_id}'` の集合所属で行う（`oe-undelivered:145` が既にこの形）。
- **相関キーが無い。** `child_spawned` に session / generation ID が無く、ペイン ID 再利用の既知制約がある。#247（pane 突合 → session_id 突合）が open なので、ペインを鍵にした検出器の新設はこの移行を追い越す。
- **委譲子だけを母集団にするには2つ除外が要る**（素朴な述語を全 spawn に当てて実測した結果。述語自体は潰れたが、この2つはログの構造についての事実なので残す）。
  1. **統括の継承**: 前任統括が起こした後任統括は前任へ報告しない（引き継ぐ）。報告0 は正常である。`%144` は多数のペインへ送っており、送信先の広がりから統括だと分かる。
  2. **再親化**: ある子は spawn 元へは0件だが、後任統括へは13件送っていた。**親だけを見る述語はこれを誤検出する。**
- **plan-first の子は完遂の外部痕跡を持たない。** PR も commit も作らないので、終端成果物が報告そのものになる。この型では「報告が無い」＝「終わっていない」であり、外部台帳で完遂を確かめる道が無い。

## owner 判断項目の取捨（plan §6 の D1〜D5）

plan は破棄するので、**枠の切り方に依存せず再利用できるものだけ**をここへ残す。落としたものも1行書く（黙って消さない）。

### 残すもの

- **D4 — exit 帯の不揃いをどう扱うか。** `oe-undelivered:309` は投影失敗で exit 1、`oe-vitals` は常に0で、observer 族として揃っていない。かつ #315 の後も 2 の帯が「呼び方の誤り」と `indeterminate` で共有されたままである。**この不揃いは本単位の枠と無関係に実在する**ので、#301（帯の整理）の判断材料としてそのまま生きる。
- **D5 — brief の報告コマンドを直すか。** `oe-send` の裸呼び出しは exit 1 になる（`~/bin` 未配布・`sync-bin.sh:57` の `CMD_NAMES` に無い）。委譲固定節を `$BIN/` 形へ直すか、`oe-send` を配布対象に足すかの選択で、**どの検知設計を採るかとは独立**である。
- **D2 の一般形 — 述語が目標事例と交わるかを確かめる。** 具体の述語は破棄したが、確かめる手順自体は枠に依存しない。knowledge item として収穫した（下記 closure）。

### 落としたもの（理由つき）

- **D1（スコープの切り方を維持するか）** — owner が本裁定で「halted-unconverged の記録単位として閉じる」と決めたので、選択肢を提示する形の問いとしては消滅した。**判断そのものは構造的発見として本文に残っている**ので、情報は失われていない。
- **D3（3ラウンド全 refuted で止めた判断を承認するか）** — 同じく本裁定で決着した（閉じる方向）。
- **plan §5 の測定できた述語（8/119 件）** — 述語ごと潰れたので数値も持ち越さない。ただし**その数値が偽陽性率の見積もりに使えない理由**（「送信0」が欠落を黙認する記録の上の否定であること）は上の地図に残した。

## closure

`episode-retrospective` の Step 1 判定は **heavy**（実行中の撤回あり・意図的に起動した外部レビュー9レーン・主成果が知見・棄却した選択肢あり・昇格候補あり）。

### closure gate checklist

- **Context / なぜ**: 冒頭に自己完結で書いている。`本文: なぜこの作業が始まったか`
- **次の消費者**: (1) この層を次に設計する担当（`本文: 潰されたカテゴリの地図（9レーン分）` を地図として読む）。(2) #301（exit 帯の整理と定期実行の配線）。(3) #291 を再スコープするときの owner。
- **follow-up routing**:
  - 検知の述語を作り直す / #291 を再スコープする → **#291 に残す**（keep-open。受け入れ条件は手つかず）
  - exit 帯の不揃い・定期実行の配線 → **#301**
  - `oe-send` の PATH 未配布と brief 固定節の書き方 → **直す場所を特定した**（`canonical/skills/doc-flow-guardrail/SKILL.md:71` の報告2段構えの行が裸呼び出しのまま）。**本単位では追わない。** 理由は配布される canonical への変更が本単位のスコープ外であることで、起票するかは owner 判断に残る。**追わない宣言の記録はこの行が正本である。**
  - 案P / 案W / 案X / 案T / 案G / `SessionEnd` / `oe-heartbeat` の探索 → **#291 に残す**（上の地図が入口）
  - #247（pane 突合 → session_id 突合）との順序 → **#247 に前方参照**（ペインを鍵にした検出器を作る前に決める）
  - 追わないもの: 本単位で作った述語の実装。**述語ごと潰れたので追わない。**
- **昇格の判定**: frontmatter の `promotion` に5件（`required` 3件 / `not-required` 2件）。`subject` は下と逐語一致させている。
  - 「観測は生 fact まで・解釈はしない」（DJ-188-2 / DJ-206-2）の着地が発見されにくく、同じ engine を触る担当が独立に再発見した → `required`。`本文: 潰されたカテゴリの地図（9レーン分）` — 棄却した案が具体名で挙がり（分類する viewer）、覆すには設計原則の再戦が要る。既存の着地は code header のコメントと episode の散文だけで、**同じ engine を触る担当（私）が独立に再発見するまで3ラウンド費やした**。discoverability が不足している証拠が本単位そのものである。置き場は decision。
  - 実害を再定義したら、新しい述語が元の目標事例と交わるかを確かめる → `required`。`本文: 結論 — スコープの切り方そのものに問題があった` — 再発する失敗の型なので置き場は knowledge item。**本 PR で収穫した**（`01KZNWK084YF0RP48337VFYMQQ`）。
  - 呼ばれる保証の無い台帳を増やすと Sensor が埋まったと誤読される型が3本目に達しようとしている → `required`。`本文: 結論 — スコープの切り方そのものに問題があった` — 置き場は #301 への判断材料（decision 級の政策判断を含む）。**昇格の実行は本スキルの外**なので判定のみ。
  - brief の報告コマンドが裸呼び出しで exit 1 になる（`oe-send` は PATH 未配布） → `not-required`。`本文: 報告経路そのものの欠落を疑って、外れた（追いかけた価値のある空振り）` — 機械契約は既に `orchestration-toolkit` が「`oe-*` は原則 PATH 未登録・`command -v` で確かめよ」として正本に持っている。要るのは固定節の修正であって昇格ではない。
  - 反証3ラウンドすべて refuted の時点で押し通さず止めた → `not-required`。`本文: 反証3ラウンドで止めた判断` — `exhaustion-before-conclusion-rule` と `reframe-on-stall-rule` の既存規範の適用例であって、新しい判断ではない。
- **status 確定**: `stable`。達成度は **未達**（halted-unconverged。実装は無く、#291 の受け入れ条件は手つかず）。
- **evidence anchor**: `oe-refute` の `output_dir` は永続しないため、3ラウンドの verdict / audit_id / 各レーンの指摘内容を本文へ転記済み。`.oe/` の plan と探索木も消えるため、再利用できる内容を本文へ取り込んだ（本裁定の (1)(2)）。
- **SO 証跡**: 反証3ラウンドの audit_id は `本文: 3ラウンドの反証がすべて refuted で終わった`、Step 4 の外部チェックは `本文: Step 4 外部チェックの結果と反映`。
- **観測の書き戻し**: `本文: 注入された negative knowledge（Step 6・期待集合）`

### 注入された negative knowledge（Step 6・期待集合）

brief の slot に載っていた item は3件である。**この3件が分母である。**

- `01KZKWJM1KTAFN22XK0WP0F96J`（委譲子の状態判定レシピ）
- `01KYMRE1N7N0J8VP3CBZZEZ8Q3`（0件を不在の証明に使うな）
- `01KYJ76D6KY8RHD2EN3RQ654PK`（機械判定不能と結論する前に別の場所を探せ）

3件すべてに観測を1レコードずつ書き戻した（`state` はいずれも `followed`）。効きの詳細は各 item の `observations` にある。

### 事実・失敗

- 3ラウンド9レーンすべて refuted で確定に至らなかった。`本文: 3ラウンドの反証がすべて refuted で終わった`
- 自分の推論が2回崩れた（`pane-issue` の遡及性・PR オラクルの向き）。どちらも手元に証拠がありながら推論しなかった。`本文: 自分の推論が2回崩れた`
- 報告経路の欠落を疑って外れた。`本文: 報告経路そのものの欠落を疑って、外れた（追いかけた価値のある空振り）`
- brief の一次データの読みを1件訂正した。`本文: 数え方の比率をそのまま述語にすると誤検出する（brief の読みへの訂正）`
- **実行中の失敗**: `tmux display-message` の戻り値を存在判定に使い、空の pid をそのまま `ps` へ渡した。`ps` 側がエラーを出したので**原因を tmux ではなく `ps` だと取り違えた**。`本文: tmux display-message は不在ペインでも成功する（新規に測った罠）`
- **撤回**: 「分類は無状態でよい」を、通知の重複抑止と分けないと成立しないと認めて撤回した（1ラウンド目）。`本文: 潰されたカテゴリの地図（9レーン分）`
- **撤回**: 「到達不能」を連言に入れた述語が、brief が指定した母集団（生きたまま黙る子）を構造的に捨てていた（3ラウンド目 cursor）。`本文: 潰されたカテゴリの地図（9レーン分）`
- **素朴な述語を全 spawn に当てた際、誤検出源を2つ実測で発見した**（統括の継承・再親化）。述語は潰れたがこの2つはログの構造の事実として残した。`本文: 先に読むべき制約（ここを踏むと同じ道を辿る）`
- **Step 4 の外部チェックで closure 自体の不備が7件挙がり、すべて直した。** `本文: Step 4 外部チェックの結果と反映`

### 決定と根拠

棄却した案とその理由は地図に集約した。`本文: 潰されたカテゴリの地図（9レーン分）`

### わかったこと

durable な基盤は活動ログだけで、完遂の代理指標はすべて遅行・短命・粒度違い・不在のいずれかで崩れる。`本文: 潰されたカテゴリの地図（9レーン分）`

### 原則

**実害を再定義して述語を作り直したら、新しい述語が元の目標事例と交わるかを確かめる。** 交わらないなら、それは範囲の縮小ではなく別問題への乗り換えである。`本文: 結論 — スコープの切り方そのものに問題があった`

### 蒸留シグナル

decision（「観測は生 fact まで」の再着地）/ knowledge item（述語と目標事例の乖離・本 PR で収穫）/ #301（exit 帯・定期実行）/ #291 keep-open。

### 残課題

- 案P をはじめとする未探索6枝。行き先は **#291**。
- 「呼ばれる保証」を #301 なしで満たす方法は**見つかっていない**。見つからなかったと書いておく。行き先は **#301**。

## Step 4 外部チェックの結果と反映

heavy tier の外部チェックを `so-compare`（codex / claude の2レーン・どちらも実返却）で実施した。出力は `tmp/so-20260810-221259/`（**揮発するので下に要点を転記する**）。確認対象は closure 品質の4観点に絞った。

**両レーンとも「closure は未完」と判定し、計7件を挙げた。全件を直した。**

| # | 指摘 | 反映 |
|---|---|---|
| 1 | 「事実・失敗」節に選択的省略がある（`display-message` の誤読・素朴な述語の誤検出源2件・無状態の撤回・3ラウンド目 cursor レーンの指摘） | 5項目を追加し、それぞれ本文へ pointer を張った |
| 2 | 3ラウンド目の cursor レーンの反証（brief の母集団を到達不能連言が捨てる／observer 常時0 が #309 F2・#299 と衝突）が本文にも closure にも無い | 地図に2行追加した |
| 3 | frontmatter が gitignored で既に存在しない `.oe/plan-291-obligation-manifest.md` を参照している | `related` から外し、#301 への参照へ差し替えた |
| 4 | follow-up routing に行き先の無い項目が2件（`oe-send` の固定節・「呼ばれる保証」） | 前者は直す場所を `file:line` で特定したうえで「本単位では追わない」を明示、後者は #301 へ routing した |
| 5 | back-propagation が brief 止まりで、直せる場所を特定していない | `canonical/skills/doc-flow-guardrail/SKILL.md:71` を特定して記載した |
| 6 | closure の昇格判定行が frontmatter の `subject` を言い換えており、各項目に `本文:` pointer が無い | `subject` を逐語一致させ、各項目に pointer を付けた |
| 7 | promotion 1 と 5 の pointer が複合 subject の全体を支持していない | subject を pointer が支持する範囲へ書き直し、5 には対応する見出しを本文に新設した |

**この節自体が Step 4 の証跡である**（`so-compare` の出力ディレクトリは永続しない）。

## 反証3ラウンドで止めた判断

3ラウンド9レーンすべてが refuted で、ラウンドごとに material な新カテゴリと事実の訂正が出続けた。**収束の兆しが観測できない状態で確定へ進むことをしなかった。**

`predecision-exploration` の暫定停止条件は「ゼロベース SO で新カテゴリが出なければ確定に進んでよい」であり、本単位はその逆（出続けた）だった。打ち切りの上限は人間判断に属するので、`halted-unconverged` のまま owner へ上げる形を選んだ。**先行して park された #291 の plan も同じ理由で `halted-unconverged` で止まっており、扱いは一貫している。**

