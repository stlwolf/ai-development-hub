---
id: "01M28CQ6J3F3V5EG2J5FNK616R"
title: "統括の計画的な交代を verb にする（#390 plan フェーズ）"
date: 2026-09-11
type: episode
status: draft
related:
  - type: derived_from
    ref: "https://github.com/stlwolf/ai-development-hub/issues/390"
    reason: "この単位の正本。ゲート0 の結論が issue 本文にある"
tags: [orchestration-engine, succession, cockpit, plan]
---

# episode — 統括の計画的な交代を verb にする（#390 plan フェーズ）

> **この episode は PR を4本またぐ。closure は PR-4 のマージ前に行う**（owner 裁定・2026-09-11）。
> 途中の PR がマージされて master に載るあいだ、この episode は開いたままになる。**閉じ忘れではない。**
> #390 は `prepare` / `take` と `start` / `retire` / 文書と受入 の4本に分かれる単位で、PR-1 の時点で
> 閉じると、閉じた記録が作業の4分の1しか語らないものになるためである。
>
> 条件が3つ付いている。各 PR の作業はその都度ここへ追記する（closure を後ろへ動かすことで
> 追記の義務を緩めない）。この注記を冒頭に置く。**単位が途中で止まったら、止まった時点で closure する**
> （「まだ最後ではない」を理由に閉じない状態を続けない）。

## なぜこの作業が始まったか

統括セッションは context が尽きるたびに交代する。いまその交代は、立ち上げプロンプト 36 行と引き継ぎ brief 50 行を毎回人が手で書き、口頭で spawn を指示する形でしか回っていない。owner は 2026-09-11 に「統括自体をシームレスに復活できる仕組み・あまりプロンプトを使わないでできる形」を求め、cockpit の改善順序として「1. 通信経路を手厚く → 2. 交代と復帰」を裁定した。1 は #336 で着地したので、次がこの単位である。本 episode は #390 のうち **plan を作るところまで**を記録する。実装は owner の Human Gate（ゲート3）の後になる。

この単位は**前任が応答できる場合**の交代だけを扱う。前任が応答しない場合（クラッシュ後の復帰）は #355 で、分かれ目は前任が自分の状態を申告できるかどうかの1点である。

## 作業の記録

### 材料を読む（2026-09-11）

読んだもの: issue #390 本文（ゲート0 の結論を含む）/ #355 / #238 / discussion `2026-07-13-discussion-supervisor-succession-recovery-and-observability.md` / 一次材料の2ファイル（`.oe/prompt-succession-14th.md` 35 行・`.oe/brief-cockpit-handoff-14th-generation.md` 50 行）/ board の line 3 と succession 手順節 / engine の `bin/README.md` のうち `oe-delegate` `oe-send` `oe-register` `oe-selfcheck` `oe-vitals` `oe-refute` の節。

board は 350KB あるので全部は読んでいない（line 1-8 と 169-192 のみ）。

### ゲート1 — 初期案と暗黙の前提を並べる（2026-09-11）

SO を回す前に、いま手元にある案と、その案が黙って置いている前提を書き出した。

暗黙の前提（issue 本文の「設計の骨」と手動の引き継ぎの実物から抽出）:

- 前提1: 交代は1回きりの出来事で、渡す瞬間がある。
- 前提2: 渡すものは文書（brief と立ち上げプロンプト）で、誰かが書かなければならない。
- 前提3: 後継は新しい pane で立ち上がる新しいセッションである。
- 前提4: board（350KB の自由記述）が durable な正本で、brief はその要約である。
- 前提5: 席は board の line 3 に書かれた pane ID で見分ける。
- 前提6: 前任は最後に停止される。
- 前提7: 引き継ぎ文書は交代の時点で書く。

前提7 が一番あやしい。交代のきっかけは context の枯渇なので、**一番長い文書を、一番 context が残っていない時点で書かせている。**

昇格の印: 引き継ぎ文書を交代時点で書く前提そのものが設計判断である

初期の案セット（カテゴリ別）:

- 引き継ぎ文書を誰が書くか: A-1 前任が手書き（現状）/ A-2 verb が観測できる事実から骨格を生成し人は会話にしか無かった分だけ書く / A-3 board 自体を引き継ぎ文書にして別 brief を作らない / A-4 前任が生きている間ずっと少しずつ書き足す / A-5 文書を作らず resume で会話ごと引き継ぐ
- 後継が席を取る工程: B-1 手順を手で踏む（現状）/ B-2 後継が自ペインで1コマンド（登記・board 張替・検算・見張り確認・記録）/ B-3 前任が spawn 時に board へ「後継待ち」の行を作り後継が埋める（issue の中間案）/ B-4 board が pane でなく session_id を宣言する（#247・スコープ外）/ B-5 機械が読む席の宣言を board 本文から外し、機械可読の別ファイルへ置いて board はそれを指す
- 前任の自己申告: C-1 会話で自由文（現状）/ C-2 verb が機械で確かめられる分（open PR・未 push・worktree・生きた子・未達の送信・board の書き残し）を自動で集め、人は機械で確かめられない分だけ申告する / C-3 後継が同じ機械の検査を独立にやり直して突合する / C-4 申告を作らず owner が見て決める
- 前任を閉じる経路: D-1 生の `tmux kill-pane` を owner が叩く（現状）/ D-2 後継が verb で閉じる（申告の記録と後継自身の検査が揃っていることを条件にする）/ D-3 前任が自分で閉じる / D-4 閉じずに席から降ろすだけにする / D-5 verb は閉じるコマンドを表示するだけで実行は owner がやる
- 交代の形そのもの: E-1 context 枯渇時点の1回きりの出来事（現状）/ E-2 重なり期間を置いて、後継が動くと確かめてから席を移す / E-3 席を file にして、読んだセッションが誰でも統括になる（identity を無くす）/ E-4 統括を短命にして頻繁に回す
- 交代のきっかけ: F-1 owner か前任が気づく（現状）/ F-2 `oe-vitals` の context 検知（既定 85%）の ping が交代の入口を兼ねる / F-3 閾値で自動 spawn

### 実測1 — 引き継ぎ文書のどこまでが機械で出せるか（2026-09-11）

立ち上げプロンプト 36 行と brief 50 行を節ごとに分け、「観測できる事実か」「会話にしか無かったものか」で仕分けた。仕分けの根拠として、実際にコマンドを叩いて値が取れるかを確かめた。

取れたもの（すべて既存のコマンドで、追加の機構なしに取れる）:

- open PR の一覧と draft かどうか（`gh pr list --json number,title,isDraft`）
- worktree の一覧（`git worktree list`）
- 生きている委譲子の一覧と、その model・context%（`oe-tree`）
- 常駐の見張りの登録状態（`launchctl list | grep oe-`。`com.stlwolf.oe-vitals` と `com.stlwolf.oe-confirm` の2本が登録済みと確認）
- 前任の session_id と context%（拍動 sidecar を pane で逆引きする。`~/.claude/state/oe-heartbeat/<session_id>.json` の中身が `{"ts":...,"context_pct":...,"pane":"%N",...}` なので、pane から session_id を引ける）

最後の1点は設計にとって非自明である。issue 本文は「前任は後継の session_id を知る手段を持たない」と書いているが、これは**前任から後継への向き**の話で、逆向き（後継が前任の session_id を引く）は sidecar の逆引きで成立する。いま brief には前任の session_id が手で書き写してあるが、これは機械で出せる。

会話にしか無かったもの（機械では出せない）:

- owner に預けている判断とその前提
- owner が決めた次のテーマの順序
- 「渡らないもの」節（owner の関心の順・前任が踏んだ誤りの型）
- in-flight の表の「次に起きること」列（状態は機械で出せるが、次に何が起きるかは判断である）

**節ごとに見ると、立ち上げプロンプト 36 行のうち約 24 行（「起動時にやること」6工程と冒頭の identity 宣言）は機械で出せる。** 残り 12 行は brief の要点の再掲で、brief を読ませれば不要になる。

昇格の印: 立ち上げプロンプトは「統括になる」verb が無いから存在している

もう1つ分かったのは、brief の「この期間に確立した運用（守ること）」節（17 項目）が、世代をまたいで書き写され続けていることである。durable な規律なので、世代ごとの brief ではなく知識の層（memory / rules / knowledge item）に置くべきもので、毎回 brief へ写すのは損失である。

### 実測2 — 交代のきっかけに使えるはずの検知が、いま動いていない（2026-09-11）

`oe-vitals` は board の `現統括:` から pane を取り、その拍動 sidecar の `context_pct` が 85% を超えたら交代を促す。これが交代のきっかけの候補（案 F-2）だった。実際に叩いて確かめた。

```
統括（pane は伏せる）の拍動: 7567 秒前（oe-vitals の既定窓 W=1800 秒）
oe-vitals: 現統括 %NNN（beat stale/2h6m · ctx=17% · pane=alive · W=1800s · T=85%）
  → 検知なし（beat stale だが pane が gone でない＝death は確定 gone でのみ判定・alive/?×stale は no-op）
```

拍動は statusLine が書くので、セッションが描画しないあいだ更新されない。**統括の拍動が窓より古くなると、context 検知は沈黙する。** いま現に沈黙している。これは「検知しなかった」と「健全だった」が読み手から区別できない形で、この単位が採用した negative knowledge の1つ目（停止を報告する主体を当人に置くな）と同じ型である。統括の拍動を書くのは統括自身のセッションだからである。

この単位のスコープ外（拍動の鮮度そのものは復帰側の軸）だが、**案 F-2 を交代の入口として当てにはできない**という設計上の結論はここで確定する。surface して親へ上げる。

### ゲート1 — ゼロベース代替探索の実行と結果（2026-09-11）

`SO_TIMEOUT=600 SO_CLAUDE_TIMEOUT=1200 oe-refute --claim .oe/claim-390-gate1.md --lanes 3 --rubric exploration` を実行した。claim は 13,474 バイトで、材料をレーンに読みに行かせない自己完結の形にした（採用した negative knowledge `01M141TB5YHB6VN62RA5TP0S3Q` の指示どおり上限も上げた）。結果は3レーンとも実返却で、空返しは1本も出ていない（cursor 87 秒 / codex 175 秒 / claude は 1200 秒の枠内で完走）。

- verdict: `refuted`（3/3 レーンが material に反証）
- audit_id: `20260911141134S4MRTPKRDR34`
- output_dir: `tmp/oe-refute-20260911141134S4MRTPKRDR34`（永続しないので内容は plan と本 episode へ転記した）

**「これ以外の技術カテゴリの代替は残っていない」という主張は成立しなかった。** 出てきた代替は plan の「棄却した案」節に理由つきで載せた。ここには、設計そのものを変えた指摘だけを書く。

#### 覆った点1 — owner の設計の骨1の理由が、実測と合わない

issue 本文の設計の骨1は「席を見分ける材料は pane と session_id の2つで、どちらも後継のプロセスの中にしか存在しない。前任は後継の session_id を知る手段を持たない」と書いている。claude レーンがこれを3経路で崩し、私が自分で全部確かめて、3つとも成立した。

- 拍動 sidecar は `~/.claude/state/oe-heartbeat/<session_id>.json` で、ファイル名が session_id、本文に pane が入る。ディレクトリを1回読めば pane から session_id を引ける。後継のプロセスに入る必要はない。[verified]
- `lib/spawn.sh` は新しい pane の ID を `OE_SPAWN_PANE_ID` に受ける。委譲で後継を spawn するなら、前任は後継が起動する前に後継の pane を持つ。[verified]
- `lib/delegate-registry.sh` の `oe_reg_record <child_pane> <label> <workspace> <parent_pane>` は、親が子の登記を書く関数である。「他人の席は書けない」という制約は engine の中に存在しない。[verified]

つまり「後継が自分で取りに行くしかない」は**技術的な制約ではなく、運用として選んだ方針**である。結論（後継が取りに行く）を変える必要は無いが、**理由は置き換えないと使えない。** 置き換えた理由は plan の DJ-4 に書いた。骨を勝手に覆さず、owner に預ける判断として残した。

昇格の印: 設計の骨の理由が実測で崩れたが結論は残る、という形の扱い方

#### 覆った点2 — 生きた子がいる交代では、前任を閉じると子の報告先が死ぬ

`bin/oe-delegate` は子の起動コマンドに `PARENT_TMUX_PANE=<親 pane> claude` を焼き込む。[verified] 環境変数は起動後に書き換えられないので、前任の pane を閉じると生きている子の戻し先が死ぬ。これは lean の決定が mode3（チャネル脆弱）として挙げている故障そのもので、対策の `@seat` mailbox は「報告の stranding が再発したら導入する」という再開条件つきで defer されている。

今日の交代は委譲子 0 体だったので踏まなかっただけである。`bin/oe-report` は親を「環境変数が先、無ければ `/tmp/oe-parent-<自 pane>` ファイル」の順で解決するので間接参照の口は既にあるが、環境変数が優先されるうえ、README が推す `oe-send "$PARENT_TMUX_PANE"` の直書き経路はこの解決を通らない。[verified]

私の初期案は前任を閉じることだけ考えていて、この依存を見落としていた。plan に DJ-11 として足した。

#### 覆った点3 — 自己申告の分業は、context の圧縮で壊れる側と人に任せる側が一致する

claude レーンの指摘。交代のきっかけは context の枯渇で、その帯では自動の圧縮が動き、圧縮前の往復は要約に畳まれる。**畳まれる側にあるのは、まさに「会話にしか無かったもの」である。** C-2（機械が集める分と人が書く分を分ける）は、壊れる部分をそのまま人に任せている。C-3（後継が機械の検査をやり直す）も機械の分をもう一度やるだけなので、この残余には届かない。

機械で救える範囲はある。`oe-confirm` と `oe-undelivered` が送信単位の到達を照合しているので、未達の送信は前任の記憶に依存しない。ただしこれは「教訓が当たらない」ことの証明ではなく、「当たる範囲が狭まった」ことの確認である。

手当ての方向として2つ出た。圧縮の直前に機械が書き出す（ハーネスの `PreCompact` イベント）か、transcript から第三者が拾うか。**`PreCompact` / `SessionStart` / `SessionEnd` は claude 2.1.268 に実在する**（実行ファイルを検索して確認）が、hub が配線しているのは `PreToolUse` / `PostToolUse` / `Stop` / `Notification` / `UserPromptSubmit` / `TaskCompleted` の6つで、この3つは未使用である。[verified] 配線はハーネス層の変更なので、この単位では owner に預ける判断として出した。

#### 覆った点4 — 交代イベントの追加は、verb 1本より広い変更になる

`schemas/oe-events.schema.json` の type の語彙は `child_spawned` / `message_sent` / `prompt_received` / `report_received` の4つで、交代に当たる型は無い。[verified] 交代イベントを足すなら schema を触る。「席を取る工程を1コマンドに畳むだけ」という見立てより広い。plan のステップに明示した。

#### 覆った点5 — pane から session_id を引く逆引きには、pane の再利用の罠がある

sidecar は掃除されない（`oe-vitals` が read-only を保つため）。実測で**異なる pane 番号が 171 個**貯まっていた。[verified] tmux server を再起動すると pane 番号は振り直されるので、過去世代と同じ番号が後継に割り当たりうる。逆引きするなら pane だけで引かず、現在の tmux server の pid と拍動の新しさで絞る必要がある。sidecar には `server_pid` が入っているので材料はある。

#### 持ちこたえた点

- 引き継ぎ文書を「機械が書く節」と「人が書く節」に分ける方向そのもの（cursor が「現状制約下の有力デフォルト」と明示）。
- 席の取得を後継の1コマンドに畳む方向（同上）。
- 段階を踏んで停止へ進む形（同上・`careful-operations-rule` と整合）。
- 重なり期間を置く形（同上・実運用と整合）。
- 前提7（引き継ぎ文書は交代の時点で書く）が怪しいという読み。ただし3レーンとも「前提3・前提5 も外すとカテゴリが変わる」と追加で指摘した。

### 実測3 — pane の再利用は既に起きている（2026-09-11）

設計SO が指摘した「pane から session_id を引く逆引きには pane 再利用の罠がある」を、自分で数えて確かめた。

```
pane を持つ sidecar: 198 件 / 異なる pane 番号: 171 個
同じ pane 番号を持つ sidecar の数（上位）: 最多の番号で 7 件、次点が 3 件で3 番号
```

**将来の危険ではなく、既に起きている。** pane だけで引くと、同じ番号の古い世代の sidecar を拾いうる。plan の DJ-2 に `server_pid` と拍動の新しさで絞る条件を入れた。

あわせて `oe-selfcheck` を実行し、この単位が前提にしている検査の状態を確かめた。

```
screen-marker        broken  稼働中のペインが 1 枚あるのに目印 'esc to interrupt' を画面のどこにも観測できない
hook-contract        ok      直近の nonce 付き送信 20 件中 17 件に宛先ペインからの受領印がある
pane-session-bridge  ok      230 件中 198 件が pane を持つ
watchdog-freshness   ok      見張り（oe-confirm）の最終走査は 178 秒前（上限 900 秒）
```

`screen-marker` が broken であることは brief の指摘どおりで、採用した negative knowledge `01KZKWJM1KTAFN22XK0WP0F96J`（画面から状態を読む形は外れる）の根拠にもなっている。この plan は画面から状態を読む形を1つも採っていない。

### ゲート2 — 設計SO の実行と、設計が変わった箇所（2026-09-11）

`SO_TIMEOUT=900 SO_CLAUDE_TIMEOUT=1800 oe-refute --claim .oe/claim-390-gate2.md --lanes 3 --rubric consensus` を実行した。claim は plan の全文を貼った 46,682 バイトの自己完結の文書で、3レーンとも実返却した。

- verdict: `refuted`（3/3 レーンが material に反証）
- audit_id: `20260911142659C063BRD7511J`
- output_dir: `tmp/oe-refute-20260911142659C063BRD7511J`

指摘の一覧と対応は plan の「設計SO（ゲート2）の結果」節にある。ここには**自分の設計の壊れ方の型**だけ書く。

#### 型1 — 検査を置く場所を、故障が起きる場所でなく気づく場所に置いていた

最初の案は「生きた委譲子がいるなら `retire` が停止を止める」だった。これは**検査を、自分が故障に気づいた工程に置いた**ものである。故障（前任と後継が権限を持ったまま併存する）が始まるのは `take` で席が動いた瞬間なので、検査は `take` の前に要る。codex レーンがこれを突いた。

置き場を移したら、副次的に DJ-7（停止が fencing である）との矛盾も消えた。同じ誤りが2箇所に出ていたのに、自分では片方しか見えていなかった。

#### 型2 — 自分の設計が自分の前提を壊す経路を見ていなかった

「後継を `oe-delegate` で spawn すると、後継自身が生きた委譲子になる」は claude レーンの指摘である。DJ-11 の検査を素直に実装すると、検査対象に後継自身が入って永久に通らない。**自分が足した検査が、自分が使う経路を塞ぐ**という形で、検査を書いたときに「誰が数に入るか」を数えていなかった。

数え方の錨（`oe_reg_list` は自己スコープなので `take` では常に0体になる）も同じ型で、**述語を書いたが母集団を書いていなかった。** 採用した negative knowledge `01KZNWK084YF0RP48337VFYMQQ`（述語を組み直したら元の目標事例を1件ずつ当て直せ）と近いが、こちらは組み直し以前に母集団が未定義だった。

#### 型3 — 1件の観測から原因を1つに決めた

DJ-9 で「統括の拍動が古いのは、セッションが描画しないあいだ更新されないからだ」と書いた。codex が配備されている設定を読んで否定した。`refreshInterval: 10` が設定されており、producer 自身も idle timer で走ると書いている。**観測（拍動が 2 時間古い・検知が沈黙している）は正しいが、原因は決められない。**

しかも原因を決めたことで「この単位への直接の害は無い」という結論まで書いていた。原因が分からないなら害の範囲も分からない。**観測を原因に化かし、その原因から安全を導いていた。** 13 代目が自分で記録した誤りの型（自分で確かめたつもりの範囲が、実際に確かめた範囲より広い）と同じである。

昇格の印: 観測から原因を決め、その原因から安全を導く、という2段の飛躍

#### 型4 — 稼働中の仕組みを「新しいテストで守る」と書き、既にある回帰ガードを見ていなかった

`oe-vitals` の `現統括:` 解決を `lib/` へ切り出す計画で、「現在の挙動を固定するテストを新設する」と書いた。claude レーンが `tests/test_oe_vitals.sh` を挙げ、そこに freeform の board で前任を併記した行から現統括を解決するケース（ケース17）が既にあると指摘した。自分で開いて確かめたら、そのとおりだった。**既にある守りを見ずに、新しい守りを足す計画にしていた。**

#### 持ちこたえた点

- DJ-6 の圧縮の穴を「塞げない」と書いたこと自体は3レーンとも妥当と認めた。ただし DJ-11 の「塞げない」の書き方は狭すぎるという指摘があり、そちらは書き直した。
- DJ-11 の fail-closed という判断そのものは lean の決定と整合しており反証にはならない、と cursor が明示した。
- DJ-2 / DJ-4 / DJ-7 の lean との整合は概ね妥当と評価された。

#### 2周目を回さなかった理由

`so.design` は weak で、弱 SO は1周で終了してよい（`document-format.md` §4.1）。`refuted` なので**この plan はここでは確定しない。** 確定はゲート3（owner の Human Gate）である。指摘を反映したうえで、確定の判断を owner に渡す。

### 採用した knowledge item への観測（closure で書き戻す下書き・2026-09-11）

観測の台帳は**注入を受けた子が自分の closure 時に**書く規約なので（`document-format.md` の observations 要素スキーマ）、この単位はまだ closure に達しておらず、item のファイルには書いていない。**ただしこの単位は plan で止まるので、実装へ進まないと台帳が永久に書かれない。** 証拠が新しいうちに下書きをここへ残す。closure ではこれを転記する。

- `01KZVHE0KJ12W3NG6A4R0WSWS4`（停止を報告する主体を当人に置くな）
  - state: `followed`
  - note: 完全性のうち機械で確かめられる部分を前任の申告から外し、後継の独立再検査へ移した（DJ-6）。生存の検知と完全性の申告で場面が分かれることを plan に明記し、分かれない残余は塞げないと書いた
- `01KZKWJM1KTAFN22XK0WP0F96J`（別ペインの状態を画面から読む形は外れる）
  - state: `followed`
  - note: 画面から状態を読む形を1つも採らず、生死は tmux の存在照会、状態は登記とイベントと拍動 sidecar だけで見る設計にした。`oe-selfcheck` の `screen-marker` が broken であることも実行して確かめた
- `01KZNWK084YF0RP48337VFYMQQ`（述語を組み直したら元の目標事例を当て直せ）
  - state: `externally_verified`
  - note: 「生きた委譲子0体」の述語で母集団を書いておらず、設計SO が `oe_reg_list` の自己スコープでは常に0体になる（fail-closed が fail-open へ反転する）ことと、後継自身が数に入ることを外部から検出した
- `01M141TB5YHB6VN62RA5TP0S3Q`（SO は読ませる量で落ちる）
  - state: `followed`
  - note: ゲート1（13,474 バイト）もゲート2（46,682 バイト）も材料を全部インラインした自己完結の claim にし、上限を 600 / 900 秒へ上げた。3レーンとも実返却で空返しは1本も出ていない

### 終端 — plan フェーズはここで止まる（2026-09-11）

plan を draft PR [#392](https://github.com/stlwolf/ai-development-hub/pull/392) に載せた。draft のまま置き、マージしない。実装に進むのは owner の Human Gate（ゲート3）が下りてからである。

この episode の closure は、実装の最初の PR のレビュー後・マージ前に行う（ゲート5）。そのとき、上の「採用した knowledge item への観測」の下書きを item のファイルへ転記する。

**親へ上げたもの**（この単位では実装せず surface した）:

- 設計の骨1 の理由が実測と合わない件（plan の owner に預ける判断5）
- 統括の拍動が古くなる原因が不明で、稼働中の見張りの土台が壊れている疑いがあること（DJ-9）
- brief の「確立した運用」17 項目を知識の層へ移す作業（plan の owner に預ける判断3）
- board が先行の schema の決定に従っていない状態（plan の owner に預ける判断7）

## 実装フェーズ

### ゲート3 通過と plan の改訂（2026-09-11）

owner が plan を承認した。承認時に固定された baseline の digest 3件（plan / brief / ゲート表）を、改訂に入る前に実物と突き合わせて全件一致を確認した。

owner の裁定で設計が2点変わった。owner の言葉は「重要なのは俺が手作業でやらないことだな。一つの指示で明確にフローが決まっていて、確実に引き継げるのが大事」である。

1. **停止のコマンドは owner が打たず後継が打つ。** plan は「段階1 は owner だけが `--execute` を打つ」と書いていた。これは統括の推しで入った段階分けで、owner の要求と正面から逆を向いていた。**人のゲートを1つ外す変更なので、申告のときに名指しした。** 安全の仕掛け3つ（停止と同じコマンドの中で再検査する・前任の session_id が記録に残っていることを前提条件にする・後継が独立に再検査する）は残し、段階の境目を「誰が打つか」から「何を壊しうるか」へ引き直した（段階1 で `--execute` が触れるのは前任のペインだけ）。
2. **「一つの指示でフローが決まる」「確実に引き継げる」を受入条件に足した。** 前者を満たすために `oe-handoff start`（前任が後継のペインを起こして引き継ぎ文書のパスを送る）を subcommand に足した。足さないと、owner が「交代して」と言ったあとにもう一度手を動かすことになる。

裁定6（圧縮の直前にハーネスのイベントで書き出す形を入れない）の帰結として、受入の主張を「残余の完全性は保証しない試験運用」まで弱めた。plan 自身が承認前に「判断6 が (a) 固定ならそう書く」と約束していた箇所なので、そのとおりにした。

digest の変化は申告済み。step の削除は無く、3件（Step 1-0 / 2-7b / 2-7c）増えた。

昇格の印: 段階分けの軸を「誰が打つか」から「何を壊しうるか」へ引き直した判断

### PR-1 の実装 — 状態を集めて引き継ぎ文書を書く（2026-09-11）

Step 1-0 から 1-6 まで実装した。入れたものは4つである。

- `projects/orchestration-engine/docs/decisions/2026-07-10-decision-238-board-schema.md` への amendment（Step 1-0）
- `projects/orchestration-engine/lib/handoff-state.sh`（状態の収集・read-only）
- `projects/orchestration-engine/templates/handoff.md.template`（引き継ぎ文書の書式）
- `projects/orchestration-engine/bin/oe-handoff` の `prepare`

#### plan から1点ずらした置き場と、その理由

plan の Step 1-1 はテンプレートの置き場を「`projects/orchestration-engine/docs/` 配下」と書いていた。**`templates/` の下に置いた。** 理由は、engine の `docs/` が蒸留木そのもの（`plans` / `episodes` / `decisions` / `discussions` / `knowledge`）で、そこに層でないディレクトリを足すと「どの規約で読む文書なのか」が名前から分からなくなるからである。`document-format.md` §9 が同じ形の混在を戒めている。あわせて `launchd/com.stlwolf.oe-confirm.plist.template` という先例があるので、そちらに倣った。

#### 設計として効いた点

**逆引きは「曖昧なら書かない」に倒した。** pane から session_id を引くとき、いまの tmux server の pid が一致する sidecar がちょうど1件のときだけ値を返す。0 件でも複数でも `unknown` にする。実測で pane を持つ sidecar 198 件に対し異なる pane 番号は 171 個しかなく、**同じ番号の別世代が既に貯まっている**からである。誤った session_id を書くと、停止の前提条件（`claude --resume` で開き直せること）が嘘になる。

**子を数える錨を引数の pane にした。** 既存の `oe_reg_list` は「parent_pane == 自ペイン」で数える。`take` と `retire` は後継のセッションで走るので、この既定で数えると常に0件になり、**fail-closed がそのまま fail-open へ反転する**。設計SO が突いた点で、テスト [5] がこの反転を直接見ている（`%10` の子を数えると1件、`%11` を親にすると0件）。

#### ゲートの結果

- `shellcheck`（verb・lib・テスト）= exit 0。Markdown のバッククォートに対する SC2016 は既存の `oe-review` と同じ形で理由つきに抑止した。
- `bash tests/test_handoff_prepare.sh` = 26 件すべて PASS。
- 「`prepare` が引き継ぎ文書以外を書き換えない」はテスト [9] が board・イベントログ・登記の mtime で見ている。

### ゲート4 — 実装SO と、自分では見えていなかった欠陥（2026-09-11）

`SO_TIMEOUT=600 OE_REVIEW_DIFF_MAX_BYTES=60000 oe-review --lanes 2 --base <コードの手前> --context <要件>` を実行した。diff は 29,894 バイトで inline された。

- verdict: `refuted`（2/2 レーンが material なコード欠陥を検出）
- audit_id / reviewed_sha: `435640b` に束縛された成果物として残る
- output_dir: `tmp/oe-review-20260911162908626X8B5MYR4M`

**5件のうち2件は、SO を待つあいだの自己レビューで先に見つけて直していた。** 残り3件は自分では見えていなかった。

#### 自分で先に見つけた2件（レーンも同じものを挙げた）

- 子を数えられなかったとき（登記の置き場が決まらない・jq 不在）を「0 件」に畳んでいた。**fail-closed のつもりが fail-open だった。**
- upstream の無い枝で `unpushed=0` と表示していた。`git log` が失敗しても `wc -l` が 0 を返して exit 0 になるので、`|| unknown` が発火しない。

#### 自分では見えていなかった3件

- **逆引きが拍動の新しさを見ていなかった。** 「現 server の pid が一致する sidecar がちょうど1件」までは絞っていたが、pane が再利用され、その番号の古い sidecar が1件だけ残っている状況では素通りする。しかも**テストの fixture が `ts:1` の古い値で、この欠陥を固定していた。**
  - 直し方を変えた。鮮度の窓を足すだけでは弱い（統括の拍動は実測で2時間古くなる）。この値の用途は「停止しても `claude --resume` で開き直せる」ことの担保なので、**担保の実体そのもの（transcript が在り、最近書かれていること）を直接確かめる**形にした。transcript を書くのはセッション本体で、statusLine とは別の主体なので、拍動とは独立した信号になる。
- **tmux が引けないことを「子が居ない」に畳んでいた。** `oe_hs_pane_alive` が tmux 不在で偽を返し、子が全部フィルタされて0件になっていた。生存一覧を1回で引き、引けなければ失敗を返す形にした。
- **登記の走査が server の名前空間を無視していた。** ファイル名は `<server_pid>_<pane>` なのに `*.json` で全部見ていたので、旧 server の残骸と現 server の同番号 pane が結びつく。現 server の分だけ見るようにした。
- **目印の検査が「在るか」だけだった。** `end` が `begin` より前にある文書では、`awk` が begin 以降を末尾まで捨てて上書きし、**人が書いた節を復元できない形で壊す**。1組だけか・順序が正しいかを、書き換える前に見るようにした。
- 加えて `gh pr list` が `-w` の workspace でなく呼び出し元の cwd を見ていた（同じ文書の中で repo 節と PR 節が別のリポジトリを指しうる）。workspace の中で呼ぶようにした。`master HEAD` と名乗って現在の HEAD を出していた点も、枝の名前を併記して名乗りと中身を揃えた。

#### この回の学び

**5件のうち4件が同じ型だった。「観測できなかった」を「観測した結果それは無い」に畳む。** 自分で2件見つけたあとも、同じ型が3件残っていた。型を知っていることと、自分のコードでその型を見つけられることは別である。

テストも 30 件から 41 件へ増やし、壊れ方の側（数えられない・古い・重複・順序違い）を見るようにした。

昇格の印: 「観測できなかった」を値に畳む欠陥は、型を知っていても自分では見つけ切れない

### ゲート4 の2周目 — 同じ型がまだ4件あった（2026-09-11）

1周目の指摘を直したうえで、**最終 HEAD に束縛した成果物を残すため**にもう1周回した（`oe-review` は `reviewed_sha` を記録するので、古い版に対する結果を新しい版の合格として使わせない）。結果はまた `refuted`（2/2）で、4件が新たに出た。

- **拍動の新しさを見ていなかった。** transcript の裏取りを足したが、pane が短い間に再利用され、新しいセッションがまだ拍動を書いていない場合、旧世代の sidecar が唯一の候補として残る。拍動そのものの鮮度も見る形にした（窓は実測より広い 6 時間を既定に）。
- **壊れて読めない登記を黙って飛ばしていた。** それが唯一の生きた子だったときに0件へ倒れる。読めないものが1つでもあれば「数えられない」として失敗する形にした。
- **一時ファイルの書込みを検査せず、trap も無かった。** 書けていない staging を `awk` が読むと、機械の節が空の文書を本物に被せる。中身は引き継ぎ文書の全文なので、置き去りは漏洩でもある。あわせて `mv` が一時ファイルの権限を持ち込む点（`0600` が `0644` に緩みうる）も直した。
- **session_id が引けないのに「次は `start`」と案内していた。** 子の件数には fail-closed のゲートを置いたのに、session_id の側には置いていなかった。**片方だけ fail-open になっていた。** 子のゲートと対称にした。

#### テストが欠陥を固定していた

1周目の指摘に「テストの fixture が `ts:1` の古い値で、この欠陥を固定している」があった。2周目の修正で拍動の鮮度を見るようにしたら、**その fixture を使っていたテストが3件落ちた。** 落ちたのは正しい。`mk_beat` を「いまから N 秒前」で作る形に変え、古い固定値を使わないようにした。

**テストが通っていることは、テストが正しいことを意味しない。** 固定値の fixture は、その値を前提にした欠陥をそのまま固定する。

#### SO の周回をここで止める理由

`so.impl` は weak で、契約上は1周で終了してよい（`document-format.md` §4.1）。2周目まで回したのは、1周目の修正でコードが変わり、成果物の束縛が古い版のままになるためである。**3周目を回して、最終 HEAD に束縛した結果を残したうえで止める。** 3周目にも指摘が残るなら、直さずに PR で開示する（無限に回さない）。

テストは 41 件から 48 件へ。

### ゲート4 の3周目 — 2つのレーンが逆向きから同じ誤りを指した（2026-09-11）

3周目も `refuted`（2/2）。ここで出た指摘のうち1件が、**この単位でいちばん学びの大きい形**をしていた。

- codex: pane が再利用され、旧世代の拍動と transcript がどちらも窓の中なら、**旧 session_id を採ってしまう**（安全側に倒れていない）。
- cursor: 逆に、同じ pane に sidecar が複数貯まっていると**件数の条件で常に unknown になり、2世代目以降はその pane で交代が始められない**（実測で最多の pane には7件貯まっている）。

**2つは逆向きの苦情に見えるが、原因は同じ1つの規則だった。** 「現 server の一致がちょうど1件のときだけ採る」という**件数で決める規則**である。件数で決めると、貯まった pane では常に unknown になり（cursor）、たまたま1件だけ残った pane では古い値を採る（codex）。どちらも件数が「いまの前任かどうか」を表していないことから来ている。

規則を**新しさで決める**形に変えた。現 server の候補のうち拍動がいちばん新しいものを採り、その拍動と transcript が両方とも鮮度の窓の中にあることを確かめる。死んだ世代の拍動は止まっていて更新されないので、新しさは「いまの前任か」を件数よりよく表す。

**塞げていない穴は、塞いだふりをせずに書いた。** pane が短い間に再利用され、**新しいセッションがまだ一度も拍動を書いていない**あいだは、旧世代の拍動が唯一の候補として残る。それが窓の中なら旧世代を採る。セッションが自分の id を名乗る経路が無いかぎり、この窓は閉じない。関数のコメントと PR に明記し、あわせて**拍動の古さを引き継ぎ文書に出す**ようにした（人が見て判断できる材料を残す）。

同じ周で出た残り2件も直した。目印の検査が「行のどこかに在る」で置換が「行頭」だったため、**目印に空白が1つ付いただけで検査を通過し、人が書いた節が末尾まで消える**経路があった。検査と置換の両方を「その行が目印そのもの」に揃えた。引き継ぎ文書の権限も、新規作成と一時ファイルの両方で最初から絞るようにした（`umask 022` の環境で world-readable になっていた）。

#### SO の周回をここで止める

宣言したとおり3周で止める。3周とも `refuted` だったが、**3周目の指摘はすべて反映した。** 4周目は回さない。残る穴（拍動が1件も書かれていない瞬間の再利用）は、直さずに開示する側へ置く。

テストは 48 件から 53 件へ。

#### 3周を通しての学び

**1周目から3周目まで、指摘の型はずっと同じだった。「観測できなかった」と「観測した結果それは無い」を同じ値に畳む。** 12 件の指摘のうち 9 件がこの型である。自分でも2件先に見つけたので型は知っていたが、**知っていてもなお、同じ型を自分のコードの中に見つけ切れなかった。**

もう1つは、**2つのレーンの逆向きの苦情が同じ原因を指すことがある**という形である。片方だけ読んで直すと、もう片方を悪化させる。両方が同じ規則を指していると気づいたのは、両方を並べて読んだからだった。

昇格の印: 逆向きの2つの指摘が同じ規則に由来する、という読み方

### ゲート4 — Copilot のレビュー（2026-09-11）

`gh pr edit 392 --add-reviewer @copilot` で依頼した。`requested_reviewers` は空のままだったが、timeline に `review_requested Copilot` と `copilot_work_started` が出たので登録されている（memory の実測どおりで、空を見て投げ直さない）。指摘は8件付いた。

**7件は妥当で直した。1件は事実として成り立たないので直さず、理由を返信した。**

成り立たなかった1件は「macOS の `find` は `-maxdepth` に対応していないので常に失敗する」というもので、実際に叩いて `exit=0` を確認した。BSD の `find` は `-maxdepth` を持つ。

直した7件のうち、**3件はまた同じ型**（観測できなかったことを値に畳む）だった。

- 登記の置き場が読めない（権限が無い）とき、glob が展開されずループが1回も回らないので「子0件」になる。
- 登記が JSON として妥当でも `.pane` や `.parent_pane` が欠けていると黙って飛ばす。
- `launchctl list` 自体が失敗したとき「見張り0本」と書く。

残り4件は移植性と曖昧さである。

- **`stat -f %m` は GNU では filesystem の情報を返して成功してしまう。** だから `|| stat -c %Y` のフォールバックは発火しない。engine には既に `date -r <file> +%s` が BSD / GNU どちらでも使えるという注記が `oe-selfcheck` にあり、そちらに合わせた。**既存の注記を読んでいれば最初から踏まなかった。**
- transcript の置き場のノブを `OE_TRANSCRIPT_ROOT` という新しい名前で作っていたが、engine には既に `OE_TRANSCRIPT_DIR` がある（`oe-selfcheck`）。**新しいノブを増やすと、非標準の置き場を指している環境で本物を見落とす。** 既存のノブに寄せた。
- 拍動の `ts` は epoch 秒なので同率が起こりうる。同率のとき glob の順で先に見つけたほうを黙って採っていた。**「どの世代か決められない」のだから unknown に倒す**形にした。
- テストの `date -r <epoch>` は BSD 専用で、GNU では第1引数がファイル名として解釈される。GNU 環境では黙って現在時刻に落ち、**鮮度のテストが何も検証しなくなる**。`perl` の `utime` に変えた。

最後の1件は**テストが検証しているふりをする**形で、2周目に見つけた「fixture が欠陥を固定する」と同じ家族である。この単位でこの型は3回出た。

テストは 53 件から 58 件へ。うち2件は「実装の文字列を grep する」形で書いてしまったので、**振る舞いを見る形に書き直した**（別の置き場を指したら引ける / 既定の置き場には無いので unknown）。ソースを grep するテストは、実装を変えた瞬間に意味を失う。

昇格の印: 新しい env のノブを足す前に、同じ役目の既存のノブを探す

### 待機中 — 自分の報告に混ぜた誤りが、この単位で直した欠陥と同じ型だった（2026-09-11）

PR-1 の報告に「Copilot は push のたびに自動で再レビューする」と書いた。memory の過去の実測を写したものだったが、**統括が timeline を当て直して誤りだと分かった。** 新しいレビューの直前には必ず明示的な依頼があり、依頼の無い push は 80 分でも 25 分でもレビューが付かない。自分でも #389 の timeline を引いて確かめた（依頼 → レビューの往復が2回あり、そのあいだの5コミットには何も付いていない）。

元の観測（push のあとにコメントが増えた）は正しく、**因果の帰属が誤っていた。** 誰かが依頼していたことを timeline で確かめていなかった。

**これは DJ-9 で自分が踏んだのと同じ型である。** あちらは「統括の拍動が古い」という1件の観測から「描画しないから更新されない」という原因を決め、設計SO に否定された。こちらは「push のあとに増えた」から「push が引き金だ」を決めた。どちらも**時間の前後で因果を決めて、引き金の候補が記録に残っている場所を見にいっていない。**

同じ型を、自分のコードの中では設計SO に指摘され、自分の報告の中では統括に指摘された。**型を知っていることと、自分がその場でそれを踏まないことは別である**という、この単位で3回目の確認になる。

### 統括のレビュー — 文書と実装の食い違い、lib の前提の置き場（2026-09-11）

owner の依頼で統括が PR #392 をレビューした。Copilot が見たのは修正前の版なので、最後の版に対する人側のレビューにあたる。CRITICAL は無く、WARNING 3件と SUGGESTION 2件。**5件とも直した。**

#### いちばん効いた1件 — 文書に書く説明が、実装より古い

`prepare` が引き継ぎ文書へ書く「session_id が引けていない理由」が、**件数で決めていた頃の説明のまま**だった。「sidecar がちょうど1件に絞れなかった（0 件か、別世代が複数ある）」と書くが、いまの実装が unknown を返す理由は7つあり、**実際に起きやすいのは「拍動が古い」と「transcript が使えない」**である。統括のセッションで拍動が5時間古くなった実例があるので、現実の経路である。

**そのとき文書は「別世代が複数ある」と書き、読んだ人は違う場所を調べる。**

同じ verb の画面表示のほうは正しく書けていた。つまり**同じ事実の説明が2箇所にあり、片方だけ直していた。**

直し方を「文言を揃える」で終わらせなかった。**lib が unknown の理由を返す**ようにし（`OE_HS_SESSION_REASON`）、verb はそれを書くだけにした。人が実装を読んで説明を書き写す経路が残るかぎり、実装を変えたときに文書だけ古くなる。**写経をやめる形にしないと、同じことがまた起きる。**

#### lib の正しさを、呼び出し側のシェルオプションに預けていた

`git worktree list | wc -l` は、git が失敗してもパイプライン全体の成否が `wc` のものになるので 0 が入る。`oe-handoff` が `set -o pipefail` を敷いているので**いまは正しく動く**が、これは lib である。統括の指摘は「env のノブは冒頭で丁寧に列挙しているのに、シェルオプションの前提は書かれていない」だった。

パイプをやめて git の rc を直接見る形にした。**この lib が何より避けたい「0 と unknown の取り違え」が、呼び出し側の設定1つで復活する形だった。**

#### 自分で名指しした故障の、片側だけを塞いでいた

`awk` の `getline` はファイルを開けないとき -1 を返す。`> 0` で見ていたので、-1 も 0 も「ループに入らない」になり、**機械の節が空の文書ができて awk は 0 で終わる**。すぐ上のコメントに「書けていない staging を awk が読んで機械の節が空の文書を本物に被せる」と自分で書いておきながら、守っていたのは書き込み側だけだった。

`getline` の戻り値が負なら `exit 3` で落とし、あわせて**差し替えた結果に目印が1組あり機械の節が空でないことを、被せる前に確かめる**ようにした。

#### 残り2件

壊れた登記の番兵に NUL を使っていたので、bash がコマンド置換で NUL を捨てるときの警告が stderr へ出ていた（読み取りだけの経路が生の警告を吐く。#347 で直したのと同じ型）。`ok <parent_pane>` / `invalid` の2形に変えた。拍動の古さが取れないときに「unknown 秒前」と表示していたのも直した。

テストは 58 件から 67 件へ。

#### この回の型

**同じ事実の説明が2箇所にあると、片方だけ古くなる。** 直し方は文言を揃えることではなく、**説明の出どころを1つにすること**である。

## PR-2 — 席を取る（`take` と `start` と交代イベント）

### 枝の切り直しと、稼働中のものに触れない境界（2026-09-12）

PR-1 は squash で入ったので、前の枝のコミットは master の先祖ではない。`git reset --hard` を使わず、`git switch -c <新しい枝> origin/master` で切り直した。plan の digest は承認時のまま（`b80e548b…`）で、baseline は動いていない。

**PR-2 は初めて「書き換える」段である。** 稼働中の board と実物の登記に触れないために、外部の verb を呼ぶところを全部ノブにした（`OE_HANDOFF_REGISTER` / `OE_HANDOFF_SELFCHECK` / `OE_HANDOFF_SEND` / `OE_HANDOFF_TMUX` / `OE_HANDOFF_CLAUDE`）。テストは一時ディレクトリの fixture と stub だけを見る。`oe-register root --force` を自分のセッションで実行してはいない。

### Step 2-1 から 2-3 — 稼働中の見張りに手を入れる

`oe-vitals` の `現統括:` 解決を `lib/seat.sh` へ切り出した。**規則は1文字も変えていない。** 移設の前に基準を取り、あとで突き合わせた。

- 既存の回帰スイート `test_oe_vitals.sh` は移設の前後とも 77 件すべて PASS で、**出力は完全に一致**した。
- 実物の board に対する `oe-vitals` の出力も比べた。**違いは経過時間の表示だけ**（`beat stale/19h3m` → `19h4m`）で、解決した pane も判定も同じだった。時計の進みであって挙動の変化ではない。

移設前のコメント（`%NNN` が無い board で `set -e` が script を落とす、という実装SO の指摘の記録）は、理由が失われないように移設先と移設元の両方に残した。

### Step 2-4 と 2-5 — 交代イベント

`schemas/oe-events.schema.json` の type に `supervisor_succession` を足し、`generation` / `reason` / `server_pid` を必須にした。**from と to の role は空にする。** 交代は spawn の親子ではなく並列の継承なので、role に parent/child を焼くと「死んだ前任の下に後継がぶら下がる」歪みを記録の側から追認することになる（#238 / succession discussion §4-2）。`server_pid` を持たせたのは #290 §7.2 が「server identity を持たせると2つの穴が同時に閉じる」と書いた位置である。

**既存のログが新しい schema でも valid のままであることは、当てられる形で確かめた。** この repo に JSON Schema の validator は無く（board も envelope も手書きの bash+jq）、`jsonschema` も入っていない。だから schema 全体の検証ではなく、**変更で valid でなくなりうる経路だけ**を機械で見た。

- 既存 2,764 行の type は4種（`child_spawned` 175 / `message_sent` 1734 / `prompt_received` 775 / `report_received` 80）で、全部が新しい enum に入る。
- 既存行に `supervisor_succession` は0件なので、新しい条件節はどの既存行にも当たらない。
- top-level に `additionalProperties: false` が無いので、任意項目を足しても既存行は落ちない。`endpoint` の定義は触っていない（diff で0行）。

**「schema 全体を検証した」とは言えない**ので、そう書かない。当てたのは上の3点である。

### Step 2-6 と 2-7b — `take` と `start`

`take` は前提を全部確かめてから書き換える。確かめる側は、引き継ぎ文書の有無 / 前任の pane / 生きた委譲子が0件 / 前任の session_id / 見張りの状態。書き換える側は、自己登記 / board の張替 / 検算 / イベント。どこで落ちても「済んだこと」と「済んでいないこと」を並べて出す。

`start` は素の tmux split で `claude` を起こす。`oe-delegate` は使わず `PARENT_TMUX_PANE` も渡さない（DJ-11）。起動と kickoff を分け、ペインが一覧に現れてから引き継ぎ文書のパスを送る。

### テストが自分のコードの欠陥を1件捕まえた

`oe-selfcheck` の終了コードで判定しない、という要件のテストを書いたら落ちた。原因は自分のコードで、こう書いていた。

```bash
sc_json="$("$OE_HANDOFF_SELFCHECK" --json 2>/dev/null)" || sc_json=""
```

**出力は捕まえているのに、終了コードが非0なら捨てていた。** `oe-selfcheck` は broken が1つでもあれば 1 を返すので、いま `screen-marker` が broken である以上、この行は常に出力を捨てる。要件を「終了コードで判定しない」と書き、実装では `||` で判定していた。

昇格の印: 要件を満たしたつもりの行が、`||` ひとつで要件の逆をやっていた

### ゲートの結果

- `shellcheck`: 7ファイルとも exit 0
- テスト: `test_handoff_prepare` 67 件 / `test_handoff_take` 55 件 / `test_oe_vitals` 77 件 / `test_event_bus` 95 件、いずれも全件 PASS
- `validate-board.sh`: **実 board の写し**（390KB）に対して張替の前後で走らせ、WARN は1件のまま・出力も同一。張替後の解決は後継を指す
- 稼働中のものが無傷であることの確認: 実 board の宣言はいまも `%84`、最終更新は 2026-09-11 14:17 のまま、実物の登記は4件のまま、実物のイベントログに交代イベントは0件

### 実装SO（ゲート4）— 席を扱うコード特有の欠陥が出た（2026-09-12）

`oe-review --lanes 2` を回した。結果は `refuted`（2/2）で、**PR-1 までとは種類の違う欠陥**が出た。PR-1 は「観測できなかったことを値に畳む」型ばかりだったが、今回は**書き換える段に固有の**ものが並んだ。

#### 検査した相手と書き換える相手がずれていた

`--predecessor` で渡された pane を board と突き合わせていなかった。古い pane を渡すと、**その pane の子と session_id を検査したうえで、いま座っている統括の席を上書きできる。** 検査は通るのに、書き換える先が別物である。

同じ形で、検査から書き換えまでのあいだに別の後継が席を取っていた場合も、黙って上書きする。`oe_seat_rewrite` に期待する現在値を渡せるようにして、**書き換える直前にもう一度確かめる**形（compare-and-swap）にした。渡された前任が board の宣言と食い違うときは進まない。例外は board が既に自分を指しているときだけで、これは席を取ったあとにイベントの記録だけ落ちた場合の修復にあたる。

#### イベントの読み直しが「過去の同じ行」で素通りしていた

emit の戻り値を信用せず読み直す、というところまでは書いていたが、**読み直しが「同じ from/to の行がログのどこかにあるか」だった。** 過去に同じ交代が記録されていれば、今回の emit が落ちても成功と判定する。emit の前の行数を覚えて、それより後ろだけを見るようにした。あわせて `type` で選ぶようにした（文字列の `grep` だと、別のイベントの本文に型名が入っているだけで拾う）。

#### awk に値を渡すと backslash が再解釈される

board の行を `awk -v repl="$new_line"` で渡していた。**`-v` は値の中の backslash をエスケープとして解釈する**ので、散文に `\t` や `\\` が入っていると席と日付以外が静かに変質する。行数は変わらないので、後続の検査も素通りする。`ENVIRON` 経由に変えた。

このテストを書くときに自分でも同じ罠を踏んだ。fixture を `printf '…a\tb…'` で作ったので、**format の中の `\t` を printf が TAB に変えてしまい**、「literal な backslash が残るか」を試したつもりで別のものを試していた。引数側（`%s`）で渡す形に直した。

#### `printf` の format が `-` で始まると落ちる

`printf '--- 済んでいないこと ---\n'` は bash が `--` を option と解釈して失敗する（実機で確認し、`無効なオプションです` が出た）。報告の見出しが毎回落ちていた。`printf '%s\n' '--- … ---'` にした。

#### 見張りの状態を表示するだけで、確かめたことにしていた

`oe-selfcheck` の行を読んで表示はするが、`broken` でも `indeterminate` でも読めなくても、そのまま登記と board の書き換えへ進んでいた。**「見張りの生存を確かめてから書き換える」という前提に対して fail-open だった。**

ここは**席を取ることそのものは止めない**と決めた。取らないと交代が止まり、context を使い切った統括が座り続けるほうが害が大きい。ただし**「確かめた」とは書かない**ようにして、確かめられなかったことを「済んでいないこと」に出す。あわせて、`watchdog-freshness` が見ているのは `oe-confirm` であって `oe-vitals` ではないので、**oe-vitals の最終走査は確かめられていない**と毎回明示する（そういう記録がそもそも無い）。

#### `start` の待ちが「入力を受け付けられる」ことの確認になっていない

ペインの存在は作った直後から成立するので、待っても TUI の初期化は保証されない。しかも待ちが切れても無条件に送っていた。**現れなければ送らない**ようにし、落ち着くまでの待ちを足した。そのうえで、**存在の確認は readiness の確認ではない**とコメントに明記した。画面を読んで判定する形は採らない（版で消える目印に頼る形になる）。

#### ゲートの結果

- `shellcheck`: 7ファイルとも exit 0
- テスト: prepare 67 / take 76 / oe-vitals 77 / event-bus 95、いずれも全件 PASS
- `validate-board.sh`: 実 board の写しに対して張替の前後で WARN 1 件のまま・出力も同一
- 実物は無傷: board の宣言は `%84` のまま、実イベントログの交代イベントは0件

### 実装SO の2周目 — 確認と書き込みの間が空いていた（2026-09-12）

1周目の指摘を直して回し直した（最終 HEAD に束縛した成果物を残すため）。また `refuted` で、3件出た。

#### 確認しただけで、割り込みを止めていなかった

1周目の指摘で「書き換える直前にもう一度確かめる」（compare-and-swap）を入れた。**そこで満足していた。** 2周目が突いたのは、**確認と書き込みのあいだが空いている**点である。後継が2つ同時に走ると、両方が同じ前任を見て両方が `rc=0` で書き、あとの `mv` が先の席を消す。検算も順序次第で両方通り、交代イベントが2本残る。

`mkdir` でロックを取る形にした（POSIX で原子的）。**「確かめた」と「割り込まれない」は別のことだった。** 1周目の指摘を「確認を足せばよい」と読んで、その1段下を見ていなかった。

昇格の印: 競合の手当てで「確認を足す」と「間を閉じる」を取り違える

#### server_pid を足した目的を、自分で破っていた

修復の経路（board が既に自分を指しているとき）で、交代イベントの有無を「宛先 pane が同じ行があるか」で調べていた。`server_pid` は「非空か」しか見ていなかった。**pane 番号は server をまたぐと再利用される**ので、別の server の時代に同じ番号だった pane 宛てのイベントを「今回の交代の記録」と誤認して、未記録なのに記録済みと報告する。

`server_pid` を足したのは、まさにこの区別のためだった。**足した値を照合に使っていなかった。**

#### 入力を検証せずに board へ書いていた

`--generation abc` でも `-1` でも通り、board には「統括abc代目」と書いたうえで、イベント側だけが値を 0 に畳んで記録を省く。**呼び方の誤りが、board を部分的に更新した不整合として残る。** 引数の時点で落とすようにした（`--reason` も同様）。

#### SO の周回をここで止める

`so.impl` は weak で契約上は1周である。2周目まで回したのは、1周目の修正でコードが変わり成果物の束縛が古い版のままになるためで、**PR-1 のときに3周で止めたのと同じ理由・同じ止め方**をとる。3周目は回さない。2周目の指摘は3件とも反映した。

テストは 76 件から 86 件へ。ロックの競合・server_pid の照合・入力の検証を見る。

### Copilot のレビュー — 自分で足した仕掛けの、その先が空いていた（2026-09-12）

4件付いた。**4件とも妥当で、4件とも直した。** 反論したものは無い。

- **ロックに置き去りの回収が無かった。** 2周目で `mkdir` のロックを足したが、途中で落ちるとロックだけが残り、**board が永久に塞がる**。持ち主の pid を書き、生きていなければ引き取る形にし、割り込みで抜けるときも subshell の `EXIT` で外すようにした。**仕掛けを足したら、その仕掛けが壊れたときのことまで書く必要があった。**
- **schema の説明に「role は空」と書いただけで、検証していなかった。** `endpoint.role` の enum は `parent` / `child` / 空を許すままなので、spawn の親子として書かれた交代イベントが valid のまま通る。**契約と検証がずれていた。** 条件節で `const: ""` に固定した。
- **テストが何も検証していなかった。** `ck "board を書き換えない" "$(cat "$CAS")" "$(cat "$CAS")"` と書いていて、**自分自身と比べていた**。呼び出しの前に控える形に直した。この単位で「テストが欠陥を固定する」型は4回目である。
- **世代が分からないときに board へ「統括代目」と書いていた。** 世代不明のまま席を取る経路は自分で許してあるのに、その経路が散文を壊す形になっていた。

昇格の印: 自分で足した安全装置の、壊れたときの経路を書き忘れる

#### ゲートの最終結果

- テスト: prepare 67 / take 96 / oe-vitals 77 / event-bus 95、いずれも全件 PASS
- `shellcheck`: 7ファイルとも exit 0
- 実物は無傷: board の宣言は `%84` のまま、ロックの残骸も0件

### 統括のレビュー（PR-2）— 機械は正しいのに、人が読む1行が矛盾していた（2026-09-12）

WARNING 2件・SUGGESTION 2件。**4件とも直した。** 返したものは無い。

#### 直し方を選んだ理由（WARNING 1・統括が「選んだ理由を残せ」と指示した件）

張替の註を新しい pane の直後に挿す設計なので、そのすぐ後ろに前任の parenthetical がそのまま続く。結果として**同じ1行が2つの世代を続けて名乗る**。機械の解決は正しい（後継の pane が先に来るので `resolve` は合う）が、**board の line 3 は後継がいちばん最初に読む場所**で、この単位が作ろうとしているのはまさにその引き継ぎの質である。

統括は案を2つ挙げた。(a) 自分の註の末尾を「以下は前任の記録:」で閉じる。(b) 前任の parenthetical の先頭に label を挿す。

**(a) を選んだ。理由は2つある。**

第一に、**(b) は既存の散文の中へ機械が文字を入れる**。この lib の方針は「系譜の散文を機械が畳まない」で、消さないだけでなく**触らない**ところまで含めて守りたい。(a) なら機械が書くのは自分が足した註の内側だけで済む。

第二に、**印の射程が違う**。(b) は直後の1つの parenthetical に label を付けるが、実際の board では註のあとに parenthetical が何個も続く（系譜が積み上がっている）。**「これより後ろは前任までの記録である」と書けば、行の残り全部を過去だと宣言できる。** 個々に label を付けて回る形にすると、付け忘れた要素が現在の記述に見える。

実 board の写しに当ててみると、後継の宣言 → 印 → 14代目の記録、と読める形になった。テスト [32] が、印が在ることと、**印が前任の記録より前に来ること**（後ろにあっては意味がない）の両方を見ている。

#### ロックの二重解放 — 自分の計測が間違っていた

統括は「`rmdir` が2回呼ばれている」と実測を添えて指摘した。**自分で確かめたら1回に見えたが、確かめ方が間違っていた。** 呼び出し回数をシェル変数で数えたので、subshell の中で増えた分が親へ戻らず、しかも出力を `>/dev/null` で捨てていた。ファイルに書いて数え直したら**2回**で、統括の実測どおりだった。

**「自分で確かめた」の中身が、確かめたい事象を観測できない形だった。** この単位で何度も出ている型（観測できていないことを結論に使う）が、今度は自分の検算の側に出た。

実害は、subshell の trap が外した**あと**に別のプロセスが `mkdir` で取ったロックを、外側の `rmdir` が消しうること。**ロックが防ぐために在る状態そのもの**になる。解放を trap の1箇所へ寄せた。

置き去りロックの引き取りにも同じ形の窓があると統括が指摘したので、そちらは**「退かす」のでなく「自分の名前で持ち去る」**形にした（`mv` で改名し、成功した1つだけが片づける）。`rmdir` で退かすと、同じく置き去りと判断した別のプロセスが先に取ったロックを消しうる。

昇格の印: 検算の方法が、確かめたい事象を観測できない形になっていないか

#### SUGGESTION 2件

`old` と `expect` を検証していなかった。**いまの `take` 経由では届かない**（呼び出し側が board と突き合わせて止める）が、この lib は復帰側（#355）からの再利用を前提に書いてある。**呼び出し側に同じガードがあることを当てにしない。** 署名のコメントに `[<expect>]` が無かった点も直した。安全装置は、省略しても呼び出しが成功してしまうので、署名に出ていないと外れたことに気づけない。

#### ゲートの結果

- テスト: prepare 67 / take 107 / oe-vitals 77 / event-bus 95、いずれも全件 PASS
- `shellcheck`: 7ファイルとも exit 0
- `validate-board.sh`: 実 board の写しに対して張替の前後で警告は増えていない
- 実物は無傷: 宣言は `%84` のまま、ロックと残骸は0件

### 取りこぼし — 同じ署名が2箇所にあり、直したのは片方だけだった（2026-09-12）

SUGGESTION 2（署名に compare-and-swap の引数が無い）を直したつもりで、**直したのはヘッダの一覧だけだった。** 関数ごとの説明ブロックの署名は5引数のまま残っていて、統括に取りこぼしを指摘された。

説明ブロックの本文には `<expect_pane>` の段落が在るので、**署名だけが欠けている**状態だった。詳しい説明のほうを読んで写す人ほど、署名行から引数を数えて呼ぶので、compare-and-swap が黙って落ちる。この lib は #355 からの再利用を前提に書いてあるので、そこが効く。

**これは PR-1 で直した「同じ事実の説明が2箇所にあると、片方だけ古くなる」と同じ型である。** あのときは「文言を揃えるのでなく、説明の出どころを1つにする」と書いた。にもかかわらず、今回は**説明を2箇所に置いたまま片方だけ直した**。型を記録しても、次の同じ場面で適用できるとは限らない。

いまは両方に署名がある状態のままにした。片方へ寄せる（説明ブロックから署名行を落としてヘッダの一覧だけにする）案も考えたが、関数の直前に署名が無いほうが読みにくく、この lib は「関数単位で読んで写される」ことを想定しているので、**2箇所に置いたうえで、食い違っていないことを見る**ほうを採った。`grep -n 'oe_seat_rewrite <board>'` で2行とも `expect` を含むことを確かめた。

昇格の印: 同じ署名を2箇所に置くと決めたなら、食い違いを見る手段も一緒に決める

### baseline を守る規律が、進捗の記録まで止めていた（2026-09-12）

owner の指摘で分かった。**PR-1 と PR-2 が終わっているのに、master の plan は step が1つもチェックされておらず、`status` も `draft` のままだった。**

原因は規律の読み方にある。ゲート3 で「plan の digest が動いたら申告せよ」「step の削除や要件の弱体化があれば止めよ」と言われ、以後の報告でも「digest 不変」を良い知らせとして扱い続けた。**その結果、plan を凍結物として扱った。** 承認後に step が削られるのを防ぐための仕組みが、**進捗を書き残すことまで抑え込んでいた。**

自分の側の誤りをはっきり書く。**「digest 不変」を成果として報告し続けた。** 報告のたびに「baseline 3件は1つも動いていません」と書いたが、**plan が動かないことは、この段階では良い知らせではなかった。** 実装が2本進んでいるのに記録が動かないのは、記録が止まっている印である。規律が禁じていたのは母集団を壊す変更であって、進捗の記録ではなかった。**禁止の規則を、その規則が触れていない行為にまで広げて適用した。**

統括が規則を2つに分けた。

- **禁止（承認の母集団を壊す変更）**: step を削る / 要件を弱める / ゲートを飛ばす / 終端を書き換える。実行の前に照会する。
- **必要（進捗の記録）**: 済んだ step にチェックを入れる / 実施の結果を書き足す / `status` を実態に合わせる。**各 PR で必ずやる。**

どちらも digest は動く。**動いたこと自体は問題ではなく、差分がどちらに当たるかを見る。**

#### 今回やったこと

PR-1 と PR-2 の 27 項目にチェックを入れ、`status` を `draft` から `in-development` にした。あわせて、**step の文言と結果がずれた箇所は「結果:」として各行の下に残した。** チェックを入れるだけだと、ずれたことが消えるからである。7 箇所ある。とくに次の3つは、済んだと書くだけでは嘘になる。

- テンプレートの置き場を `docs/` 配下でなく `templates/` にした（step の文言と違う場所）。
- 実 board に対する `oe-vitals` の出力は「完全に一致」ではなく、**経過時間の表示だけが違った**。
- 既存のイベントログの検証は **schema 全体の検証ではない**。当てたのは3点だけである。
- `start` の「入力を受け付けられるようになってから送る」は**満たせていない**。確認できるのはペインが現れるところまでである。

step の集合が承認時と同一（削除も追加も無い）ことと、step の本文が1文字も変わっていないことを、承認版との突合で確かめた。

#### この構造について

統括は #391（承認ゲートの裁定に従うと baseline が動く）と同じ根だと書いた。あちらは「裁定に従うほど baseline が壊れる」、こちらは「baseline を守るほど記録が止まる」である。**どちらも、凍結を守る規律と、前に進んだことを書く規律が同じ対象の上で衝突している。** 材料として #391 へ繋がる。

昇格の印: 禁止の規則を、その規則が触れていない行為まで広げて適用していないか

