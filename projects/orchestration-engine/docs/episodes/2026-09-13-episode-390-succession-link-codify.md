---
id: "01M2D54AA82BZZ339Q9AQV8AZK"
title: "#390 交代の直後に踏む工程をガードレールのスキルへ書く"
date: 2026-09-13
type: episode
status: stable
related:
  - type: derived_from
    ref: "https://github.com/stlwolf/ai-development-hub/issues/390"
    reason: "本 episode の作業対象"
  - type: relates_to
    ref: "projects/orchestration-engine/docs/plans/2026-09-13-plan-390-succession-link-codify.md"
    reason: "この episode が記録する作業の計画"
  - type: relates_to
    ref: "projects/orchestration-engine/docs/episodes/2026-09-12-episode-390-retire-and-docs.md"
    reason: "前の単位。follow-up の表と受入の境界がこの単位の入力になっている"
tags: [orchestration, succession, doc-flow-guardrail, skill, handoff]
promotion:
  - subject: "工程を書く場所と、その場所が読まれる経路は別に決まる"
    verdict: required
    ref: "本文: 後継がスキルを読む導線が無いことが分かった（2026-09-13）"
  - subject: "上流が固定した受入の境界は「どこまで確かめるか」であって「何を確かめるか」ではない"
    verdict: required
    ref: "本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）"
  - subject: "引用した一次情報と食い違う自分の断定が、同じ節に同居する"
    verdict: required
    ref: "本文: 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）"
  - subject: "枠の薄さと受入の検査可能性が衝突したとき、受入を優先して検算を枠に残した"
    verdict: required
    ref: "本文: 反映しきらなかった指摘（残る緊張として記録する）"
  - subject: "前の単位が「残る」と書いた制約を、当て直さずに次の単位の設計へ写した"
    verdict: not-required
    ref: "本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）"
  - subject: "上流が与えた識別子を自分で組み立て直して検証し、外れると上流を疑う"
    verdict: required
    ref: "本文: ゲート3 を通過し、scope が5点になった（2026-09-13）"
  - subject: "指摘を受けて直すとき、同じ理由が当たる箇所を同じファイルの中で数え直していない"
    verdict: required
    ref: "本文: Copilot の指摘（ゲート4・2026-09-13）"
  - subject: "訂正を episode に書いて、上流の設計文書へ戻していなかった"
    verdict: not-required
    ref: "本文: Copilot の指摘（ゲート4・2026-09-13）"
  - subject: "つけかえの順序を retire --execute のあとに置く"
    verdict: not-required
    ref: "本文: 順序が結果を変えることが分かった（2026-09-13）"
  - subject: "検算を root の本数ではなく前任の不在と子の出現で見る"
    verdict: not-required
    ref: "本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）"
  - subject: "報告の宛先を出所の種類ではなく新しさで決める"
    verdict: not-required
    ref: "本文: 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）"
  - subject: "規範の適用範囲とゲートの検査範囲を独立に決めた"
    verdict: not-required
    ref: "本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）"
knowledge_observations:
  - "01M1VTJJFF1TK067ZX3X912G0P"
  - "01KYMRE1NC7XX6N66RQ0MGGHF1"
  - "01KYMRE1N7N0J8VP3CBZZEZ8Q3"
  - "01KYJ76D7CS7EBY3WDYY1NS9Y2"
  - "01M1TK0A7AX20V0XKWYNAR5WM5"
---

# #390 交代の直後に踏む工程をガードレールのスキルへ書く

**この episode は着手時に枠を作り、作業しながら追記している。** 後追いで再構成した節にはその旨を冒頭に書く。

## この単位の入力

前の単位（PR #394）の closure が、follow-up のうち3件を次の単位へ割り当てた。対象は `canonical/skills/doc-flow-guardrail/SKILL.md` の3点で、受入の境界も closure が明示している。詳細は plan の「背景」と「受け入れの境界」に書いた。

## 作業の記録

### ブリーフの断定を当て直した（2026-09-13）

統括のブリーフが挙げた前提8件を merged master で確かめた。**5件は一致し、3件は食い違った。** 内訳は plan の「ブリーフの断定を当て直した結果」に書いた。

食い違いのうち2件は、そのまま作業の中身を変える。

- **直す行は1箇所ではなく2箇所だった**（`SKILL.md:71` と `:72`）。ブリーフは1箇所として書いていた。
- **negative knowledge item `01M1TK0A7AX20V0XKWYNAR5WM5` の置き場が違った。** ブリーフが書いた `docs/harness/knowledge/items/` には無く、実在するのは engine 木のほうである。**closure の観測はこちらへ書き戻す。**

3件目は作業を変えないが、主張の強さを変える。ブリーフは「あなたの `PARENT_TMUX_PANE` が何を指していても」と書いていたが、**この子の環境変数は正しい宛先を指していた。** 交代のあとに起きた子だからである。**確かめられたのは「交代前に起きた子では古くなる」ことまでで、「常に古い」ことではない。**

### 順序が結果を変えることが分かった（2026-09-13）

`bin/oe-register:185-190` の guard を読んで、`oe-register link` が前任の生死で分岐することが分かった。**前任が停止済みなら orphan の引き取りとして `--force` 無しで通り、生きていると拒否される。** つけかえを `retire --execute` の前に置くか後ろに置くかで、必要な引数も、GC が前任の root 登記を掃くかどうかも変わる。

**ブリーフはこの分岐に触れていない。** 「交代の直後に踏む工程」としか書いていないので、順序は自分で決める必要があった。plan の DJ-2 に理由を書いた。

### 後継がスキルを読む導線が無いことが分かった（2026-09-13）

`templates/handoff.md.template` と実物の引き継ぎ文書（112行）のどちらにも、`doc-flow-guardrail` / `oe-register` / `cold-start` の3語が1つも出てこない。**この単位でスキルに工程を書いても、後継が引き継ぎ文書だけで進めば読まれない。**

昇格の印: 工程を書く場所と、その場所が読まれる経路は別に決まる。前の単位の判定6（verb のあいだの工程が落ちると完走しない）は、書く場所を決めただけでは閉じない。

範囲外なので実装せず、plan の「範囲外」と「owner に預ける判断」に surface した。

### 昇格判定6 の実行先がブリーフから落ちていた（2026-09-13）

前の単位の closure は判定6 を `required`・置き場は decision とし、**実行の置き場をこの単位に指定している。** ブリーフの scope は `SKILL.md` の3点だけで、decision に触れていない。**明示的に「decision は立てない」を選んだのか、判定6 が落ちたのかが読み取れない。** plan の「owner に預ける判断」に案A / 案B として出した。

### 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）

`so-compare` を既定の2レーン（codex と claude）で1回回した。**2レーンとも「要修正」で返り、指摘を実物で当て直した結果、反証できたものは1件も無かった。** 変更の内訳は plan の「設計SO で変えたこと」の表に書いた。証跡は `tmp/so-design-20260913-193652/`（揮発するので表のほうが正本）。

**いちばん重い指摘は検算が空振りすることだった。** 初稿は「`cockpit` の root が1本であること」を検算に書いていたが、`bin/oe-tree:641-654` を読み直すと root は「parent 参照のうち entry が無い pane」と「parent が空の entry」の和である。前任の登記が GC で消えると、まだつけかえていない子が居てもラベルが `?` に変わるので、**子が1体も付いていない状態でも「`cockpit` の root は1本」が真になる。** 検算を「前任の pane が木に現れないこと」と「件数の一致」へ書き直した。

**2番目は受入が変更を見ていなかったことである。** owner が明示した3項はコマンドの存在・引数・出力だけを見るので、`SKILL.md` を1文字も編集しなくても全部通る。**境界（実運用の交代までは求めない）は動かさず、静的な受入6項を足した。**

昇格の印: owner が定めた受入の境界は「どこまで確かめるか」を決めるもので、「変更そのものを見なくてよい」とは別である。境界をそのまま写すと、変更を見ない受入ができあがる。

**3番目は前提そのものの誤りである。** 初稿は「子が0体の交代では書き込みの契機が無いので root が2本並ぶ姿は残る」と書いていた。これは前の単位の episode の記述をそのまま写したものだが、`bin/oe-register:158-162` を読むと `oe-register root` は冪等に再登記でき、その書き込みが GC を呼ぶ。**契機は作れるので、記述を撤回した。**

昇格の印: 前の単位が「残る」と書いた制約を、当て直さずに次の単位の設計へ写した。上流が断定していたのは前の単位の自分たちである。

### ゲート3 を通過し、scope が5点になった（2026-09-13）

owner が「推奨で go」を出し、4件とも推奨どおりに裁定された（`.oe/addendum-390-guardrail-hg-go.md`）。**scope は `SKILL.md` の3点から5点になった** — cold-start の入口条件（120行目）と、既存の素の `oe-register` の呼び方（127行目）が加わった。増えたのは同じ節の中の数行である。

**統括から訂正を1件受け、こちらの誤りだと確認した。** plan の「食い違ったもの」に書いた「negative knowledge item の置き場がブリーフと違う」は誤りである。ブリーフの `item:` は engine 木のパスを書いており、実在もそちらである。`docs/harness/` なのは `source:`（収穫元 episode）のほうで、**item と source を取り違えて、harness 側のパスを自分で組み立てたうえで「実在しない」と判定していた。**

昇格の印: 「ブリーフの断定を当て直す」工程そのものが、当て直しの対象を取り違える形で外れた。検索が0件を返したとき、パターンではなく**自分が組み立てた対象パス**のほうを疑う必要があった。

**確かめ直した結果、残った事実はある。** item の置き場（engine 木）と `source.ref`（harness 木）は実際に別の蒸留木であり、本スキルが定める「item は `source.ref` と同じ木」と食い違う。**範囲外の記述をこの範囲だけに絞り直した。**

### 実装とゲートを通した（2026-09-13）

`canonical/skills/doc-flow-guardrail/SKILL.md` に5点の編集を入れ、ゲートを4本通した。

**GATE 1-a（受入 1・2・3）。** `oe-register --help` の口が手順の記述と一致する。guard（`bin/oe-register:185-193`）は「対象の子の親が非空・自分でない・生存」の3条件で拒否し、gone なら orphan として素通りする。`root` の冪等再登記（同 `:158-165`）は `record_or_die` を通り、その先で GC が走る（`lib/delegate-registry.sh:78`）。`oe-tree` を実行し、親子と自分の位置が読める形で出ることを確認した。root 合成の規則（`bin/oe-tree:641`）と、entry も `pane_title` も引けないときラベルが `?` になること（同 `:634-639`）も読み直した。

**GATE 2-3（受入 2・3）。** `oe-send [--brief <path>] [--] <target> [ad-hoc...]` の口と、書いた例の並びが一致する。空の target を実際に渡すと `oe-send: target is required` で exit 2 になった（`<報告先>` を使う根拠）。`%N` が登記を通さず素通しすることと、`oe-report` が `PARENT_TMUX_PANE` を先に読むことも実物で確認した。

**GATE 4-a（受入 1）。** #355 は OPEN、`bin/` に `oe-reseat` は0本、129行目が指す discussion は実在、#238 と #239 はどちらも OPEN。

**GATE 5（静的受入）。** 設計SO の指摘で8項から始め、**実装SO と Copilot の反映で 23 項になった。最終の実行で 23/23 が OK である。** 内訳は「手順5 が retire の出力から取ると書いてあるか」「検算が pane の不在と子の出現で書かれ、root の本数では書かれていないか」「入口条件に後継が入っているか」「宛先の順序が一意か」「例がシェルで壊れないか」「129行目の復旧の routing が残っているか」など、**変更そのものを見る項だけで構成されている。**

あわせて3つの設定ルートの `skills/doc-flow-guardrail` を `readlink` で照合し、いずれも hub の同じディレクトリを指していることを確認した（sync は実行していない）。**ただしこれはこの diff の配布の証拠ではない** — 3リンクは master の作業ツリーを指しているので、確かめられたのは「3ツールが同じ1ファイルを見る構造である」ことまでである。

### 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）

**2レーンとも「このままのマージには反対」で返った。** 指摘を実物で当て直した結果、**反証できたものは1件も無かった。** 証跡は `tmp/so-impl-20260913-214945/`（揮発するので下の表が正本）。

**いちばん重いのは、配布物へ書き込んだ事実そのものが誤っていたことである。** 初版の「報告の宛先」の項に「`oe-send` は生の `%N` を登記を通さず素通しするので、**死んだ pane でも**送信そのものは成功しうる」と書いた。**これは誤りである。** 素通しするのは登記による解決だけで、生存確認はその下の transport が必ず行う（`lib/delegate-send.sh:188-200`）。実際に存在しない pane へ送ると `oe_send_line: target pane not found: %99999` で exit 1 になる（実行して確認）。

**しかもこの誤りは、自分が読んだ `oe-send --help` の出力と正面から食い違っていた。** その出力には「宛先が無い（生存しない `%N`・解決できないラベル・曖昧なラベル）→ 1」と書いてあり、plan にはそれを引用してある。**引用した2行あとで逆のことを書いていた。** 根拠にしたのは `oe_reg_resolve` のコメント1箇所だけで、その下の層を読んでいない。

昇格の印: 同じ節の中に、引用した一次情報と、それと食い違う自分の断定が同居していた。**引用は「読んだ」ことの証拠にはなるが、「その内容に合わせて書いた」ことの証拠にはならない。** 両者が別の工程であることは negative knowledge item `01KYMRE1NC7XX6N66RQ0MGGHF1` が言っているとおりで、**上流の断定ではなく自分の断定で同じ型を踏んだ。**

**2番目は、自分で作った穴である。** 起動方法の註（`command -v` で確かめる）を手順4 に置いたが、**手順5 は「後継は手順4 を打たない」と指示していた。** 後継が飛ばす行に、後継が要る註を入れていた。`oe-register` は3ツールのどこにも配られていないので、hub の外に居る後継は `exit 127` を踏む。

**3番目は、engine が明示的に拒んでいる畳み方を、スキルの側が黙って復活させていたことである。** `oe_hs_children_of` が数えられないとき、`take` も `retire` も「0 件ではありません」とわざわざ書き分けている。初版の手順5 は一覧が必ず出る前提で書かれていたので、この表示に当たった後継は素直に読めば0件へ倒れる。

| 指摘 | どちらのレーン | 当て直した結果 | 対応 |
| --- | --- | --- | --- |
| 「死んだ pane でも送信は成功しうる」は実装と食い違う | claude | `lib/delegate-send.sh:188-200` を読み、実際に `%99999` へ送って exit 1 を確認。**指摘が正しい** | **記述を撤回し、「生存しない `%N` は exit 1 で弾く」「ただし exit 0 は配送の成功を意味しない」に書き直した** |
| 起動方法の註が、後継が飛ばす手順4 にしかない | claude | `scripts/sync/sync-bin.sh:64` の配布8本に `oe-register` が無いことを再確認。**指摘が正しい** | **手順5 の末尾に verb の起動方法の routing を足した** |
| 一覧が出なかったときの分岐が無く、0件に畳む | 両方 | `bin/oe-handoff:536-537` と `:1076-1077` の書き分けを確認。**指摘が正しい** | **「0件に畳まず `oe-tree` で前任の下を拾う」を足した** |
| 「一覧の各 `%N`」はラベル中の `%N` も拾いうる | codex | 一覧は `<pane> <label>` の形で、`--label` は LF/CR しか拒否しない（`bin/oe-register:79-83`）。**指摘が正しい** | **「各行の先頭フィールドだけを採る」に限定した** |
| 優先順位と「食い違ったら送らない」が矛盾し、正常な交代直後に必ず停止する | codex | 交代後は brief も環境変数も古いのが想定状態なので、常に食い違う。**指摘が正しい** | **順序で決めることに一本化し、停止は「明示の宛先を確かめられないとき」に限定した** |
| 「直近」だけでは遅着した古い明示が新しい後継を上書きする | codex | 生の `%N` は登記を介さず解決される。**指摘が正しい** | **「明示を受けたら、その pane が `oe-tree` に生きた root として出ることを確かめてから採る」を足した** |
| GC は best-effort で、掃けないことがある | 両方 | `lib/delegate-registry.sh` に掃除せず正常終了する枝が複数あることを確認。**指摘が正しい** | **「GC は掃けないことがあるので、消えたかは木で見る」に弱めた** |
| 「`retire` の出力が毎回この穴を明示する」は成り立たない | claude | 該当の文面（`bin/oe-handoff:1214-1219`）は `--execute` が停止と不在確認まで通った経路にしか出ない。**指摘が正しい** | **「毎回」を撤回し、経路を限定した** |
| 「後継は手順4 を打たない」は無条件には成り立たない（`take` は自己登記の手前で抜ける枝が複数ある） | claude | `bin/oe-handoff` の早期 return を確認。**指摘が正しい** | **`take` の「済んだこと」に出ていれば要らない、という条件付きに直した** |
| `<報告先>` は shell として壊れる（`<` はリダイレクト）うえ、隣の double-quote の指示が宙に浮く | 両方 | そのとおり | **`%N` へ変え、quote の規則を「変数で渡すなら」の条件付きに直した** |
| pane 再利用で link が拒否されたとき `--force` を足すのは危険 | codex | guard は「生きた別親」と判定する（`bin/oe-register:185`）。**指摘が正しい** | **「拒否されたら `--force` を足さず、pane が再利用されていないかを確かめる」を足した** |
| 登記 JSON が壊れている場合は前任の生死に関わらず `--force` を要求する | claude | `bin/oe-register:191-194` を確認。**指摘が正しい** | 手順は `--force` を足さない側に倒しているので、記述は変えない（`orchestration-toolkit` への routing で足りると判断） |
| 「件数の一致」は後継が自分の子を持たないことを前提にしている | claude | そのとおり | **「引き受けた子が自分の下に出ること」に書き換え、件数の一致という言い方をやめた** |

### 反映しきらなかった指摘（残る緊張として記録する）

**両レーンとも「手順5 は薄い枠の宣言と両立していない」と言い、claude は「残すのは第1文と宛先の通知の2つ」と具体的に提案した。** 手順5 は6行から5行に減ったが、**それ以上は削っていない。** 理由は、**owner が固定した受入の3項目めが「手順が書く検算が、そのコマンドの出力で実際に取れる」だからである。** 検算を落とすと、受入の1項が検査対象を失う。詳細（guard の分岐・`--force`・GC）は `orchestration-toolkit` への routing 1行に畳んだが、**移設先への追記そのものは範囲外なので今回はやっていない。** 枠の厚みと受入の要求がここで衝突している。

**codex の「3つの symlink の照合はこのコミットの証拠になっていない」も正しい。** 3リンクはいずれも master の作業ツリーを指しており、この worktree は指していない。**確かめられたのは「3ツールが同じ1ファイルを見る構造である」ことまでで、「この diff が3配布先で読める」ことではない。** マージされるまで配布先には届かない。

### Copilot の指摘（ゲート4・2026-09-13）

**「Changes recommended」で、重大1件・中程度4件・軽微5件が返った。全件が正しく、全件を反映した。** 実装SO が見なかった箇所を4件拾っている。

| 指摘 | 重要度 | 当て直した結果 | 対応 |
| --- | --- | --- | --- |
| **停止から link までの間に `%N` が別のペインに再利用されると、無関係な相手を子に登記する。** `oe-register link` は相手が live かと現在の親しか見ず、一覧の時点の session や世代とは結び付かない（`bin/oe-register:182-207`） | 重大 | コードを読んで**正しいと確認** | **「間に別の作業を挟まず、停止の直後に打つ」を足した**（窓を狭める）。**照合つきの口が要ることは範囲外として surface 済み** |
| **`oe-tree` の root は「親が空」か「親の登記が無い」pane だけなので、入れ子の委譲では親が中間ノードになり、「root として出る」を満たさない**（`bin/oe-tree:642-653`） | 中 | `本文: 実装とゲートを通した（2026-09-13）` で読んだ合成規則と一致。**正しい** | **「生きたノードとして出る」に直し、交代の直後だけ root になることを注記した。** 実装SO が見落とした箇所である |
| **`delegate-task` が同じ配布物の中で `oe-send "$PARENT_TMUX_PANE"` を指示し続けるので、宛先の規則が競合する** | 中 | 実物に在る（`canonical/skills/delegate-task/SKILL.md:218-241`） | **他スキルを触らずに済む形で、固定節に「この規則が優先する」を1句足した。** `delegate-task` 自体の修正は範囲外のまま |
| **`oe-register root` の再登記では子は移らないので、「前任が残っていたら書き込みを起こす」では直らない** | 中 | そのとおり。GC が掃くのは登記であって親子関係ではない | **「まずつけかえ残しを疑う」と「子が0体のときだけ再登記」に分けた。** 初稿は2つの症状を1つの処方で書いていた |
| **`oe-register link <%N>` はシェルへ貼ると `<` がリダイレクトとして解釈される** | 軽微（3票） | そのとおり | **`%N` に直した。** 固定節では同じ理由で `<報告先>` を `%N` に直していたのに、手順5 では同じ罠を作っていた |
| **`retire` の子の一覧は `    - %N ラベル` の形なので、「先頭フィールド」をそのまま採ると `-` を渡すことになる**（`bin/oe-handoff:1081-1083`） | 軽微（3票） | コードで**確認**。実装SO の指摘（ラベル中の `%N` を拾う）を反映したときに、箇条書き記号のほうを見落としていた | **「箇条書き記号を落とした先頭フィールド」に直した** |
| **静的受入の件数が plan と episode で食い違う**（plan は6項・episode は8項） | 軽微（3票） | そのとおり | **plan 側に「ゲート4 のあと15項になった」と書き、内訳の在り処を示した** |
| **plan の設計の前提に `oe-send` の誤った記述が残っている** | 軽微（2票） | そのとおり。**episode では訂正したのに plan を直していなかった** | **plan の4箇所すべてを訂正した** |
| **plan の受入条件が「件数の一致」のままで、実装の「子が自分の下に出る」とずれている** | 軽微（2票） | そのとおり | **plan を実装に合わせ、変えた理由も書いた** |
| **plan の範囲外に 127行目の行が残っている**（ゲート3 で scope に入った） | 軽微（1票） | そのとおり | **裁定で scope に入ったと書き直した** |
| **episode が `status: draft` で closure が未記入** | 軽微（3票） | レビュー時点ではそのとおり | **closure を書き、`stable` に確定した**（Copilot はこのコミットの前に見ている） |

**この回で分かったことが2つある。**

**ひとつは、同じ罠を1つのファイルの中で片方だけ直していたことである。** 固定節では `<報告先>` がシェルで壊れるという理由で `%N` に直したのに、**同じコミットで書いた手順5 には `<%N>` を残していた。** 直す理由は共有されていたのに、適用範囲を同じファイルの中でも広げていない。

昇格の印: 指摘を受けて直すとき、同じ理由が当たる箇所を同じファイルの中で数え直していない。**指摘された箇所だけを直すのは、指摘を「その行の問題」として読んでいるということである。**

**もうひとつは、上流の文書（plan）への back-propagation を落としていたことである。** `oe-send` の誤りは episode で訂正したのに、**同じ誤りが plan に4箇所残っていた。** 訂正を「この単位の記録」に書いて、「この単位の設計根拠」には戻していない。

## closure（ゲート5・マージ前）

### Step 1: tier 判定 = **heavy**

heavy トリガに4つ当たる。**実行中に撤回が6回あった**（plan の初稿全体・「root が2本残る」・`oe-send` の生存判定・`<報告先>` という差し込み記号・「宛先は root として出ることで確かめる」・「前任が残っていたら再登記で直る」）。**意図的に起動した外部レビューを3回通した**（設計SO・実装SO・closure の外部チェック、いずれも弱・2レーン）。**これに常設の Copilot が加わる。****非自明な設計判断があり、棄却した案を具体名で挙げている**（置き場4案・順序2案・宛先4案・129行目3案）。**昇格候補がある**（下記の判定8件）。

### Step 2: closure gate checklist

- **Context / なぜ**: 冒頭の「この単位の入力」に、前の単位（PR #394）の closure が3件を割り当てたことと、受入の境界がそこにあることを書いてある。**発端そのもの（交代で後継がつけかえを飛ばし、owner が画面を見て指摘した）は plan の「背景」に在る。**
- **次の消費者**: 3つある。**(1) 次に統括を交代する世代**（手順5 を読んで踏む）。**(2) #355（復旧側）を実装する単位**（129行目の分割がその境界を確定した）。**(3) `delegate-task` の同じ記述を直す単位**（範囲外として surface した）。
- **follow-up routing**: 下の表に全件。行き先の無い箇条書きは置いていない。
- **昇格の判定**: 8件を1件ずつ下に判定した（frontmatter の `promotion` と1対1）。
- **status 確定**: `draft` → `stable`。**達成度は「達成」。** scope の5点はすべて入り、静的受入と owner の受入3項がその場のコマンドで通っている（件数は `本文: 実装とゲートを通した（2026-09-13）` が正本で、ここには書かない）。
- **evidence anchor**: SO の生出力は `tmp/` で揮発するので、**指摘の全件を表へ転記した**（設計SO は plan の「設計SO で変えたこと」節、実装SO と closure の外部チェックは本 episode）。コマンドの出力は `本文: 実装とゲートを通した（2026-09-13）` へ file:line 付きで書いた。**ゲート3 の baseline の digest も、`.oe/` が gitignored なので plan へ転記した**（closure の外部チェックの指摘）。
- **SO 証跡リンク**: 設計SO `tmp/so-design-20260913-193652/`・実装SO `tmp/so-impl-20260913-214945/`・closure の外部チェック（Step 4）は下記。Copilot は PR #396 のレビューに残る（**揮発しないので転記は要点のみ**・`本文: Copilot の指摘（ゲート4・2026-09-13）`）。
- **観測の書き戻し**: **注入された5件すべてに1レコードずつ書き戻した**（`validate-knowledge` が両 store で OK）。item id は frontmatter の `knowledge_observations` に残した。内訳は `followed` 4件・`injected_not_used` 1件である。

### この単位の中心 — 配布物へ書き込んだ事実そのものが誤っていた

**この単位でいちばん重いのは、手順が抜けていたことではなく、それを埋める文に事実の誤りを入れたことである。** 「`oe-send` は死んだ pane でも送信が成功しうる」と書いた。実装は exit 1 で弾く。

**しかも否定する一次情報を、自分で引用済みだった。** plan の2行前に `oe-send --help` の「宛先が無い（生存しない `%N`）→ 1」を引いてある。**引用と、それと食い違う自分の断定が、同じ節に同居していた。**

**実害は、後継がいちばん要る手がかりをスキルが否定する形だったことである。** 交代のあと前任のペインは閉じているので、古い宛先へ送った子は exit 1 で即座に気づける。同じ固定節が「送信後に exit code を確かめる」と教えているのに、その exit code の意味を1つ上の項が打ち消していた。

**検出したのは実装SO の1レーンだけである。** 設計SO も、自分のゲート照合も、ここを踏んでいない。理由は明白で、**自分のゲートは「書いた検算が取れるか」を見ており、「書いた事実が正しいか」を見ていなかった**（`本文: 反映しきらなかった指摘（残る緊張として記録する）`）。

### 同じ型が、この単位の中で3回出た

**「確かめた範囲より広く書く」が4回である。** 前の単位の closure が「世代をまたいで3度目」と書いた型で、**同じ流れの次の単位でさらに4回出た。**

- **item の置き場**（`本文: ゲート3 を通過し、scope が5点になった（2026-09-13）`）。自分で組み立てたパスが見つからないことを「brief が誤り」と結論した。統括が訂正した。
- **「root が2本残る」**（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`）。前の単位の episode の記述を、当て直さずに設計へ写した。設計SO が撤回させた。
- **`oe-send` の生存判定**（上の節）。引用した一次情報と食い違う断定を書いた。実装SO が検出した。

- **「宛先は `oe-tree` に生きた root として出ることで確かめる」**（`本文: Copilot の指摘（ゲート4・2026-09-13）`）。交代の直後だけで成り立つ性質を、すべての委譲に当たる条件として書いた。Copilot が検出した。

**4回とも外部が見つけており、自分では1件も見つけていない。** 前の単位の closure が判定10 で「止め方が分かっていない」と `unknown` にした型は、この単位でも止まっていない。**しかも4件目は、実装SO の2レーンを通り抜けて配布物に残り、Copilot が拾った。** 「外部が見つける」と言っても、**どのゲートが見つけるかは安定していない。** 撤回までは到達しているが、**「検出の側は働いている」と言い切れるのは、ゲートを4段重ねた結果としてである。**

### 昇格の判定

**本文の印は6件**（`昇格の印:` の行）。**印が無い判断も独立に問い、6件見つけた。** 合計12件を1件ずつ判定する。**closure の外部チェックで2回直している** — codex レーンが「3つの設計判断を1件に畳んでいる」と言って3件に割り、claude レーンが「印が6本あるのに5本しか数えていない」と言って6本目と、その節のもう1つの判断を足した。frontmatter の `promotion` と1対1に対応する。

| # | 判断 | 判定 | 理由 |
| --- | --- | --- | --- |
| 1 | **工程を書く場所と、その場所が読まれる経路は別に決まる**（`本文: 後継がスキルを読む導線が無いことが分かった（2026-09-13）`） | **`required`** | Q1: 置き場3案（スキル / `retire` の出力 / 引き継ぎ文書のテンプレート）が live だった。Q2: 議論側で、実物を見ても決まらない（厚い手順 vs 読まれる場所）。Q3: 前提は「導線が machine-local の board にしかない」で、テンプレートに1行入れば偽になる。Q4: verb を足すたびに再訪される。**置き場は decision。実行はこの単位の外**（下の routing） |
| 2 | **上流が固定した受入の境界は「どこまで確かめるか」であって「何を確かめるか」ではない**（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`） | **`required`** | 再発する失敗の型なので**置き場は knowledge item**。Step 5 で `01M2DEC1G68X8PH2QEW2H1DWZZ` として同じ PR に収穫した |
| 3 | **引用した一次情報と食い違う自分の断定が、同じ節に同居する**（`本文: 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）`） | **`required`** | 同じく再発する失敗の型で**置き場は knowledge item**。Step 5 で `01M2DEC1G6GG9RTXGM6CRJEGZQ` として収穫した。既存の `01KYMRE1NC7XX6N66RQ0MGGHF1` は**上流の断定**が対象で、こちらは**自分の断定**なので trigger が別である |
| 4 | **枠の薄さと受入の検査可能性が衝突したとき、受入を優先して検算を枠に残した**（`本文: 反映しきらなかった指摘（残る緊張として記録する）`） | **`required`** | Q1: 両レーンが「1行に削れ」と具体案つきで提案し、live だった。Q2: 議論側（枠の設計原則 vs 受入の検査可能性はどちらも価値判断）。Q3: 前提は「受入の3項目めが検算を見る」で、受入が変われば偽になる。Q4: このスキルへ何かを足すたびに立つ。**置き場は decision。実行はこの単位の外** |
| 5 | **前の単位が「残る」と書いた制約を、当て直さずに次の単位の設計へ写した**（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`） | `not-required` | **既存の `01KYMRE1NC7XX6N66RQ0MGGHF1`（上流の断定を写すとき）に着地済み**で、前の単位の episode は上流に当たる。**書き戻した観測が触れているのは brief の断定と `oe-send` の2件で、この件には触れていない**（closure の外部チェックの指摘。観測は append-only なので書き換えず、ここに書く） |
| 6 | **上流が与えた識別子を自分で組み立て直して検証し、外れると上流を疑う**（`本文: ゲート3 を通過し、scope が5点になった（2026-09-13）`） | **`required`** | **初稿では `not-required`（既存 item に着地済み）としたが、closure の外部チェックが覆した。** 既存の `01KYMRE1N7N0J8VP3CBZZEZ8Q3` が求める行動変更（対照を同じ経路に通す・生の件数も取る・経路と不在を分けて書く）は、**今回の失敗を防がない** — 検索の経路は健全で、壊れていたのは入力のほうだからである。対照を通せば正しくヒットして「経路は生きている」と確認できてしまう。**再発する失敗の型なので置き場は knowledge item。** Step 5 で `01M2DF73WN60TQS6XED0NNHKB7` として収穫した |
| 6-b | **指摘を受けて直すとき、同じ理由が当たる箇所を同じファイルの中で数え直していない**（`本文: Copilot の指摘（ゲート4・2026-09-13）`） | **`required`** | **closure の外部チェックが「印が6本あるのに5本しか数えていない」と指摘して見つかった。** `<報告先>` を直した理由が、同じコミットで書いた `<%N>` に及んでいなかった。**置き場は判定3 の item（`01M2DEC1G6GG9RTXGM6CRJEGZQ`）と近いが trigger が別**（あちらは書くとき、こちらは直すとき）。**この単位では収穫せず、次に同じ型が出たときに判断する**（過剰収穫を避ける） |
| 6-c | **訂正を episode に書いて、上流の設計文書（plan）へ戻していなかった**（`本文: Copilot の指摘（ゲート4・2026-09-13）`） | `not-required` | **back-propagation の規律は `episode-retrospective` の closure gate checklist が既に持っている。** 新しい判断ではなく、既存の項目を履行し損ねただけである |
| 7 | **つけかえの順序を `retire --execute` のあとに置く**（`本文: 順序が結果を変えることが分かった（2026-09-13）`） | `not-required` | **1段目で外れる。** 順序とその理由（guard が前任の生死で分岐する）がスキル本文に書いてあり、正本だけを読んで正しく使える |
| 8 | **検算を root の本数ではなく前任の不在と子の出現で見る**（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`） | `not-required` | **1段目で外れる。** 見方と「なぜ root の本数では見ないか」がスキル本文に書いてある |
| 9 | **報告の宛先を出所の種類ではなく新しさで決める**（`本文: 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）`） | `not-required` | **1段目で外れる。** 順序と検証の手順がスキル本文に書いてある |
| 10 | **規範の適用範囲とゲートの検査範囲を独立に決めた**（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`） | `not-required` | **既存の `01KYJ76D7CS7EBY3WDYY1NS9Y2` に着地済み。** 同じ item に観測を書き戻した |

**判定1と判定4 は `required` だが、この単位では実行しない。** 本スキルの範囲が判定までであり、かつ owner が「decision は立てず、ガードレールへの記載で足りる」と裁定している（判断1・案A）。**裁定は前の単位の判定6 に対するもので、この2件を名指してはいない**ので、行き先は下の routing に置いて統括へ surface する。

### owner の条件への応答 — 判定6（前の単位）を decision にしなかった理由

**ゲート3 の裁定が案Aに条件を付けた**（「`required` と判定されたものを decision にしない選択なので、立てない理由を episode に残すこと」）。理由は2つある。

**ひとつは、判定6 の内容が手順として着地したことである。** 判定6 は「verb が実物になっても、verb のあいだを繋ぐ工程が落ちていれば交代は完走しない」だった。この単位はその工程を cold-start の手順5 として書き、入口条件も直した。**decision が持つべき「なぜそう決めたか」のうち、実行に要る部分はスキル本文に入っている。**

**もうひとつは、decision に残すべき「棄却した案と覆すコスト」が、この単位の plan に durable に在ることである。** 置き場4案とその棄却理由は `projects/orchestration-engine/docs/plans/2026-09-13-plan-390-succession-link-codify.md` の「棄却した案」節にあり、plan は同じ PR で committed 層へ着地する。**decision を立てないことで失われるのは、その内容へ辿る導線の短さだけである。**

**失われたものを正直に書く。** 判定6 は「verb の集合で足りるか、あいだの工程まで手順に含めるか」という**再訪される分岐**である。#395 が申告の形式を変えるときにも立つ。**decision が無いと、その単位は plan の「棄却した案」まで降りないと前回の判断を知れない。** これが案Aの代償である。

### follow-up の行き先

| 残課題 | 行き先 |
| --- | --- |
| **判定1（工程を書く場所と読まれる経路は別）の昇格の実行** | **この単位の完了報告に明記して統括へ渡す**（新しい issue はブリーフが禁じている）。owner が置き場（decision を立てるか、ガードレールの記載で足りるとするか）を決める。**判断材料は「導線が machine-local の board にしかなく、配る成果物の側に無い」こと**（ゲート3 での統括の訂正） |
| **判定4（枠の薄さと受入の衝突）の昇格の実行** | **同じく完了報告に明記して統括へ渡す。** **このスキルへ次に何かを足す単位が同じ分岐に立つ** |
| `oe-handoff` の `take` / `retire` の出力につけかえの工程を足す | **完了報告に明記して owner の判断を仰ぐ。** 効き目ではスキルより上だが engine のコードなので触っていない。**この単位だけでは #390 の穴は閉じない**ことを PR 本文にも書いた |
| 引き継ぎ文書のテンプレートへの導線 | **owner 裁定でこの単位では触らない**（判断4）。統括が別途扱う。新しい issue も立てない |
| `canonical/skills/delegate-task/SKILL.md:218-241` が同じ古い宛先を教えている | **緩和済み + 完了報告に明記。** Copilot の指摘を受けて、固定節に「この規則が他スキルの記述に優先する」を足したので競合は解消している。**`delegate-task` 本体の修正は次の単位**で、置き場の決定は統括に渡す |
| `oe-register` / `oe-send` / `oe-handoff` が `~/bin` へ配られない | **追わない。** `orchestration-toolkit` が既に規約を持ち、この単位はそこへ routing した。**配布を増やすかは engine の判断で、この単位の外** |
| 子が自分の現在の親を機械で引く口が無い | **追わない。** engine に verb を足す話で、宛先の規則は `oe-tree` を目で読む形で成立している |
| `--force` 無しで安全に前倒しできる照合つきの口が無い | **追わない。** 手順は `--force` を足さない側に倒したので、無くても成立する |
| `oe-send` が生の `%N` の同一性を検証しない（pane 番号の再利用） | **追わない。** 手順5 と宛先の規則の両方に「再利用を疑う」を書いた。engine 側の検証は別単位 |
| `take` と `retire` の子の一覧がスナップショットで、機械可読な最終集合が無い | **追わない。** 手順を「`retire` の出力から取る」「数えられなければ `oe-tree` で拾う」に倒したので、無くても成立する |
| negative knowledge item `01M1TK0A7AX20V0XKWYNAR5WM5` の置き場と `source.ref` が別の蒸留木にある | **追わない（store 側の判断）。** 観測の書き戻し先は現在の置き場で正しく、動かすかは store の保守側が決める |
| board の succession 手順の前任 step 3 が、裁定で覆った規則をまだ書いている | **解決済み（closure で実物を確認した）。** 現行の board の該当行は「生きた委譲子が残っていても交代は止まらない（owner 裁定 2026-09-13。この行は裁定より前に書かれたもので、2026-09-13 に15代目が直した）」になっている。**前の単位の follow-up が「次の単位が同じ PR で直す」としていたが、統括が先に直していた。** 追う先は無い |
| **前の単位の episode（`projects/orchestration-engine/docs/episodes/2026-09-12-episode-390-retire-and-docs.md`）に、この単位が撤回した記述が残っている**（「子が0体の交代では契機が無いので、そのときは残る」） | **追わない。** episode は**その時点の記録**なので、後から真になった事実で書き換えない層である（`document-format.md` の層の定義）。**この単位の episode が撤回を記録し、配布物である skill が正しい記述を持つので、消費者は困らない。** 前方参照としてこの行を残す |
| **engine の `bin/README.md` に、この単位で撤回した断定が2件残っている。** ひとつは「`retire` の出力は穴の在り処を毎回明示する」（実際は `--execute` が停止まで通った経路にしか出ない）。もうひとつは「root が2本並ぶのは残っている歪み」（冪等な再登記で GC の契機を作れるので、子が0体でも解消できる） | **この単位では直さない**（ブリーフが `bin/README.md` を範囲外にした）。**完了報告に明記して統括へ渡す。** 実害は小さい（どちらも安全側の記述で、読んだ人が余分に確かめるだけ）が、**スキル本体と engine の README が食い違ったまま残る** |
| **`oe-handoff` が tmux 上の Claude セッションに固有であること**（`retire` は前任のペインの先頭プロセス名が `claude` でなければ閉じない） | **追わない。** 手順5 は「計画的な交代の後継として起きたなら」で条件づけられているので、他のツール文脈では発火しない。**engine 側の一般化はこの単位の外** |
| **plan の「検証していない前提」4件**（工程が実際に踏まれるか / `oe-register link` を実打していない / 3ツールの実起動環境 / Cursor と Codex がこのスキルを自動で読むか） | **前3件は次の交代で観測される**（踏まれたかは `oe-tree` に出る）。**4件目は追わない** — 読まれ方はツール側の仕様で、この単位では変えられない |
| 「確かめた範囲より広く書く」型の止め方 | **#395 と同じ単位**（前の単位の判定10 の routing を引き継ぐ）。**この単位で3回出て3回とも外部が検出したので、検出は効いていて予防は効いていないという観測が足せた** |
| 収穫した2件の item の効き目 | **追わない。** 次に trigger に当たった単位が観測を書き戻す（段5 の輪） |

### Step 3: 内容セクション（出力型 × 消費チャネル）

**事実・失敗**:

- **外部レビューの指摘は全件が表として本文にある。** 設計SO の16件は plan の「設計SO で変えたこと」の表、実装SO の13件は `本文: 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）` の表、Copilot の11件は `本文: Copilot の指摘（ゲート4・2026-09-13）` の表にある。**どの表も「指摘 / どちらのレーン / 当て直した結果 / 対応」を1行ずつ持ち、落としたものは無い。** 下の項はそのうち性質の違うものを名指ししたもので、全件の列挙ではない。
- 配布物へ書き込んだ事実そのものが誤っていた（`本文: この単位の中心 — 配布物へ書き込んだ事実そのものが誤っていた`）。
- 「確かめた範囲より広く書く」型が3回出て、3回とも外部が検出した（`本文: 同じ型が、この単位の中で3回出た`）。
- 検算が空振りする形で書かれていた。root の本数で見ると、子が1体も付いていなくても真になる（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`）。
- 受入が、変更を1文字も入れなくても全項通る形だった（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`）。
- **停止から登記のつけかえまでの間に pane 番号が再利用されると、無関係な相手を子に登記しうる**（`本文: Copilot の指摘（ゲート4・2026-09-13）`・重大判定）。
- **宛先の確認を「`oe-tree` に生きた root として出る」と書いたが、入れ子の委譲では親は中間ノードなので成り立たない**（同上）。
- **「前任が残っていたら再登記で直る」という処方が、子の移動には効かない**（同上）。1つの処方で2つの症状を書いていた。
- **同じ罠を1つのファイルの中で片方だけ直していた**（`本文: Copilot の指摘（ゲート4・2026-09-13）`）。
- **訂正を episode に書いて、上流の plan へ戻していなかった**（`本文: Copilot の指摘（ゲート4・2026-09-13）`）。
- 起動方法の註を、後継が飛ばすよう指示した行に置いていた（`本文: 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）`）。

**決定と根拠**: 置き場4案・順序2案・宛先4案・129行目3案の棄却理由は plan の「棄却した案」節にある。**とくに「`orchestration-toolkit` へ書いて routing する案」がスキル自身の宣言に照らして最も筋が良いこと**を認めたうえで、owner 裁定を理由に棄却した。

**わかったこと（W）**:

- **`oe-register link` の可否は前任の生死で決まる**（`本文: 順序が結果を変えることが分かった（2026-09-13）`）。停止済みなら orphan の引き取りとして `--force` 無しで通る。
- **`oe-register root` は冪等で、再登記の書き込みが GC の契機になる。** 子が0体でも契機は作れる（`本文: 設計SO（ゲート2・弱・2レーン）で plan を書き直した（2026-09-13）`）。
- **`oe-tree` の root は「親として参照されるが登記の無い pane」を含む。** 前任の登記が消えてもラベルが `?` の root として残る（同上）。
- **`oe-send` は生存しない `%N` を exit 1 で弾く**（`本文: 実装SO（ゲート4・弱・2レーン）で、自分が入れた事実の誤りが出た（2026-09-13）`）。

**原則**:

- **引用は「読んだ」証拠であって「内容に合わせて書いた」証拠ではない**（Step 5 で `01M2DEC1G6GG9RTXGM6CRJEGZQ` として収穫）。
- **上流が固定する受入の境界は上限であって、下限は自分で書く**（Step 5 で `01M2DEC1G68X8PH2QEW2H1DWZZ` として収穫）。

**行動変更**:

- **トリガ**: 計画的な交代で後継として起きたとき。**機構**: cold-start の入口条件が後継を名乗り、手順5 が工程を持つ。**着地先**: `canonical/skills/doc-flow-guardrail/SKILL.md`（この PR）。
- **トリガ**: 子が親へ報告する宛先を決めるとき。**機構**: 固定節の「報告の宛先」が順序と検証を持ち、可変節に欄がある。**着地先**: 同上。

**蒸留シグナル**:

- **decision 候補が2件**（判定1・判定4）。**行き先は統括の surface で、この単位では実行しない。**
- **knowledge store へ2件収穫した**（判定2・判定3 に対応）。
- **判定5・6・8 は既存 item に着地済みなので新規収穫はしない。**

**残課題**: 上の routing の表に全件ある。

### Step 4: heavy tier の外部チェック（実施した）

`so-compare` を closure の4観点（失敗の選択的省略 / routing の網羅 / evidence anchor / back-propagation）に絞って1回回した。**「まだ完全ではない」で高2件・中3件・低1件が返り、全件を反映した。** 証跡は `tmp/so-closure-20260913-221604/`（揮発するので下の表が正本）。

| 指摘 | 当て直した結果 | 対応 |
| --- | --- | --- |
| **実装SO・設計SO の指摘が事実・失敗から選択的に落ちている**（13件と16件のうち、名指しされているのは5項目） | 表は本文にあるが、**事実・失敗の側に「全件は表にある」と言う包括の pointer が無かった。** 指摘が正しい | **事実・失敗の先頭に、3つの表を名指しする包括 pointer を足した** |
| **前の単位が「同じ PR で直す」とした board の欠陥が、具体的な前方参照なしに再 defer されている** | **実物を見たところ、既に直っていた。** 現行の board の該当行は裁定後の内容で、「2026-09-13 に15代目が直した」と注記まで入っている | **「解決済み・追う先なし」に書き換えた。** 指摘は「宙に浮いている」で正しく、答えは「浮いていたのではなく済んでいた」だった |
| **engine の `bin/README.md` に、この単位で撤回した断定が2件残っているのに routing されていない** | 実物で**確認**（`bin/README.md:221` の「毎回」・`:224` の「残っている歪み」） | **follow-up の表に足した。** 範囲外なので直さず、完了報告で統括へ渡す |
| **follow-up に「行き先」と呼べない行があり、plan から落ちた残件もある** | そのとおり | **「統括へ surface」を「完了報告に明記して統括へ渡す」に具体化し、落ちていた2件（`oe-handoff` の tmux/Claude 固有・plan の未検証の前提4件）を足した** |
| **昇格判定7 が3つの独立した判断を1エントリに畳んでおり、分母が過小** | そのとおり。**「1判定 = 1エントリ」の契約に反していた** | **3件に割り、合計を10件にした。** 個々の `not-required` の判定自体は変えていない |
| **事実・失敗の pointer が実在する見出しと一致していない**（短縮表記・「同上」） | そのとおり | **全て実在する見出しの完全形に直した** |

**2レーン目（claude）は codex が見なかった点を7件出した。** 2レーンとも「まだ完全ではない」で、こちらも全件を反映した。

| 指摘 | 当て直した結果 | 対応 |
| --- | --- | --- |
| **Copilot ラウンドの失敗が事実・失敗に1行も入っていない。** 重大判定の「pane 番号の再利用」と、自分が配布物へ書いた誤り2件（`oe-tree` の root 条件・再登記の処方）が落ちている | そのとおり | **3件を事実・失敗に足した** |
| **手順5 の routing 先が、指した内容を持っていない。** `orchestration-toolkit` に在るのは起動方法だけで、guard と `--force` は `delegate-task`、GC の詳細はどちらのスキルにも無い | 実物で**確認**（`orchestration-toolkit` に `--force` 0件・GC 0件／`delegate-task` に guard と `--force` は在る） | **routing 先を実物どおりに書き分けた。** GC は「スキル層に無い」と明示し、engine の README と lib を指した |
| **静的受入の件数が3か所で食い違う**（plan は6項・episode は23項・Copilot 対応欄は15項） | そのとおり。**「同じ数を2箇所に書くと片方が古くなる」型の再発で、是正したつもりの plan にも数が残っていた** | **plan から件数を完全に外し、正本を episode の1箇所にした** |
| **ゲート3 の baseline の digest が committed 文書に無い。** `.oe/` は gitignored なので、マージ後に母集団を復元できない | `.gitignore` で**確認** | **digest 3件を plan へ転記した** |
| **昇格の印が6本あるのに、判定は5本ぶんしか数えていない** | 数えたところ**6本**（`grep '^昇格の印:'`） | **6本目と、その節のもう1つの判断を足して12件にした** |
| **判定6 は過小。** 着地先とされた item の行動変更（対照を同じ経路に通す等）は今回の失敗を防がない。検索の経路は健全で、壊れていたのは入力のほうだから | 既存 item の本文を読んで**確認** | **`required` へ覆し、3件目の item `01M2DF73WN60TQS6XED0NNHKB7` として収穫した** |
| **判定5 の理由の後半が、書き戻した観測の実物と合っていない**（観測は brief の断定と `oe-send` の2件に触れ、この件には触れていない） | 実物で**確認** | **観測は append-only なので書き換えず、判定表のほうに食い違いを書いた** |
| **前の単位の episode と engine の README に、この単位が撤回した記述が残っている**（「子が0体の交代では契機が無いので残る」） | 実物で**確認**（前の単位の episode の該当行・README） | **follow-up の表に足した**（README 分は codex レーンの指摘で既に入っていた） |
| 設計SO の転記先が「本文の表」と書かれているが、実際は plan にある | そのとおり | **転記先を文書ごとに書き分けた** |

**指摘の性質が、これまでのゲートと違った。** 設計SO と実装SO が見たのは「書いた内容が正しいか」で、この回が見たのは**「書いた記録が、起きたことを取りこぼしていないか」**である。**6件のうち4件は、内容ではなく記録の形の欠陥だった。**

**Step 4 は実施した（辞退していない）。** 2レーンとも返り、合計13件の指摘を全件反映した。

### Step 5: negative knowledge の収穫（in-PR 相乗り）

3件を収穫し、同じブランチにコミットした（`validate-knowledge` 通過）。**置き場はこの episode と同じ木**で、**収穫時は `docs/harness/knowledge/items/` だったが、owner 裁定（2026-09-14）で4層ドキュメントごと `projects/orchestration-engine/docs/knowledge/items/` へ移した**（経緯は次の単位の episode `2026-09-13-episode-390-predecessor-handoff-flow.md` の「4層ドキュメントを engine の木へ寄せた」節）。

- `01M2DEC1G6GG9RTXGM6CRJEGZQ` — 引用した一次情報と食い違う自分の断定が同居する。
- `01M2DEC1G68X8PH2QEW2H1DWZZ` — 上流が固定した受入の境界を写すと、変更を見ない受入ができる。
- `01M2DF73WN60TQS6XED0NNHKB7` — 上流が与えた識別子を組み立て直して検証すると、外れたときに上流を疑う方向へ倒れる（**closure の外部チェックで判定6 が覆ったので足した**）。

**既存 item との重複は列挙で確かめた。** 最も近いのは `01KYMRE1NC7XX6N66RQ0MGGHF1`（上流の断定）と `01KZ21M0N0FZ4YMR2R6DPHTWQ4`（合格条件）で、どちらも trigger が別である。

### Step 6: 注入された knowledge への観測の書き戻し

**slot の5件すべてに1レコードずつ書き戻した。** id は frontmatter の `knowledge_observations` に durable に残した。

| item | state | 要点 |
| --- | --- | --- |
| `01M1VTJJFF1TK067ZX3X912G0P` | `followed` | 既存規定を2つ狭めたが、どちらも消さずに残し、routing 先が同じ sync で3ツールへ配られることを確かめてから寄せた |
| `01KYMRE1NC7XX6N66RQ0MGGHF1` | `followed` | brief の断定8件を着手前に当て直した。**ただし自分の断定では同じ型を踏んだ** |
| `01KYMRE1N7N0J8VP3CBZZEZ8Q3` | `injected_not_used` | trigger に当たったが、brief 本文を読み直さずに「brief が誤り」と結論し、統括に訂正された |
| `01KYJ76D7CS7EBY3WDYY1NS9Y2` | `followed` | 規範の適用範囲とゲートの検査範囲を DJ-9 で別々に判断した |
| `01M1TK0A7AX20V0XKWYNAR5WM5` | `followed` | 前の単位の教訓を Step 0 として手順に落とし、食い違い2件を検出した。**手順化は検出を上げたが再発は止めていない** |

